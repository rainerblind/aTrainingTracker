# Project Verification & Testing Plan

This document defines the verification procedures for all project requirements. This project follows a **Test-Driven Development (TDD)** approach, where tests are defined before implementation.

## 1. Automated Unit Tests (SWE.4)

*These tests verify the core logic of individual software units without requiring a physical device.*

| Test ID | Jira ID | Component | Requirement ID(s) | Test Description | Status |
|:---|:---|:---|:---|:---|:---|
| **TST-UNT-001** | | `NumericalEncodingUtils` | `REQ-DAT-004` | Verify that encoding and decoding LatLng/Double lists result in zero data loss. | Verified |
| **TST-UNT-002** | | `TimeFormatter` | `REQ-FIL-003`, `REQ-UI-002` | Verify Removal of leading zeros and correct localized string generation for s, m:s, and h:m:s formats. | Verified |
| **TST-UNT-003** | | `EquipmentDiscovery` | `REQ-TRK-004`, `REQ-SET-003` | Verify sport-type resolution logic based on varied active sensor ID sets and speed. | Verified |
| **TST-UNT-004** | | `MovingAverageFilter` | `REQ-FIL-001` | Verify correct averaging of a sequence of numbers (Numbered and Timed variants). | Proposed |
| **TST-UNT-005** | | `ExponentialSmoothing` | `REQ-FIL-002` | Verify the recursive smoothing formula: `y[k+1] = α m[k] + (1-α) y[k]`. | Proposed |
| **TST-UNT-006** | | `SportTypeMapping` | `REQ-EXP-006` | Verify that `SportTypeDatabaseManager` correctly handles a `null` Strava mapping as a persistent state. | Proposed |
| **TST-UNT-007** | | `EditWorkoutViewModel` | `REQ-EXP-006` | Verify that selecting a sport type with a `null` Strava mapping automatically sets `uploadToStrava` to `0`. | Proposed |
| **TST-UNT-008** | | `Accumulator` | `REQ-PRO-003` | Verify that hardware deltas received during a pause are discarded, while deltas received while active are accumulated correctly. | Verified |
| **TST-UNT-009** | | `WorkoutDataMapper` | `REQ-UI-011` | Verify that the extrema rows only include the "Big 6" performance metrics: HR, Speed/Pace, Cadence, Power, Altitude, and Temperature. | Verified |
| **TST-UNT-010** | | `MappablePath` | `REQ-MAP-007` | Verify that coordinate projection (latLngs) is lazy and only occurs once. | Proposed |
| **TST-UNT-011** | | `TrackingViewsDb` | `REQ-PRO-005` | 1. Simulated fresh install (Wipe data): Verify schema contains ShowElevationProfile. 2. Simulated upgrade (v9->v10): Verify ShowElevationProfile is added. | Verified |
| **TST-UNT-012** | `ATT-457` | `WorkoutSummariesViewModel` | `REQ-PER-008` | Verify `getFilteredWorkouts` correctly filters by `sportId` (Sub-Sport) when provided. | Proposed |
| **TST-UNT-013** | `ATT-458` | `WorkoutSummariesViewModel` | `REQ-PER-008` | Verify `getFilteredWorkouts` correctly filters by `bSportType` (Base Sport) when `sportId` is null. | Proposed |
| **TST-UNT-014** | `ATT-476` | `WorkoutSummariesDbHelper` | `REQ-DAT-008` | Verify that `onUpgrade` does not call `setTransactionSuccessful()` explicitly, which would conflict with `SQLiteOpenHelper`'s internal transaction management. | Verified |
| **TST-UNT-015** | `ATT-502` | `WorkoutClusterEngine` | `REQ-SET-065` | Verify that 6-term altitude-aware cluster similarity calculation correctly applies displacement weights when altitude positions are present, differentiates routes with distinct peaks/valleys, and cleanly falls back to 4-term matching when altitude coordinates are null or disabled. | Proposed |
| **TST-FIL-002** | | `WorkoutSamplesDb` | `REQ-DAT-003` | Verify that `calcExtremaValue` returns `null` (not 0.0) when querying a sensor that has only NULL values or does not exist in the table. | Verified |
| **TST-EXP-004** | `ATT-517` | **TCX Schema Compliance Audit** | `REQ-EXP-001` | 1. Perform a TCX export of a workout containing Cadence and Power data. 2. Inspect the raw XML content. 3. Verify that `<Cadence>`, `<Watts>`, and `<RunCadence>` tags contain ONLY integer values (no decimal points). | Exported TCX file strictly uses integer formatting for cadence and power tags. | Verified |
| **TST-EXP-005** | `ATT-902`, `ATT-1105` | **Smart Asymmetric Strava Naming & Pre-Upload Discovery Test** | `REQ-EXP-008` | 1. Unit test pre-upload duplicate discovery (`findExistingStravaActivityForWorkout`): verify when Strava API returns an activity matching the workout's start time, TCX file upload (`POST /api/v3/uploads`) is bypassed, avoiding duplicate activity creation on Strava, and `doUpdate(isDuplicate = true)` is invoked.<br>2. Unit test pre-upload duplicate discovery when no matching activity exists: verify `findExistingStravaActivityForWorkout` returns null and normal TCX upload proceeds.<br>3. Unit test duplicate upload handling (`isDuplicate = true`) where Strava title is a default time-of-day name (e.g. `"Radfahrt am Morgen"`, `"Lauf am Abend ⛅"`, `"Morning Ride"`): verify Strava update request omits `name` (preserving Strava title) and local SQLite `WorkoutSummaries.WORKOUT_NAME` preserves local app workout name (not overwritten by default Strava title).<br>4. Unit test duplicate upload handling where Strava title is custom (e.g. `"Sunday Club Ride"`, `"Schwarzwald Tour"`): verify Strava update request omits `name` (preserving Strava title) and local SQLite `WorkoutSummaries.WORKOUT_NAME` is enriched with the custom Strava title.<br>5. Unit test `isDefaultStravaName()` regex evaluator across German and English time-of-day templates, with and without weather emojis (`⛅`, `🌧️`, `☀️`), and assert custom titles return `false`.<br>6. Unit test duplicate upload detection resilience: verify `checkAndUpdateDuplicate` identifies duplicate activity IDs when returned in `error` or `status` fields across varied phrasings (`"duplicate of activity <id>"`, `"duplicate of <id>"`).<br>7. Unit test fresh upload (`isDuplicate = false`) where workout name equals `fileBaseName`: verify Strava update request omits `name` parameter.<br>8. Unit test fresh upload where workout name is custom: verify Strava update request contains `name`.<br>9. Verify other metadata (`sportName`, `gearId`, `description`, `trainer`, `commute`) continue to be updated in both paths.<br>10. Unit test reactive in-memory cache update in `WorkoutRepository`: verify `reloadExportStatusesFor` refreshes `workoutName` in memory when updated in SQLite. | All test cases pass; existing Strava activities bypass TCX file upload, Strava titles are never overwritten, local app names are preserved when Strava title is default, custom Strava titles enrich local app database, and fresh uploads auto-name or serialize custom titles properly. | Verified |
| **TST-EXP-006** | `ATT-1114` | **Strava Duplicate Reconciliation Equipment, Retired Gear & Detailed Sport Type Enrichment Test** | `REQ-EXP-009` | 1. Unit test `EquipmentDbHelper.getIdFromStravaId`: verify returns matching local equipment `_ID` when present, or `-1` when absent.<br>2. Unit test `EquipmentDbHelper` database upgrade to version 2: verify `Retired` column is safely added via `ALTER TABLE` without dropping existing data and defaults to 0.<br>3. Unit test `SportTypeDatabaseManager.getSportTypeIdFromStravaName`: verify Strava sport type names (e.g. `"MountainBikeRide"`) map to the correct local `sport_id` (e.g. MTB).<br>4. Unit test `StravaUploader.doUpdate(isDuplicate = true)` with known local equipment: verify update payload sent to Strava omits `sport_type`/`type` and `gear_id` (preserving Strava cloud metadata), while local SQLite `WorkoutSummaries.EQUIPMENT_ID` is updated to the matching equipment ID.<br>5. Unit test `StravaUploader.doUpdate(isDuplicate = true)` with missing/retired gear: mock Strava `GET /api/v3/gear/{id}` returning `DetailedGear` with `retired = true`; verify new equipment record is created in `Equipment.db` using original Strava name with `Retired = 1`, and local `WorkoutSummaries.EQUIPMENT_ID` is updated.<br>6. Unit test `StravaUploader.doUpdate(isDuplicate = true)` with detailed sport type: verify local `WorkoutSummaries.SPORT_ID` is updated to MTB and Strava cloud sport type is not overwritten.<br>7. Unit test `WorkoutRepository.reloadExportStatusesFor`: verify `sportId`, `sportName`, `bSportType`, and `equipmentId` are reloaded from cursor into the in-memory cache.<br>8. Verify fresh upload (`isDuplicate = false`) continues to send local sport type and equipment to Strava. | All test cases pass, confirming accurate equipment and sport type enrichment from Strava, retired gear lazy fetch and storage with original name, preservation of Strava cloud metadata, and in-memory cache reactivity. | Verified |
| **TST-EXP-007** | `ATT-912` | **Strava Segment PB Feedback Ingestion, Database Update & UI Celebration Test** | `REQ-EXP-010` | 1. Unit Test `SegmentsDatabaseManager.updateSegmentPrTime`: verify returns `true` and updates `pr_time` when `newPrTime < currentPrTime` or `currentPrTime <= 0`; verify returns `false` and leaves row untouched when `newPrTime >= currentPrTime`; verify returns `false` when segment does not exist in `StarredSegmentsTable`.<br>2. Unit Test `SegmentsRepository.updateSegmentPr`: verify `allSegmentsWithPath` StateFlow updates the matching segment's `prTime_raw` and formatted `prTime` string reactively.<br>3. Unit Test `StravaUploader.doUpdate` with PR feedback: mock `getStravaActivity` returning JSON with segment effort having `pr_rank = 1`; verify `SegmentsDatabaseManager.updateSegmentPrTime` and `SegmentsRepository.updateSegmentPr` are called with correct segment ID and elapsed time.<br>4. Unit Test `StravaUploader.doUpdate` without PR feedback (`pr_rank = 2`, `pr_rank = null`): verify neither database nor repository PR methods are invoked.<br>5. Unit Test `StravaActivitySection` celebration banner: verify celebration banner renders at the top of the section when $\ge 1$ segment efforts have `prRank == 1` or `komRank == 1`; verify banner is omitted when zero rank-1 efforts are present.<br>6. Unit Test Localization Parity: verify celebration banner strings exist and pass translation tests across all 9 supported application locales (EN, DE, ES, FR, IT, JA, NL, PL, PT).<br>7. Invariants: verify `REQ-EXP-008` (smart naming), `REQ-EXP-009` (duplicate reconciliation), `REQ-UI-131` (segment accordion), and `REQ-UI-144` (best efforts) continue to pass without regression.<br>8. Clean-Room Regression: execute `./gradlew testDebugUnitTest`. | Segment PB is automatically updated in SQLite and in-memory flow upon Strava upload feedback; new PRs are celebrated with a dedicated banner in Workout Summary; all invariants remain preserved with 0 test failures. | Verified |
| **TST-EXP-008** | `ATT-913`, `ATT-1178` | **Strava Segments Mandatory Daily Automated Synchronization Test** | `REQ-EXP-011` | 1. Unit Test `StravaSegmentsSyncWorker.schedule`: verify when Strava is connected (`getStravaAccessToken() != null`), `enqueueUniquePeriodicWork` is called with fixed 1-day periodic request (`1, TimeUnit.DAYS`), `NetworkType.CONNECTED`, and `requiresBatteryNotLow(true)`; verify when Strava is disconnected, `cancelUniqueWork` is called on WorkManager.<br>2. Unit Test `StravaSegmentsSyncWorker.doWork`: verify when Strava is disconnected, worker returns `Result.success()` without network calls; verify when connected, worker invokes `SegmentsRepository.syncStarredSegments(BSportType.UNKNOWN)` and updates `SP_LAST_UPDATE_TIME_OF_STRAVA_SEGMENTS` with formatted timestamp upon success; verify worker returns `Result.retry()` on network/API exception.<br>3. Unit Test `StravaSettingsDialog` UI state: verify automated segments sync toggle and interval dropdown are omitted; verify manual update card displays formatted timestamp and triggers sync.<br>4. Invariants: verify `REQ-EXP-008`, `REQ-EXP-009`, `REQ-EXP-010`, `REQ-LIV-001` - `REQ-LIV-004` remain preserved with 0 regressions. | Mandatory daily segments synchronization reliably schedules with WorkManager with fixed 1-day interval and updates segments in the background; UI omits unnecessary interval controls; all tests pass. | Verified |
| **TST-EXP-009** | `ATT-914`, `ATT-1178` | **Strava Routes Mandatory Daily Automated Synchronization Test** | `REQ-EXP-012` | 1. Unit Test `StravaRoutesSyncWorker.schedule`: verify when Strava is connected (`getStravaAccessToken() != null`), `enqueueUniquePeriodicWork` is called with fixed 1-day periodic request (`1, TimeUnit.DAYS`), `NetworkType.CONNECTED`, and `requiresBatteryNotLow(true)`; verify when Strava is disconnected, `cancelUniqueWork` is called on WorkManager.<br>2. Unit Test `StravaRoutesSyncWorker.doWork`: verify when Strava is disconnected, worker returns `Result.success()` without network calls; verify when connected, worker invokes `RoutesRepository.syncRoutesFromStrava()` and returns `Result.success()` when sync succeeds; verify worker returns `Result.retry()` on network/API exception or when sync returns false.<br>3. Unit Test `StravaSettingsDialog` UI state: verify automated routes sync toggle and interval dropdown are omitted; verify manual update card displays formatted timestamp and triggers sync.<br>4. Invariants: verify `REQ-EXP-008` (smart naming), `REQ-EXP-009` (duplicate reconciliation), `REQ-EXP-010` (segment PB), `REQ-EXP-011` (segment periodic sync), and `REQ-ROU-001` (route clustering) remain preserved with 0 regressions. | Mandatory daily routes synchronization reliably schedules with WorkManager with fixed 1-day interval and updates routes in the background; UI omits unnecessary interval controls; all tests pass. | Verified |
| **TST-EXP-010** | `ATT-1190` / `ATT-1214`, `ATT-1215` | **Minimized Strava Activity Feedback Persistence & Backward-Compatible Rendering Verification** | `REQ-EXP-013` | 1. *Unit Test `StravaActivityParser.minimize` Data Stripping*:<br>• Provide a realistic raw Strava activity JSON containing athlete profile, kudos, comments, external map polyline URLs, photos, segment efforts, and best efforts.<br>• Verify the minimized JSON payload includes `"v": 2` and strictly preserves `id`, `segment_efforts`, and `best_efforts`.<br>• Verify all transient social fields (`kudos_count`, `comment_count`, `athlete`, `photos`, `map`) are 100% stripped.<br>2. *Unit Test Null & Default Value Omission*:<br>• Provide segment efforts and best efforts with null PR/KOM ranks and zero distances.<br>• Verify that null ranks and non-positive distances are omitted from the serialized JSON rather than serialized as explicit nulls.<br>3. *Unit Test Parser Backward Compatibility & UI Parity*:<br>• Parse both legacy full JSON (v1) and minimized JSON (v2) through `StravaActivityParser.parse()`.<br>• Verify returned `StravaActivity` models are identical in segment effort counts, PR ranks, starred flags, and best efforts.<br>• Verify `SegmentPrCelebrationBanner` renders identical celebration state.<br>4. *Unit Test Defensive Exception Handling & Truncation Resilience*:<br>• Feed truncated, malformed, and empty JSON strings to `StravaActivityParser.parse()`; verify it returns `null` safely without throwing runtime exceptions.<br>5. *Unit Test Strict CQS Invariance*:<br>• Verify `StravaUploadDbHelper.getStravaActivityData()` executes zero SQLite write operations or row updates.<br>6. *Unit Test SQLite Migration to DB_VERSION 6*:<br>• Initialize `StravaUploadDbHelper` at version 5 with legacy uncompacted JSON rows and malformed JSON rows.<br>• Trigger `onUpgrade` to version 6; verify legacy rows are migrated in-place to `"v": 2` schema, malformed rows safely become null/tombstone without halting migration, and unhandled disk errors roll back cleanly.<br>7. *Unit Test Disconnect Data Purge & WAL Checkpoint*:<br>• Verify `StravaDataPurgeManager.purgeAllStravaData()` clears `StravaUploads` table completely and triggers WAL checkpoint.<br>8. *Regression Suite*:<br>• Execute `./gradlew testDebugUnitTest` across all modules with zero regressions. | Data minimization achieves 90%+ storage reduction; transient social fields are completely eliminated; offline celebration and segment rendering retain 100% parity; CQS, DB_VERSION 6 migration, and disconnect wipe invariants are preserved. | Verified |
| **TST-EXP-011** | `ATT-1219` / `ATT-1226` | **Reactive Strava Segment Synchronization Progress & Transient Decoupling Verification** | `REQ-EXP-014` | 1. *Unit Test `SegmentsRepository.isSyncing` Lifecycle*:<br>• Verify `isSyncing` initial value is `false`.<br>• Trigger `syncStarredSegments(BSportType.UNKNOWN)` with mocked delay or suspended network fetch; verify `isSyncing` emits `true` during active execution across both Bike and Run worker phases.<br>• Verify `isSyncing` emits `false` immediately upon successful completion.<br>• Mock network/JSON parsing exception during `syncStarredSegments`; verify `isSyncing` resets cleanly to `false` in `finally`.<br>2. *Unit Test `syncStarredSegmentsWorker` Defensive Cleanup*:<br>• Mock an unhandled exception inside `syncStarredSegmentsWorker(BSportType.BIKE)`; verify `_refreshingSports` set does NOT retain `BSportType.BIKE` post-exception.<br>3. *Unit Test Persistence Invariant & Separation of Concerns*:<br>• Verify that during active synchronization (`isSyncing == true`), `TrainingApplication.getLastUpdateTimeOfStravaSegments()` is NOT mutated with `"now"` or any transient UI text.<br>• Verify `TrainingApplication.setLastUpdateTimeOfStravaSegments` is called ONLY at the conclusion of successful sync with a formatted date string.<br>• Verify if sync fails with an exception, `TrainingApplication.getLastUpdateTimeOfStravaSegments()` remains unchanged from its initial value.<br>4. *Unit Test / Compose Verification of `StravaSettingsDialog` UI Binding*:<br>• In `StravaSettingsDialog`, simulate `isSegmentsSyncing = true`; verify the rendered secondary text equals `stringResource(R.string.lastUpdateOfSegmentsNow)`.<br>• Simulate `isSegmentsSyncing = false`; verify the rendered secondary text equals `segmentsLastUpdate`.<br>• Simulate user clicks while `isSegmentsSyncing = true`; verify `syncSegmentsAsync` is not invoked (debounced).<br>5. *Unit Test 9-Language Resource Parity*:<br>• Verify string resource `lastUpdateOfSegmentsNow` is defined with valid non-empty translation across all 9 locales: `values` ("now"), `values-de` ("jetzt"), `values-es` ("ahora"), `values-fr` ("maintenant"), `values-it` ("adesso"), `values-ja` ("今"), `values-nl` ("nu"), `values-pl` ("teraz"), `values-pt` ("agora").<br>6. *Clean-Room Regression Suite*:<br>• Execute `./gradlew testDebugUnitTest` across all modules with zero regressions. | `SegmentsRepository.isSyncing` reactively reflects sync state; SharedPreferences is never polluted with transient text; UI displays localized "now" during sync; clicks are debounced; 9-language parity verified; 100% test pass rate. | Verified |

## 2. Regression & Manual Verification (SWE.5 / SWE.6)

These procedures ensure high-level system integrity and UI consistency.

| Test ID | Jira ID | Category | Requirement ID(s) | Procedure | Expected Result | Status |
|:---|:---|:---|:---|:---|:---|:---|
| **TST-REG-001** | | **Tracking** | `REQ-TRK-001`, `REQ-TRK-003`, `REQ-TRK-007`, `REQ-MAP-003` | Start workout, background app for 10 min, check bearing jitter. | Persistent 1Hz sampling, active notification, filtered map camera. | Proposed |
| **TST-REG-002** | | **Branding** | `REQ-EXT-001`, `REQ-EXT-002`, `REQ-MAP-005` | Navigate to Strava Segments list and Detail map. | Correct orange/white logos visible in headers and context rows. | Proposed |
| **TST-REG-003** | | **Cloud** | `REQ-EXP-001`, `REQ-EXP-002` | Complete workout with Strava enabled. | File export success UI and successful upload to third-party API. | Proposed |
| **TST-REG-004** | | **Pause Logic** | `REQ-PRO-003` | Connect hardware distance sensor. Start workout. Record distance. **Pause**. Continue movement (e.g., spin wheel). **Resume**. Record more distance. Repeat for all relevant protocols (ANT+/BTLE). | Total distance reflects only active movement. No distance increase or 'jump' is recorded during the pause phase. | Proposed |
| **TST-INT-002** | | **UI/Logic** | `REQ-EXP-006` | In Sport Type Editor, select "- No upload -". Then create/edit workout with this type. | Strava checkbox is unchecked and disabled in workout editor. | Proposed |
| **TST-INT-003** | | **Mapping** | `REQ-EXP-007` | In Sport Type Editor, select a new Strava type (e.g., Gravel Ride). Save and reopen. | The new type is persisted and correctly displayed in the editor and summary views. | Proposed |
| **TST-INT-004** | | **UI/Logic** | `REQ-UI-009` | In Tracking Tab Configuration, toggle "Show Elevation Profile". | Elevation profile is displayed below the map on the tracking screen. | Proposed |
| **TST-INT-005** | | **UI Layout** | `REQ-UI-010` | Open the Tab Configuration on a narrow device or split-screen. | Checkboxes wrap into multiple rows instead of overlapping. Labels remain fully visible. | Proposed |
| **TST-INT-006** | | **UI Layout** | `REQ-UI-010` | Audit the checkbox sequence in the tab configuration header. | Checkboxes appear in the exact order: Map, Elevation Profile, Live Segments, Lap Button. | Proposed |
| **TST-REG-005** | | **Map Context** | `REQ-MAP-008` | Open detail map. Verify active item is primary. Verify background items of same sport are muted (0.3). Verify background items of different sport are highly muted (0.1). | Full tiered spatial context is maintained. | Proposed |
| **TST-NAV-001** | `ATT-239` | **Navigation** | `REQ-SET-050` | 1. Open the navigation drawer and verify the new sections (Training, Spatial, My Equipment, Ecosystem, System). 2. Verify 'Sport Types' and 'Training Zones' are in 'My Equipment'. 3. Verify individual items for 'Strava' and 'Dropbox' are in 'Ecosystem'. 4. Open 'Preferences' and verify moved items are no longer present. | Drawer is organized logically. All promoted items are accessible and functional. Preferences screen is clean and focused. | Proposed |
| **TST-NAV-002** | `ATT-249` | **Navigation** | `REQ-UI-112` | 1. Open the navigation drawer. 2. Verify section "Equipment" (EN) / "Ausrüstung" (DE). 3. Verify section "Synchronization" (EN) / "Synchronisation" (DE). 4. Verify item "Dateiexport" (DE). | Drawer labels are updated to professional terminology across supported languages. | Verified |
| **TST-NAV-003** | `ATT-258` | **Direct Config Navigation** | `REQ-SET-051` | 1. Open Navigation Drawer -> Settings. 2. Tap 'Tracking Tabs'. | 1. An 'AlertDialog' to choose Activity Type appears immediately over the current screen. 2. Selecting a type opens the configuration cockpit (TrackingTabsFragment in CONFIGURATION mode) for that type. | Verified |
| **TST-NAV-004** | `ATT-261` / `ATT-1016` | **Direct Display Settings Dialog & State Parity Verification** | `REQ-SET-052` | 1. Ensure `pref_display_options` is unset in SharedPreferences (default state).<br>2. Open Navigation Drawer -> Settings -> Display.<br>3. Verify all three switches ("force portrait", "keep screen on", "keep screen unlocked") appear in the ON (checked) state, matching `TrainingApplication` defaults.<br>4. Toggle "keep screen on" to OFF, then toggle "keep screen unlocked" to OFF. Verify both switches remain OFF and SharedPreferences is updated with `["forcePortrait"]`.<br>5. Toggle "force portrait" to OFF and dismiss dialog. Verify `MainActivityWithNavigation` dynamically clears orientation lock (`SCREEN_ORIENTATION_UNSPECIFIED`) and clears `FLAG_SHOW_WHEN_LOCKED`.<br>6. Run automated regression suite `./gradlew testDebugUnitTest` including `DisplaySettingsTest`. | 1. Display switches display in the ON state by default, achieving 100% parity with runtime behavior.<br>2. Multiple switches can be toggled without state overwrites.<br>3. Host activity dynamically reflects flag and orientation changes upon dismissal.<br>4. Automated unit tests pass with zero regressions. | Verified |
| **TST-NAV-005** | `ATT-262` | **Direct Units Settings Dialog** | `REQ-SET-053` | 1. Open Navigation Drawer -> Settings. 2. Tap 'Units'. | 1. A dialog to choose between Metric and Imperial appears immediately over the current screen. 2. Selecting a unit updates the application state. | Verified |
| **TST-NAV-006** | `ATT-264` | **Direct Export Settings Dialog** | `REQ-SET-054` | 1. Open Navigation Drawer -> Ecosystem. 2. Tap 'Export'. | 1. A dialog containing file export settings appears immediately over the current screen. 2. Toggling formats works as expected. | Verified |
| **TST-NAV-007** | `ATT-526` / `ATT-243` | **Declarative Navigation Drawer & Item Density Verification** | `REQ-UI-123` | 1. Open Navigation Drawer and verify that `AppNavigationDrawer` renders smoothly without `InflateException` or crashes.<br>2. Verify that drawer item vertical spacing is compact (~40-42dp height) and fits all hubs cleanly without excessive scrolling.<br>3. Verify all 5 categories (Training, Spatial, Equipment, Synchronization, System) and their navigation items route correctly to corresponding screens or dialogs.<br>4. Start tracking, pause tracking, and stop tracking, verifying that the top tracking drawer item label reactively updates between "Start Tracking", "Tracking", and "Pause".<br>5. Execute unit tests and run regression suite `./gradlew testDebugUnitTest`. | Navigation drawer renders deterministically via Jetpack Compose with compact density, reactive title updates, and zero inflation errors. | Verified |
| **TST-NAV-008** | `ATT-658` | **MainActivityWithNavigation Kotlin Migration & Lifecycle Verification** | `REQ-UI-124` | 1. Build debug APK (`./gradlew assembleDebug`) and verify clean compilation of `MainActivityWithNavigation.kt` with zero warnings or errors.<br>2. Execute full regression unit test suite (`./gradlew testDebugUnitTest`).<br>3. Verify Java-to-Kotlin interop for companion object constants (`MainActivityWithNavigation.EXTRA_RESUME_INTERRUPTED_WORKOUT`, `SELECTED_FRAGMENT_ID`, `SELECTED_FRAGMENT`).<br>4. Launch `MainActivityWithNavigation` on test device:<br>• Verify initial fragment loading (`TrackingTabsFragment`).<br>• Open drawer and navigate between hubs/fragments (Workouts, Periods, Map, Segments, Routes, Locations, Sensors, Bikes, Shoes, Sport Types, Training Zones, Strava, Dropbox, Export).<br>• Trigger modal dialogs from drawer (Units, Display Settings, Tracking Layouts / Sport Selection, Export, Search Settings).<br>• Test interrupted workout resumption intent handling.<br>• Test back gesture closing navigation drawer without exiting the app. | `MainActivityWithNavigation` compiles cleanly in Kotlin, executes without regression across all existing unit tests, and reproduces 100% of runtime navigation and lifecycle behavior on device. | Verified |
| **TST-INT-008** | `ATT-268` | **Settings Cleanup Verification** | `REQ-SET-055` | 1. Gradle Build: Ensure the project compiles successfully after reorganization and deletions. 2. Navigation Test: Verify all settings items in the drawer (Units, Display, Search, Export, Strava, Dropbox, Tracking Tabs) still function correctly. | 1. Build is successful. 2. All settings features remain fully functional. | Verified |
| **TST-PRO-001** | `ATT-273` | **Jira Attachment Download** | `REQ-PRO-008` | 1. Identify a Jira ticket with attachments. 2. Run `jira_util.py download-all KEY`. | 1. All attachments from the ticket are downloaded to `docs/attachments/`. | Verified |
| **TST-PRO-002** | `ATT-274` | **Jira Type Visibility** | `REQ-PRO-009` | 1. Run `jira_util.py list`. 2. Run `jira_util.py show KEY`. | 1. Issue types (Bug, Task, etc.) are clearly visible in the output. | Verified |
| **TST-PRO-003** | `ATT-327` | **Plan Sub-Task Verification** | `REQ-PRO-010` | 1. Identify a task requires a plan. 2. Verify that the agent creates a Jira sub-task with summary `[Plan] SCRUM-XXX: Summary` and description matching the plan. | Sub-task is created correctly and linked to parent. | Verified |
| **TST-FIL-001** | `ATT-275` | **Null-Safe Filter Verification** | `REQ-FIL-011` | 1. Execute `AverageFilterTest` (or similar unit test). 2. Pass `null` as a new value to the filter. 3. Call `getFilteredValue`. | 1. The system MUST NOT throw a NullPointerException. 2. The filtered value SHALL remain consistent with previous data or return null if no valid data exists. | Verified |
| **TST-FIL-003** | `ATT-425` | **Atomic Altitude Correction** | `REQ-FIL-004` | 1. Start a workout with barometric altitude at 100m. 2. Record data for 1 minute. 3. Trigger a correction to 200m (e.g. by nearing a known location). 4. Stop workout and inspect summary. | 1. Samples table altitudes MUST be shifted by +100m. 2. Summary Min/Max altitude MUST be updated. 3. Encoded altitude stream MUST reflect the shift. | Verified |
| **TST-UI-075** | `ATT-272` | **Settings Terminology Audit** | `REQ-UI-115` | 1. Open Navigation Drawer -> Settings. 2. Verify items "Display" and "Search". 3. Switch to German. 4. Verify items "Display" and "Suche". | Labels are concise and free of redundant qualifiers ("Settings"/"Einstellungen"). | Verified |
| **TST-REG-007** | | **Pause Time Sync** | `REQ-TRK-009`, `REQ-UI-038` | 1. Start workout. 2. Pause after 10s. 3. Wait 20s while paused. | Verify Active Time is 10s and Total Time is ~30s in the UI and DB. | Verified |
| **TST-REG-008** | `ATT-257` | **Dropbox Auth** | `REQ-EXP-002` | 1. Navigate to 'Dropbox' in Ecosystem. 2. Tap 'Connect to Dropbox'. 3. Complete OAuth flow in browser. 4. Return to app. | 1. The header SHALL show "Connected to Dropbox". 2. Navigating away and back SHALL maintain the "Connected" state. | Verified |
| **TST-UI-001** | | **UI Design** | `REQ-UI-011` | Inspect the workout summary extrema table on various device themes. | The table has distinct headers, clear alignment, and high-quality typography matching the Material 3 standards. | Verified |
| **TST-UI-011** | | **Formatting** | `REQ-UI-011` | Verify that units in data rows are surrounded by square brackets (e.g., `[km/h]`). | - | Verified |
| **TST-UI-012** | | **Hierarchy** | `REQ-UI-011` | Verify that the Sensor Name and the Numeric Values share the same visual weight, while the unit is micro-scaled. | - | Verified |
| **TST-UI-013** | | **Alignment** | `REQ-UI-011` | Verify that in all data rows, the icon, sensor label, and numeric values are vertically aligned to their bottom edge. | - | Verified |
| **TST-UI-014** | | **Typography** | `REQ-UI-011` | Verify that units use a micro font size (e.g., 8-9.sp) and a lighter color (reduced alpha) than the sensor name. | - | Verified |
| **TST-UI-015** | | **Notation** | `REQ-UI-011` | Verify that the "Average" header column displays the mathematical symbol (Ø) instead of the word "average". | - | Verified |
| **TST-UI-016** | | **Proportion** | `REQ-UI-011` | Verify that the icon height visually matches the height of the sensor name (bodyLarge line height). | - | Verified |
| **TST-UI-017** | | **Alignment** | `REQ-UI-011` | Verify that the icon, sensor label, and bracketed unit are perfectly aligned to the same bottom baseline in the Compose Preview. | - | Verified |
| **TST-UI-018** | | **Baseline Precision** | `REQ-UI-011` | Verify that the bottom edge of the icon and the bottom of the text characters are visually perfectly aligned. | - | Verified |
| **TST-UI-019** | | **Hierarchy** | `REQ-UI-011` | Verify context-aware bolding: HR/Power (Avg+Max), Speed/Pace/Cadence (Avg), Alt/Temp (Min+Max). | - | Verified |
| **TST-UI-020** | | **Contrast** | `REQ-UI-011` | Verify that the "-" placeholder is visually lighter (alpha 0.3) than numeric values. | - | Verified |
| **TST-UI-021** | | **Redundancy** | `REQ-UI-012` | Verify that Min/Max altitude no longer appear in the Workout Details row, only Ascent and Descent. | - | Verified |
| **TST-UI-026** | | **Consistency** | `REQ-UI-013` | In the Workout Summary card, verify that the altitude labels at the start/end of the chart axes match the Min/Max values shown in the Extrema table. | - | Verified |
| **TST-UI-027** | | **Harmonization** | `REQ-UI-014` | Start tracking; verify the Speed Average in the extrema table exactly matches the value derived from Total Distance / Active Time (e.g., if distance is 100m and active time is 100s, avg speed must be 1.0 m/s). | - | Verified |
| **TST-UI-028** | | **Localization** | `REQ-UI-018` | Inspect Workout Summary in various languages. Verify Ascent/Descent uses relative terminology (e.g., "Höhenmeter", "Elevation") and Min/Max uses absolute terminology (e.g., "Höhe", "Altitude"). | Correct linguistic distinction across all supported languages. | Verified |
| **TST-UI-029** | | **Session Isolation** | `REQ-UI-009` | 1. Start a workout, move, stop workout. 2. Immediately start a new workout. | Verify Map and Elevation Profile are empty at start. | Verified |
| **TST-UI-030** | | **Metric Consistency**| `REQ-UI-020` | Audit Workout and Segment items. Verify \"Gain\" uses `ic_ascent` in both. Verify \"Elevation\" label in Workout is bottom-aligned with values. Verify all icons/text share a consistent baseline. | - | Verified |
| **TST-UI-031** | | **Layout Hierarchy** | `REQ-UI-021` | Open Workout Summary. Inspect the Elevation section header (left column). | - | Verified |
| **TST-UI-034** | | **Identity Branding UI** | `REQ-UI-024` | 1. Open Route List. 2. Inspect Strava route: verify logo is below name and no \"Source: Strava\" text exists. 3. Inspect Local route: verify \"Source: Local\" text is below name. 4. Verify no branding logo in metrics row. | - | Verified |
| **TST-LOG-001** | | **Logic** | `REQ-UI-011` | Verify that all rows, including "Pace", follow strict mathematical ordering (numerical minimum in Min column, numerical maximum in Max column). | - | Verified |
| **TST-LOG-002** | | **Logic** | `REQ-UI-011` | Verify that for a running activity, both Speed and Pace rows are generated from the single Speed data source. | - | Verified |
| **TST-MAN-001** | | **General** | All other `REQ` | Ad-hoc functional testing on a physical device. | Feature performs according to the rationale defined in requirements. | Proposed |
| **TST-STP-001** | | **Localization** | `REQ-STP-001` | Review generated markdown files in `docs/store_presence/`. Verify that Title, Short Description and Full Description are correctly translated. | Adhere to Google Play character limits. | Verified |
| **TST-STP-002** | `ATT-276` | **Messaging Review** | `REQ-STP-002` | 1. Compare new Play Store description with user's draft and project vision. | Description is punchy, highlights key features (Automation, Privacy, Clusters), and maintains professional tone. | Verified |

### 5. Migration & Data Integrity (Epic ATT-281)

| ID | Jira ID | Summary | Req ID | Procedure | Expected Result | Status |
|:---|:---|:---|:---|:---|:---|:---|
| **TST-MIG-001** | `ATT-117` | **Full Migration Cycle** | `REQ-MIG-001`, `002`, `004`, `006` | 1. Generate local backup. 2. Wipe app data. 3. Restore from backup. | App state (settings, history, equipment) is perfectly restored. | Proposed |
| **TST-MIG-002** | `ATT-117` | **Deduplication Verification** | `REQ-MIG-005` | 1. Import a backup containing already existing workouts. | Duplicate workouts are identified by `FILE_BASE_NAME` and skipped. | Proposed |
| **TST-MIG-003** | `ATT-93` | **Automated Dropbox Backup** | `REQ-MIG-003` | 1. Enable regular backups. 2. Verify archive exists in Dropbox after background task runs. | Backup is present and valid in `/Backups/` folder. | Proposed |
| **TST-MIG-004** | `ATT-290` | **Full Restore Confirmation (Dropbox)** | `REQ-MIG-007` | 1. Navigate to 'Backup \u0026 Restore'. 2. Tap \"Dropbox: Restore Backup\". | 1. A confirmation dialog appears with a warning about data loss. 2. Tapping 'Cancel' dismisses the dialog. 3. Tapping 'Restore' starts the process. | Proposed |
| **TST-MIG-005** | `ATT-295` | **Informative Labels Audit** | `REQ-MIG-008` | 1. Open Backup \u0026 Restore dashboard. 2. Verify Dropbox button label. 3. Verify presence of descriptions in Restore and Import sections. | Labels are accurate and follow the dual-action naming. Descriptions are clearly visible and explain the operation impact. | Proposed |
| **TST-MIG-006** | `ATT-321` | **Default Backup Interval Verification** | `REQ-MIG-009` | 1. Clear application data. 2. Navigate to 'Dropbox' settings. 3. Inspect 'Backup Interval'. | The interval SHALL be set to 'Daily' by default. | Verified |
| **TST-MIG-007** | `ATT-322` | **Enable Backups by Default Verification** | `REQ-MIG-010` | 1. Clear application data. 2. Navigate to 'Dropbox' settings. 3. Inspect 'Automated Backups' switch. | The switch SHALL be in the 'ON' position by default. | Proposed |
| **TST-MIG-008** | `ATT-323` | **Tabbed UI Navigation** | `REQ-MIG-011` | 1. Open drawer and verify 'Import \u0026 Backup' label. 2. Open screen and verify 3 tabs: Import, Backup, Restore. 3. Swipe between tabs. | UI is organized into tabs. Navigation is smooth. Titles match requirements. | Verified |
| **TST-MIG-009** | `ATT-793` | **Pre-Import Workout Cluster Tuning Bottom Sheet & Info Action Verification** | `REQ-MIG-012` | 1. Navigate to 'Import & Backup' -> 'Import' tab.<br>2. Select a single legacy TCX file or trigger bulk TCX scan to launch pre-import tuning.<br>3. Verify interface renders as a Material 3 `ModalBottomSheet` spanning full device width with drag handle, header title, and Info button.<br>4. Tap Info button (`Icons.Outlined.Info`) and verify `ClusterInfoDialog` appears with educational content, and dismisses cleanly back to bottom sheet.<br>5. Verify smooth scrolling and spacious layout of `ClusterTuningContent` (master slider, detailed tolerance sliders, sport type switch, altitude switch).<br>6. Adjust parameters, tap OK, and verify tolerances persist to `SharedPreferences` and import commences.<br>7. Re-open sheet, tap Cancel or dismiss sheet via drag, and verify no import commences and unconfirmed edits are discarded. | The bottom sheet provides a spacious, responsive layout without spatial confinement or clipped controls. The Info button launches `ClusterInfoDialog` properly. Tolerances persist upon confirmation and import commences smoothly. | Verified |
| **TST-MIG-010** | `ATT-331` | **Idempotent TCX Import** | `REQ-MIG-013` | 1. Import a TCX file. 2. Re-run 'Scan TCX' for the same file. | The system SHALL detect the existing workout and skip it. No clustering dialog SHALL appear. | Verified |
| **TST-MIG-011** | `ATT-337` | **Legacy Recovery Performance Audit** | `REQ-MIG-015` | 1. Ensure several legacy workouts exist in the local database. 2. Run 'Scan TCX' with those same files present in Dropbox. | The skipping messages appear almost instantaneously, without the 2-5 second delay previously caused by file downloads. | Verified |
| **TST-MIG-012** | `ATT-339` | **Paginated Cloud Scan Verification** | `REQ-MIG-016` | 1. Use a Dropbox account with > 1000 TCX files in the target folder. 2. Trigger 'Scan TCX'. | The app SHALL discover all files and process the entire set. | Verified |
| **TST-PRO-004** | `ATT-333` | **Decoupled Interaction Queue** | `REQ-MIG-014` | 1. Start a bulk TCX import that triggers multiple cluster naming candidates. 2. Verify that while the ClusterNamingDialog is shown, the background progress indicator remains visible. 3. Complete the first naming task. | User interaction does not 'hide' the import status. Requests are presented sequentially while maintaining visibility of the background state. | Verified |
| **TST-UI-035** | | **Compact Metric Row** | `REQ-UI-025` | 1. Open Route List. 2. Inspect gap between identity row and metrics row. | - | Verified |
| **TST-UI-036** | | **Branding Audit** | `REQ-UI-026` | 1. Open Segment List. 2. Verify top logo height (e.g., 24dp). 3. Inspect individual Segment Items (if enabled). | - | Verified |
| **TST-UI-038** | | **Detailed Metric Scale** | `REQ-UI-028` | 1. Open Periods screen. 2. Inspect sub-sport rows and \"Longest Workout\" highlight. | Verify icons are 14dp and text is bodySmall. | Verified |
| **TST-UI-039** | | **Pause Movement Isolation** | `REQ-PRO-004` | 1. Start workout. 2. Move to create track. 3. **Pause**. 4. Move 50m. | Observe map. No movement recorded during pause. | Verified |
| **TST-SET-031** | | **Unified Spatial Markers** | `REQ-SET-041` | 1. Open Workout Details, Period Map, and Route Heatmap. 2. Inspect Start/End markers. | 1. All views use Green pins with `control_start` icon and Red pins with `control_stop` icon. 2. Icons are vertically centered in the pin base. | Proposed |
| **TST-UI-043** | | **Device Management Modernization** | `REQ-UI-041`, `REQ-UI-043` | 1. Navigate to Pairing screen. 2. Verify Tabbed layout. 3. Interact with Pair switches. 4. Click device to open Edit Dialog. 5. Modify a setting (e.g. name). | - | Verified |
| **TST-UI-045** | | **Reactive Map Updates** | `REQ-MAP-009` | Open Route Detail Map. Toggle the \"Active\" switch in the header. | The route polyline on the map SHALL immediately change its color and width without requiring any camera movement (zoom/pan). | Verified |
| **TST-UI-046** | | **Stats Summary Block Visual Alignment** | `REQ-UI-046` | Inspect the `StatsSummaryBlock` in the Equipment or SportType details screen. | The block SHALL use the unified `MetricItem` component. Headings (Workouts, Distance, Time, Ascent) SHALL be positioned above their respective values. Visual hierarchy (icons, typography) SHALL be consistent with Workout and Segment list items. | Verified |
| **TST-UI-047** | | **Live Segment Sheet Swipe Limit** | `REQ-UI-047` | 1. Start tracking. 2. When a segment is active, swipe up on the bottom sheet. | The sheet SHALL stop expanding once the header and elevation profile are fully visible. It SHALL NOT cover the entire screen or reach the status bar. | Verified |
| **TST-UI-048** | | **Sensor Status Header Visual Audit** | `REQ-UI-048` | Inspect the `SensorStatus` row at the top of the tracking screen. | The row SHALL contain only sensor icons. No text labels (e.g., "HR", "Speed") SHALL be visible below the icons. Icons SHALL use a 22dp scale to remain legible without dominating the cockpit. | Verified |
| **TST-UI-049** | | **Sensor Source Dialog Verification** | `REQ-UI-049` | 1. Start a session. 2. Tap any sensor icon (active or inactive) in the header. | A dialog SHALL appear. For active sensors, it shows real-time telemetry. For inactive sensors, it identifies the paired source device(s) or internal fallbacks. | Verified |
| **TST-UI-051** | | **Grade Legend Visibility** | `REQ-MAP-010` | 1. Open any Elevation Profile (e.g., in Aftermath). 2. Tap the info icon. | The grade color legend SHALL appear. Tap again to hide. | Verified |
| **TST-UI-050** | | **Sensor Identity Logic** | `REQ-UI-050` | Open `SensorSourceDialog`, `EditDeviceDialog`, and `DeviceItem`. | Both SHALL feature identical Row 1/2 blocks. `EditDeviceDialog` SHALL include Row 3 (Manufacturer). The LED dot and the Battery icon SHALL be perfectly aligned on their vertical centers. | Verified |
| **TST-UI-071** | `ATT-237` | **Branding** | `REQ-UI-062` | 1. Open the app to the main tracking screen. 2. Inspect the header area. | Hamburger menu is visible on the very left. Tapping it opens the navigation drawer. | Verified |
| **TST-UI-074** | `ATT-386` | **Weekly Period Date Range** | `REQ-UI-116` | 1. Open the 'Periods' screen. 2. Navigate to the 'Weeks' tab. 3. Inspect the period items. | Each week item SHALL display its start and end dates (e.g., \"May 11 - May 17\") below the primary week label. | Verified |
| **TST-UI-072** | `ATT-369` | **Progress Notification Neutrality** | `REQ-UI-101` | 1. Trigger a Periods sync or Cluster initial load. 2. Inspect the background color of the progress card. | The card background SHALL be the neutral *surface* color. Branding colors (e.g. secondaryContainer) MUST NOT be used for the card background. | Verified |
| **TST-UI-073** | `ATT-376` | **Bar Graph Synchronization** | `REQ-UI-104` | 1. Clear application data to trigger a Periods migration. 2. Navigate to the 'Periods' screen. 3. Observe the bar graph as the newest months are aggregated first. 4. Scroll the period list. | 1. During migration, the graph SHALL display the most recent months (right side of the graph) by default. 2. As the list grows into the past, the "Current" selection in the graph SHALL remain synchronized with the top-most visible period in the list. | Verified |
| **TST-BUG-006** | `ATT-397` | **Adaptive Header Framing** | `REQ-UI-117` | 1. Open the app on a device with a large status bar (e.g. Pixel 10). 2. Navigate to Workouts, Periods, and My Locations. | The top of the list SHALL NOT be overlapped by the header. No unexpected empty space SHALL exist between the header and the list. | Verified |
| **TST-BUG-007** | `ATT-410` | **Internal Altitude Source Visibility** | `REQ-UI-118` | 1. Open tracking screen on device with pressure sensor. 2. Tap Altitude icon. | 'Altitude from Pressure' SHALL be listed as an active source/backup with real-time value. | Verified |
| **TST-BUG-008** | `ATT-414` | **Self-Healing Pressure Sensor Registration** | `REQ-DAT-006` | 1. Use Database Inspector to delete the 'Altitude from Pressure' entry from the `Devices` table. 2. Restart the app on a device with a pressure sensor. 3. Navigate to 'My Sensors'. | The 'Altitude from Pressure' sensor SHALL be automatically re-registered and visible in the list. | Verified |
| **TST-BUG-009** | `ATT-416` | **Internal Sensor Pairing Control** | `REQ-CON-010` | 1. Navigate to 'My Sensors'. 2. Deactivate the 'Altitude from Pressure' sensor using the toggle switch. 3. Verify that the sensor status LED turns grey and real-time altitude updates from this source stop. 4. Reactivate and verify resumption. | The system correctly starts and stops internal hardware telemetry based on user pairing preference. | Verified |
| **TST-MIG-013** | `ATT-401` | **Interaction Queue Size Limiting** | `REQ-MIG-017` | 1. Start bulk import of > 10 unclustered workouts. 2. Verify engine pauses at 10 queued items. 3. Monitor network/parsing logs. | Interaction queue size is strictly enforced (limit: 10). The engine MUST NOT download or parse more files until space is cleared in the queue. | Verified |
| **TST-MIG-014** | `ATT-403` | **Sparse Data Import** | `REQ-MIG-018` | 1. Import a TCX file missing HR/Power. 2. Inspect summary and DB schema. | Missing sensors are NOT displayed in summary and NOT created as columns in samples table. | Verified |
| **TST-MIG-015** | `ATT-405` | **Dropbox Wait Feedback** | `REQ-MIG-019` | 1. Trigger 'Scan TCX' from Dropbox. 2. Observe the progress status messages. | Explicit messages like "Waiting for Dropbox..." or "Downloading [File]..." SHALL be visible during network operations. | Verified |
| **TST-MIG-016** | `ATT-435` | **Positive Import UI Alignment** | `REQ-MIG-020` | 1. Navigate to 'Import & Backup'. 2. Inspect the 'Import' tab content. | 1. The redundant introduction message is GONE. 2. The section title is positive (e.g., \"Workout Import\") and avoids recovery/legacy terminology. 3. Buttons have feature-oriented labels. | Verified |
| **TST-MIG-017** | `ATT-549` | **Concurrent TCX Import Background Queuing & Throttling** | `REQ-MIG-014`, `REQ-MIG-017` | 1. Start bulk import of multiple TCX files requiring route cluster naming. 2. Leave the first cluster naming dialog open without confirming immediately. 3. Monitor background progress indicators. | The TCX import engine SHALL continue downloading and processing subsequent files concurrently in the background while waiting for user interaction, pausing ONLY when 3 items accumulate in the interaction queue. | Verified |
| **TST-MIG-018** | `ATT-560` | **Recursive Cloud Folder Scanning & Informative Import Summary** | `REQ-MIG-016`, `REQ-MIG-021` | 1. Set up a Dropbox folder structure containing TCX files in multiple paths and subdirectories (e.g., `/TCX/2025/` and `/apps/Workouts/TCX/`). 2. Trigger bulk recovery. 3. Inspect final success message. | The import engine discovers files across all paths and subdirectories without terminating early. The final message clearly states the number of newly imported workouts, skipped existing workouts, and total files scanned. | Verified |
| **TST-MIG-019** | `ATT-571` | **Workout Name Filename Integrity** | `REQ-MIG-022` | 1. Import a TCX file named `2026-08-01_10-00-00.tcx`. 2. Inspect the workout list and database entry for `FILE_BASE_NAME` and `WORKOUT_NAME`. | The workout name and `FILE_BASE_NAME` in the database match `2026-08-01_10-00-00` exactly. No numeric counter prefix (e.g. `1_`) is prepended. | Verified |
| **TST-MIG-020** | `ATT-491` | **Unlinked Dropbox Import Deactivation & Null Safety** | `REQ-MIG-023` | 1. Ensure Dropbox is not linked in settings. 2. Open 'Import & Backup' -> 'Import' tab. 3. Verify 'Scan Dropbox' is styled in grey. 4. Click 'Scan Dropbox' and verify informational dialog appears without crashing. 5. Run automated unit test suite. | The feature is visibly deactivated in grey, clicking it displays the informational dialog, and credential reading never crashes with NullPointerException. | Verified |
| **TST-MIG-021** | `ATT-618` | **High-Fidelity TCX Telemetry & Multi-Lap Structure Import** | `REQ-MIG-024` | 1. Construct a multi-lap TCX test fixture containing: 2 distinct `<Lap>` elements with explicit `<TotalTimeSeconds>`, `<DistanceMeters>`, `<Calories>`, and `<MaximumSpeed>`; trackpoints inside `<Extensions><TPX>` containing `<Speed>` and `<RunCadence>`; an activity `<Notes>` field; and a 5-minute pause gap between Lap 1 end and Lap 2 start. 2. Execute `LegacyImportEngine.importFromTcx()` on the fixture. 3. Query `LapsDatabaseManager.getLaps(workoutId)`: Verify 2 distinct lap records exist with matching durations, distances, and metrics. 4. Query `WorkoutSamplesDatabaseManager`: Verify samples have `SensorType.SPEED` and `SensorType.CADENCE` values from the TPX stream, and `SensorType.LAP_NR` values corresponding to 0 and 1. 5. Query `WorkoutSummariesDatabaseManager.getWorkoutSummary(workoutId)`: Verify active duration equals the sum of `<TotalTimeSeconds>` (excluding the 5-minute pause gap), calories are populated, and notes match. | All laps, instantaneous speed/cadence streams, active durations, calories, and workout notes are faithfully preserved without data loss or pause inflation. | Verified |
| **TST-MIG-022** | `ATT-741` | **TCX Import Leave Unclustered Integrity Verification** | `REQ-MIG-025` | 1. Construct a spatial workout fixture where `suggestCluster()` finds no existing cluster match. 2. Simulate user selection of "Leave Unclustered" (`Pair(null, null)`). 3. Verify `LegacyImportEngine` leaves `WorkoutSummaries.CLUSTER_ID = -1L`. 4. Trigger `WorkoutRepository.reloadWorkoutData()`. 5. Verify `WorkoutClusterEngine.onWorkoutFinished()` does NOT create an auto-generated fallback cluster, does NOT assign any cluster, and does NOT mutate `WorkoutSummaries.CLUSTER_ID` (it remains `-1L`). 6. Verify the workout name does NOT become "Workout at [date]" / "Einheit bei [date]". 7. Verify the workout is queryable under the Unclustered tab (`clusterId == -1L`). | The workout remains unassigned (`clusterId == -1L`), no new cluster is created in `WorkoutClusterDatabaseManager`, the original workout name is preserved, and the workout appears in the Unclustered workouts collection. | Verified |
| **TST-MIG-023** | `ATT-909` | **TCX Import Periods Aggregation & Self-Healing Sync** | `REQ-MIG-026` | 1. Clear in-memory `WorkoutRepository` instance to simulate cold repository state during import.<br>2. Import a historical TCX workout file via `LegacyImportEngine.importFromTcx()`.<br>3. Verify that `WorkoutRepository.reloadWorkoutData(workoutId)` and `PeriodsRepository.onWorkoutFinished()` are invoked directly.<br>4. Query `PeriodSummariesDatabaseManager`: verify Day, Week, Month, and Year periods exist for the workout timestamp and contain updated statistics (`total_distance`, `total_duration`, `total_workouts`).<br>5. Simulate an out-of-sync state by inserting a finished workout into `WorkoutSummaries` without updating `PeriodSummaries.db`.<br>6. Trigger `PeriodsRepository.loadPeriods()`.<br>7. Verify that the self-healing count check detects the discrepancy and executes `performHierarchicalMigration()` to reconcile all missing periods. | Imported workouts are immediately visible in all period summaries without requiring broadcast reception or app restart. The self-healing integrity check automatically detects and repairs any discrepancy between finished workouts in SQLite and period summaries. | Verified |
| **TST-MIG-024** | `ATT-909` | **TCX Import Cluster Heuristics & Reactive UI Cache Refresh** | `REQ-MIG-027` | 1. Construct a TCX workout fixture with sport type `UNKNOWN` (e.g. unmapped activity) and average speed typical of cycling/running.<br>2. Ensure an existing cluster exists with matching route geometry.<br>3. Execute `LegacyImportEngine.importFromTcx()`.<br>4. Verify that `suggestCluster()` does NOT pass `setOf(BSportType.UNKNOWN)` and is not penalized with `+5.0` distance penalty.<br>5. Verify that extracted workout name is passed to `suggestCluster()` enabling the 50% name-match similarity discount.<br>6. Verify `WorkoutSummaries.B_SPORT` is populated with the inferred `BSportType` name.<br>7. Verify `WorkoutClusterRepository.refreshClusters()` is invoked and in-memory caches (`allClusters`, `clusterStats`, `unclusteredWorkouts`) immediately update with the newly imported session. | Workouts with unknown/missing sports are accurately matched against route clusters based on spatial geometry and speed heuristics. Cluster names boost similarity scoring. Assigned workouts update cluster hit counts and preview states in real time across the UI. | Verified |
| **TST-MIG-025** | `ATT-911` | **TCX Import Optional Strava Upload & UI Switch Verification** | `REQ-MIG-028` | 1. Configure Strava as connected (`TrainingApplication.uploadToCommunity(STRAVA) == true`).<br>2. Call `LegacyImportEngine.importFromTcx()` with `uploadToStrava = false`. Verify `WorkoutSummaries.UPLOAD_TO_STRAVA` is saved as `0`, and verify zero Strava export tasks are scheduled in `ExportManager`.<br>3. Call `LegacyImportEngine.importFromTcx()` with `uploadToStrava = true`. Verify `WorkoutSummaries.UPLOAD_TO_STRAVA` is saved as `1`, and verify Strava export task is scheduled via `ExportManager.exportWorkoutTo(workoutId, STRAVA)`.<br>4. Call `LegacyImportEngine.bulkRecoverFromDropbox()` with `uploadToStrava = false` and verify that the flag is forwarded to individual TCX imports, resulting in `UPLOAD_TO_STRAVA = 0`.<br>5. Render `PreImportTuningBottomSheet` when Strava is connected: verify that the Strava upload toggle switch is visible and accurately reflects `BackupRestoreViewModel.uploadToStravaOnImport`. Toggle the switch and verify `TrainingApplication.uploadImportedWorkoutsToStrava()` updates immediately.<br>6. Render `PreImportTuningBottomSheet` when Strava is disconnected: verify that the Strava toggle switch is not visible.<br>7. Execute `TranslationParityTest` to verify that `import_upload_to_strava_label` and `import_upload_to_strava_summary` exist across all 9 localized `strings.xml` resource files. | Disabling Strava upload prevents auto-upload and sets `UPLOAD_TO_STRAVA = 0`. Enabling Strava upload schedules export and sets `UPLOAD_TO_STRAVA = 1`. Pre-import bottom sheet conditionally renders the switch only when Strava is connected. All 9 language resource directories satisfy translation parity. | Verified |
| **TST-MIG-026** | `ATT-1133` | **TCX Import Route Cluster Auto-Matching Parity & Lossless Candidate Discovery** | `REQ-MIG-029` | 1. Create an existing cluster in `WorkoutClusterDatabaseManager` with start point $P_{start}$, end point $P_{end}$, apex $P_{apex}$, distance 10,000m, and name "River Loop".<br>2. Construct an imported workout fixture whose start point is displaced by 350m north from $P_{start}$ (exceeding default 200m endpoint tolerance by 1.75x), while end point, apex, and distance match tightly within tolerance, yielding a composite similarity score $\approx 0.55$ (< 1.0).<br>3. Invoke `WorkoutClusterEngine.suggestCluster()` with the workout's spatial signature. Verify that the cluster is returned as the matching suggestion without premature SQLite pruning.<br>4. Simulate `LegacyImportEngine.importFromTcx()` with this workout: Verify that `suggestCluster()` auto-assigns the workout to the cluster without invoking `listener.onNewClusterCandidate()`.<br>5. Construct a workout with 3D altitude extrema points and distinct name: verify that `ClusterInteraction` in `BackupRestoreViewModel` receives and holds `workoutName`, `candidateSportTypes`, `minAltPos`, and `maxAltPos`.<br>6. Verify that `ClusterNamingDialog` passes these parameters to `clusterEngine.scoreClusters()`, resulting in 100% numerical parity with `suggestCluster()`.<br>7. Construct a workout that genuinely diverges from all existing clusters (composite similarity $\ge 1.0$): verify that `suggestCluster()` returns `null`, `onNewClusterCandidate()` is called, and all candidate scores in `scoreClusters()` are $\ge 1.0$. | Workouts with start point deviation $> 1.0\times$ endpoint tolerance but composite similarity $< 1.0$ are auto-matched without manual dialog intervention. `suggestCluster` and `ClusterNamingDialog` compute identical similarity scores. | Verified |
| **TST-MIG-027** | `ATT-1116` | **High-Fidelity GPX Workout Import & Full Telemetry Ingestion Parity** | `REQ-MIG-030` | 1. Construct a multi-segment GPX test fixture containing: metadata title and description; multiple `<trkseg>` elements representing laps; trackpoints with latitude, longitude, elevation (`<ele>`), and UTC timestamps (`<time>`); Garmin TrackPointExtension (`gpxtpx:TrackPointExtension`) containing heart rate (`<gpxtpx:hr>`), cadence (`<gpxtpx:cad>`), speed (`<gpxtpx:speed>`), and temperature (`<gpxtpx:atemp>`); power extension (`<power>`); and sport type `<type>running</type>`.<br>2. Execute `LegacyImportEngine.importFromGpx()` on the fixture.<br>3. Verify that `WorkoutSummaries` records the workout with matching start time, workout name, notes, sport (`BSportType.RUN`), and total distance calculated from cumulative coordinates.<br>4. Query `LapsDatabaseManager`: verify multiple distinct lap records are saved corresponding to the `<trkseg>` segments, each with valid duration, distance, and average speed.<br>5. Query `WorkoutSamplesDatabaseManager`: verify sample rows contain `LATITUDE`, `LONGITUDE`, `ALTITUDE`, `DISTANCE_m`, `SPEED_mps`, `HR`, `CADENCE`, `POWER`, `TEMPERATURE_c`, and corresponding `LAP_NR` values.<br>6. Verify deduplication: attempting to re-import the same file skips insertion (`isWorkoutExisting` returns true).<br>7. Verify cluster routing and period integration: verify `suggestCluster()` and `PeriodsRepository.onWorkoutFinished()` are called.<br>8. Verify `BackupRestoreViewModel.importLegacyFile()` detects `.gpx` files and executes `importFromGpx()` successfully. | GPX workouts import seamlessly with high-fidelity telemetry (HR, cadence, speed, power, temp), multi-lap structure, cumulative distance calculation, and full parity with TCX pipeline. | Verified |
| **TST-BUG-011** | `ATT-420` | **Eager Pressure Sensor Visibility** | `REQ-DAT-006` | 1. Use Database Inspector to delete the 'Altitude from Pressure' entry from the `Devices` table. 2. Restart the app on a device with a pressure sensor. 3. Navigate to 'My Sensors'. | The sensor SHALL be automatically re-registered and immediately visible in the list on the FIRST run after deletion. | Verified |
| **TST-BUG-010** | | **Internal Sensor Iconography** | `REQ-UI-119` | 1. Open 'My Sensors'. 2. Inspect the 'Altitude from Pressure' entry. | The entry SHALL display the standard `ic_altitude` icon instead of a generic smartphone icon. | Verified |
| **TST-DAT-001** | `ATT-423` | **Automated Altitude Discovery** | `REQ-DAT-007` | 1. Record/Import a workout starting at Location A (Alt=100m). 2. Record/Import another workout starting at Location A (Alt=110m). 3. Verify the 'Known Locations' database entry for Location A. | The database SHALL contain an entry for Location A with an average altitude of 105m and a hit count of 2. | Verified |
| **TST-DAT-002** | `ATT-482` | **Precise Altitude Learning** | `REQ-DAT-007` | 1. Start workout at Location A (200m). 2. Descend to 100m. 3. Stop workout. 4. Inspect 'Known Locations' DB. | Location A MUST be learned as 200m. It MUST NOT be 100m (the session minimum). | Verified |
| **TST-MAP-010** | `ATT-437` | **Zoom-Adaptive Heatmap Styling** | `REQ-MAP-016` | 1. Open a Workout Cluster heatmap. 2. Zoom out to level 11/12. 3. Observe line width and marker pins. 4. Zoom in to level 17+. | 1. At level 11/12, lines are thin and member markers (pins) are HIDDEN. 2. At level 17+, individual traces are prominent and member markers appear clearly. | Verified |
| **TST-UI-077** | `ATT-474` | **Consistent Spacing Audit** | `REQ-UI-121` | 1. Open Workout List, Route List, and Cluster List. 2. Verify all cards have 12dp horizontal internal padding. 3. Verify vertical spacing between sections (Header, Details, etc.) is uniform (12dp). 4. Verify list items are separated by 8dp. | Perfect horizontal alignment and uniform vertical rhythm across all list items. | Proposed |

## 3. Structural & Compliance Checks

| Test ID | Jira ID | Focus | Requirement ID(s) | Method | Status |
|:---|:---|:---|:---|:---|:---|
| **TST-STR-001** | | **Stability** | `REQ-CON-005`, `REQ-TRK-005`, `REQ-DAT-005`, `REQ-UI-008` | Static analysis and architectural audit. | Verified |
| **TST-STR-002** | | **Thread Safety** | `REQ-PRO-002` | Static analysis of `synchronized` blocks to ensure no nested locks or I/O within locks. | Verified |
| **TST-STR-003** | | **Data Integrity** | `REQ-UI-009` | 1. Static analysis of `onUpgrade` (v9->v10).<br>2. Static analysis of `onCreate` and `addDefaultTab`. | 1. `onUpgrade` must set `SHOW_ELEVATION_PROFILE = SHOW_MAP`.<br>2. `addDefaultTab` must initialize `SHOW_ELEVATION_PROFILE` using the value provided for `showMap`. | Verified |
| **TST-STR-004** | | **Consistency** | `REQ-TRK-008` | Static analysis of `TrackerService.java`. | `IMPORTANT_SENSOR_TYPES` must strictly contain only the "Big 6" + Temperature metrics. | Verified |
| **TST-STR-005** | | **Map DSL** | `REQ-MAP-006` | Static audit of `ATrainingTrackerMap` callers. | All data layers added via DSL block, not direct parameters. | Verified |
| **TST-STR-006** | | **Metric Consistency**| `REQ-UI-015` | Visual audit of Workout, Route, and Segment screens. | All use `MetricItem` for data rows. | Verified |
| **TST-STR-007** | | **Formatter Sync** | `REQ-UI-016` | Static audit of `LocalMetricFormatter` usages. | No direct `new DistanceFormatter()` etc. in UI layer. | Verified |
| **TST-STR-008** | | **List Foundation** | `REQ-UI-017` | Audit of `MappableListItem` usages. | All list cards share same foundation and padding. | Verified |
| **TST-STR-011** | | **Thread-Safe Sensor Management** | `REQ-PRO-006` | Static audit of `MySensorManager` and `MyDevice`. | All methods accessing shared sensor maps/lists are synchronized and return defensive copies. | Verified |
| **TST-UI-052** | | **Source Header Color Audit** | `REQ-UI-051` | Open `SensorSourceDialog` for a sensor with active and standby sources. | Header text colors SHALL match: Source=Dark Green, Backup=Light Green, Not connected=Grey. | Verified |
| **TST-UI-053** | | **Longest Workout Navigation** | `REQ-UI-052` | 1. Open a Period Summary with multiple workouts. 2. Tap the "Longest Workout" section. | The app SHALL navigate to the list and automatically scroll to the targeted workout. | Verified |
| **TST-UI-054** | | **Location Audit Metric** | `REQ-UI-053` | 1. Start a tracking session with a location provider active. 2. Tap the location icon in the `SensorStatus` header. | The `SensorSourceDialog` SHALL show "Accuracy" and its current value (e.g. "5 m") instead of "Longitude". | Verified |
| **TST-UI-055** | | **Multi-Track Visibility Audit** | `REQ-MAP-011`, `REQ-MAP-012` | 1. Open the Workout Details screen for a recorded activity. 2. Tap the "Layers" FAB. 3. Toggle different track types (GPS, Fused, Network). | The map SHALL update polylines in real-time to show/hide the selected tracks. The selection menu includes a color legend. | Verified |
| **TST-UI-056** | | **Map Preference Persistence** | `REQ-MAP-013` | 1. Toggle technical tracks in Workout Details. 2. Close and reopen the workout. | The previously selected track layers SHALL remain visible. | Verified |
| **TST-UI-057** | | **Period FAB Order** | `REQ-MAP-014` | 1. Open the Period Map. 2. Verify the vertical order of FABs. | The Share button SHALL be the topmost item in the FAB stack. | Verified |
| **TST-UI-058** | | **High-Fidelity Period Tracks** | `REQ-MAP-015` | 1. Compare a track in a Period Map overview with the same track in Workout Details. | Both tracks SHALL show identical fine-granular detail. | Verified |
| **TST-UI-059** | | **Alpha Consistency Audit** | `REQ-UI-054` | Static analysis of `WorkoutExtrema.kt` and `MetricItem.kt`. | No hardcoded `copy(alpha = ...)` literals; all use `TTAlpha` constants. | Verified |
| **TST-UI-060** | | **Primary Metric Hierarchy** | `REQ-UI-055` | Open Workout Details or Segment Details. | The primary value (e.g. Distance) SHALL be rendered in the `primary` theme color. | **Deactivated** |
| **TST-UI-061** | | **Theme Audit** | `REQ-UI-056` | Project-wide grep for `Color(0xFF...)` and `copy(alpha = ...)`. | All UI-related colors and alphas MUST use semantic tokens. | Proposed |
| **TST-EXT-001** | | **Strava Custom Tabs** | `REQ-EXT-004` | 1. Click "Connect to Strava". 2. Verify that a Custom Tab opens. | The authorization page SHALL open in a Chrome Custom Tab. | Proposed |
| **TST-EXT-002** | | **Strava OAuth Flow** | `REQ-EXT-005` | 1. Complete Strava authorization. 2. Observe the loading state. | The app SHALL show a non-blocking loading indicator and correctly store the resulting token. | Proposed |
| **TST-EXT-003** | `ATT-588` | **Strava Profile Scope & Equipment Synchronization Flow** | `REQ-EXT-006` | 1. Verify `StravaHelper.getAuthorizationUrl()` includes `profile:read_all` in the OAuth scope. 2. Provide simulated Strava `/athlete` responses with shoes and bikes. 3. Execute synchronization. 4. Verify equipment is populated in `EquipmentDbHelper` and unlinked duplicate records are avoided. 5. Verify null access token handling. | Gear items from Strava athlete payload are parsed and stored accurately into `EquipmentDbHelper` with correct sport and frame types without duplication. | Verified |
| **TST-EXT-004** | `ATT-497` (`ATT-607`) | **Strava Route Synchronization & Connection Trigger Flow** | `REQ-EXT-007` | 1. Simulate Strava connection success (`StravaAuthState.Success`) in `StravaUploadFragment` and verify route synchronization is initiated. 2. Trigger on-demand sync via `updateStravaRoutes` in Strava settings. 3. Provide mock Strava routes payload (`GET /athletes/{id}/routes`) and stream responses (`GET /routes/{id}/streams`). 4. Verify routes are parsed, streams are converted to `PathPoint`s, and records are stored in `RoutesDatabaseManager` with `RouteSource.STRAVA`. 5. Verify timestamp updates in `TrainingApplication`. 6. Verify missing token/athlete ID handling. | Routes from Strava are synchronized automatically on connection and manually on request, stored with detailed geometry in `RoutesDatabaseManager`, update sync timestamp, and seed into `WorkoutClusterEngine`. | Verified |
| **TST-EXT-005** | `ATT-602` | **Automated Strava Upload for Imported TCX Workouts** | `REQ-EXT-008` | 1. Configure Strava upload as enabled (`TrainingApplication.uploadToCommunity(FileFormat.STRAVA)` returns true). 2. Import a valid TCX workout file via `LegacyImportEngine.importFromTcx()`. 3. Verify that an asynchronous upload task is scheduled with `ExportManager`. 4. Verify that `ExportStatusDatabaseManager` records the pending export status for the imported workout. 5. Verify that when Strava upload is disabled, no upload is scheduled. 6. Verify that local database records and statistics remain intact regardless of network status. | Successfully imported TCX workouts automatically schedule a Strava upload job via ExportManager when Strava integration is active. | Verified |
| **TST-EXT-006** | `ATT-1078` | **Strava Data Purge on Deauthorization & Revocation, and Reauthorization Integrity Flow** | `REQ-EXT-009` | 1. Populate mock Strava tokens, athlete ID, and upload preferences in `SharedPreferences`. 2. Insert mock Strava activity JSON and Strava ID in `StravaUploadDbHelper`. 3. Insert mock starred segments in `SegmentsDatabaseManager`. 4. Insert mock Strava route in `RoutesDatabaseManager`. 5. Insert mock equipment in `EquipmentDbHelper` with `StravaId` and `StravaName`, linked to an ANT+ sensor in `LINKS`. 6. Insert mock native workout in `WorkoutSummariesDatabaseManager` with samples and laps. 7. Execute data purge (simulating user disconnect or refresh token rejection). 8. Verify: tokens and athlete ID removed from `SharedPreferences`; `StravaUploadDbHelper` contains zero Strava activity data or Strava IDs; `SegmentsDatabaseManager` contains zero starred segments; `RoutesDatabaseManager` contains zero Strava routes; `EquipmentDbHelper` equipment row still exists with preserved `_id`, `Name`, and sensor links, but `StravaId` and `StravaName` are `NULL`; native workouts in `WorkoutSummariesDatabaseManager`, samples, and laps remain 100% intact; `WorkManager` has canceled sync workers. 9. Simulate reauthorization: set new mock token, execute `StravaEquipmentSynchronizeThread` with matching bike/shoe names, verify equipment row has `StravaId` re-attached without creating new duplicate rows. 10. In `StravaSettingsDialog`, verify tapping 'Disconnect' triggers the confirmation dialog detailing consequences; verify tapping 'Cancel' dismisses without side effects; verify tapping 'Disconnect' executes deauthorization and purge. | All Strava-originated data (credentials, activity details, segments, routes, gear IDs) is thoroughly purged without affecting native workouts or hardware sensor pairings; reauthorization smoothly re-links existing equipment by name without duplicates; interactive disconnect prompts user with clear consequences before executing purge. | Verified |
| **TST-EXT-007** | `ATT-1177` | **Strava Route & Segment 7-Day TTL Cache Retention, Orphan Pruning & Route Preservation Flow** | `REQ-EXT-010` | 1. Insert mock Strava route (`RouteSource.STRAVA`) with `synced_at = now - 8 days` and another with `synced_at = now - 1 day` in `RoutesDatabaseManager`. 2. Insert mock local route (`RouteSource.LOCAL_GPX`) with `synced_at = 0` / legacy timestamp. 3. Insert mock starred segment and streams with `synced_at = now - 8 days` and another with `synced_at = now - 2 days` in `SegmentsDatabaseManager`. 4. Execute TTL pruning routine. 5. Verify: expired Strava route (> 7 days) is deleted; fresh Strava route (< 7 days) is retained; expired starred segment and its coordinate streams are deleted; fresh segment is retained; local GPX route is completely untouched. 6. Simulate remote sync with orphan detection: populate active Strava route IDs set excluding a currently cached fresh Strava route; execute orphan pruning; verify missing route is deleted. 7. Test route preservation: take an existing Strava route, invoke `duplicateRouteAsLocal`, verify new route is created with `RouteSource.LOCAL_GPX` and identical geometry/bounds; advance simulated time past 7 days and execute TTL pruning; verify duplicated local route survives while original Strava route is evicted. 8. Verify native workout databases (`WorkoutSummaries.db`, `WorkoutSamples.db`, `Laps.db`) and `Equipment.db` are completely untouched throughout all pruning operations. | Strava routes and segments older than 7 days are automatically evicted; orphaned Strava routes and segments are pruned during sync; local GPX and workout routes never expire; duplicated local routes survive TTL pruning; native workouts and hardware pairings remain 100% intact. | Verified |
| **TST-UI-062** | | **Protocol-Aware Menu** | `REQ-UI-044` | 1. Open the Bluetooth sensor list. 2. Check the options menu. 3. Open the ANT+ sensor list. 4. Check the options menu. | The "Check ANT+ Installation" option SHALL NOT be visible in the Bluetooth view, but SHALL be visible in the ANT+ view. | Verified |
| **TST-UI-063** | | **Global Icon Clipping** | `REQ-UI-057` | 1. Open the app in Dark Mode. 2. Inspect sensor and protocol icons. | All icons SHALL appear with subtle 4dp rounded corners, and square white corner artifacts (on PNG assets) SHALL be masked. | Verified |
| **TST-UI-066** | | **Elevation Profile Visibility** | `REQ-UI-058` | 1. Open the Recurring Path Heatmap. 2. Inspect the bottom of the screen. | The elevation profile SHALL NOT be visible. | Verified |
| **TST-UI-067** | | **Robust Sharing Snapshot** | `REQ-UI-059` | 1. Open a Workout Cluster Heatmap (where elevation is hidden). 2. Tap the Share button. 3. Verify that a loading indicator appears on the button. 4. Select a sharing target in the system dialog. | 1. The app SHALL NOT crash. 2. A combined image containing the Header and Map (but no elevation profile) SHALL be generated. 3. The UI MUST remain responsive during generation. | Verified |
| **TST-UI-069** | | **Safe Map Snapshot** | `REQ-UI-060` | 1. Trigger a snapshot multiple times rapidly. 2. Attempt a snapshot on a device with low memory. | 1. App does not crash. 2. A Toast message ("Unable to generate share image") appears if the snapshot fails. | Verified |
| **TST-SET-001** | | **Workout Learning Engine** | `REQ-SET-006`, `007`, `008` | 1. Track a route. 2. Name it \"Park Loop\" and set sport to \"Running\". 3. Track the same route again. | 1. The second workout SHALL automatically be named \"Park Loop #2\". 2. The sport SHALL automatically be set to \"Running\". 3. Changing the name to \"Morning Loop\" SHALL update future suggestions to \"Morning Loop\". | Verified |
| **TST-SET-002** | | **Favorite Tracks Heatmap** | `REQ-SET-009` | 1. Navigate to \"Workout Clusters\" drawer. 2. Select a recurring path. | 1. A map SHALL open showing a heatmap of all associated workouts. 2. Start, Stop, and Apex markers SHALL be visible as clear pins. | Verified |
| **TST-SET-003** | | **Cluster Tuning** | `REQ-SET-010`, `011` | 1. Navigate to \"Workout Clusters\". 2. Open Tuning menu. 3. Adjust the master slider. 4. Optionally toggle detailed controls. 5. Tap \"Recalculate\". | 1. The master slider SHALL adjust all three tolerances proportionally. 2. The Workout Cluster database SHALL be wiped. 3. A background migration SHALL rebuild the clusters with the new parameters. | Verified |
| **TST-SET-004** | | **Workout Cluster Item Map** | `REQ-SET-012` | 1. Navigate to \"Workout Clusters\". 2. Inspect list items. | 1. Each item SHALL display a small map. 2. Start (Green pin with start icon), End (Red pin with stop icon), and Apex (Blue pin with distance icon) SHALL be visible on the map. | Verified |
| **TST-SET-005** | | **Name-Aware Clustering** | `REQ-SET-013` | 1. Identify two workouts with same name but high spatial drift. 2. Trigger recalculation. | The system SHALL group them into the same cluster by applying the name-match bonus to the similarity score. | Verified |
| **TST-SET-006** | | **Auto-Dismiss Tuning** | `REQ-SET-014` | 1. Open Tuning screen. 2. Tap \"Recalculate All Clusters\". 3. Wait for completion. | The UI SHALL automatically navigate back to the Favorite Tracks list view upon completion. | Verified |
| **TST-SET-007** | | **Unique Cluster Naming** | `REQ-SET-015` | 1. Create a cluster named \"Home\". 2. Create a spatially distinct route and also name it \"Home\". | The second cluster SHALL be automatically named \"Home var 2\". | Verified |
| **TST-SET-008** | | **Edit Cluster Identity** | `REQ-SET-016` | 1. Select a workout cluster. 2. Tap Edit Icon in TopAppBar. 3. Enter new name and select a different sport. 4. Save. | 1. The name and sport SHALL be updated in the database. 2. The UI SHALL immediately reflect the new name and sport icon in the list and details view. | Verified |
| **TST-SET-009** | | **Manual Workout Reassignment** | `REQ-SET-017` | 1. Open a Workout Heatmap. 2. Tap a specific workout polyline. 3. Select a different cluster from the candidate list. 4. Save. | 1. The workout SHALL be moved to the target cluster. 2. Centroids for both clusters SHALL be recalculated. 3. The UI SHALL refresh to show the workout in its new home. | Verified |
| **TST-SET-010** | | **Edit Workout Suggestions** | `REQ-SET-018` | 1. Edit a workout. 2. Tap the \"My Locations\" icon in the Name field. 3. Select a suggested cluster. | 1. The workout name SHALL update to the cluster's name. 2. The sport SHALL update to the cluster's sport. 3. Saving SHALL persist the cluster association. | Verified |
| **TST-SET-011** | | **Manual Cluster Creation** | `REQ-SET-019` | 1. Navigate to \"Workout Clusters\". 2. Tap \"+\" FAB. 3. Fill in name and distance. 4. Tap map to set 3 points. 5. Save. | 1. A new cluster SHALL be created in the database with 0 hit count. 2. The cluster SHALL be visible in the list. | Verified |
| **TST-SET-012** | | **Workout Peek & Move** | `REQ-SET-020` | 1. Open a Workout Heatmap. 2. Tap a specific workout track. 3. Observe the Bottom Sheet peek. 4. Tap the \"Move\" FAB in the peek. | 1. The workout details SHALL be visible in the sheet. 2. The \"Move Workout\" dialog SHALL appear upon tapping the FAB. | Verified |
| **TST-SET-013** | | **Manual Fingerprint Edit** | `REQ-SET-021` | 1. Open a Workout Heatmap. 2. Tap a signature marker (Start/End/Apex). 3. Select \"Edit Position\". 4. Tap map to set new position. 5. Save. | 1. The cluster centroid SHALL be updated in the database. 2. Future matching SHALL use the new coordinates. | Verified |
| **TST-SET-014** | | **Workout Markers in Cluster** | `REQ-SET-022` | 1. Open a Workout Heatmap. 2. Observe the distribution of markers for all members. 3. Tap a specific workout track OR any marker. 4. Inspect markers in the peek. | 1. Markers for ALL member workouts SHALL be visible with subtle transparency. 2. Tapping a marker SHALL correctly trigger the detailed peek for that workout. | Verified |
| **TST-SET-015** | | **Imperial Units in Clusters** | `REQ-SET-023` | 1. Switch to Imperial. 2. Add manual cluster (miles). 3. View tuning details (mile). | 1. Manual distance input SHALL accept miles and store as meters. 2. Tuning tolerances SHALL be displayed in miles with high precision. | Verified |
| **TST-SET-016** | | **Cluster Equipment Determination** | `REQ-SET-024` | 1. Open a Workout Cluster. 2. Verify the associated equipment list (→). | 1. The list of equipment linked to the cluster's primary sport SHALL be visible. | Verified |
| **TST-SET-017** | | **Workout Cluster Collapsing Header** | `REQ-SET-025` | 1. Navigate to \"Workout Clusters\". 2. Scroll the list downwards. 3. Observe the TopAppBar behavior. | 1. The header SHALL collapse smoothly as the user scrolls. 2. The header SHALL re-expand when the user scrolls back to the top. | Verified |
| **TST-SET-018** | | **Workout Cluster Tabbed Layout** | `REQ-SET-026` | 1. Navigate to \"Workout Clusters\". 2. Observe the tabs at the top. 3. Swipe between tabs (All, Bike, Run, Other). | 1. The list SHALL filter clusters correctly based on the selected sport type tab. 2. Swiping SHALL be smooth and synchronized with the tab indicator. | Verified |
| **TST-SET-019** | | **Workout Cluster Scroll Persistence** | `REQ-SET-027` | 1. Scroll down the \"All\" tab. 2. Select a cluster to open its heatmap. 3. Navigate back. | 1. The list SHALL remain scrolled to the previously viewed position. 2. The active tab SHALL be preserved. | Verified |
| **TST-SET-020** | | **Unified FAB Layout** | `REQ-SET-028` | 1. Navigate to \"Workout Clusters\". 2. Observe FAB position and styling. 3. Compare with \"Sport Types\" and \"Equipment\" FABs. | 1. The FAB SHALL be positioned at the bottom-right. 2. It SHALL correctly respect system navigation bar padding. | Verified |
| **TST-SET-021** | | **Identity Arbitration** | `REQ-SET-029`, `030` | 1. Link a specific sensor to a bike. 2. Record a session with that sensor on a route matched to a cluster with a different majority sport. | 1. The workout SHALL adopt the cluster's Name. 2. The sport and equipment SHALL remain hardware-driven (Vetoing the route majority). | Verified |
| **TST-SET-022** | | **Route-to-Cluster Matching** | `REQ-SET-031` | 1. Import a GPX Route. 2. Navigate to \"Workout Clusters\". | 1. A new Workout Cluster SHALL automatically be created for the imported route. 2. Recalculating history SHALL also include existing routes as cluster seeds. | Verified |
| **TST-SET-023** | | **Reference Route in Heatmap** | `REQ-SET-032` | 1. Open a Workout Cluster that is linked to an explicit Route. 2. Observe the heatmap visualization. | 1. The authoritative route's path SHALL be visible as a solid, prominent line overlaid on the workout heatmap. | Verified |
| **TST-SET-024** | | **Workout Cluster Deletion** | `REQ-SET-033` | 1. Open a Workout Cluster detail view. 2. Long-click the header. 3. Select \"Delete\". 4. Confirm deletion. | 1. The cluster SHALL be deleted. 2. Associated workouts SHALL remain in history but lose their cluster assignment. | Verified |
| **TST-SET-025** | | **Stable Cluster Naming** | `REQ-SET-034` | 1. Identify a cluster named \"Park Loop\". 2. Track a workout matching this cluster. | 1. The workout SHALL be named \"Park Loop #X\". 2. The cluster name SHALL remain \"Park Loop\". | Verified |
| **TST-SET-026** | | **Preview Heatmap** | `REQ-SET-035` | 1. Open the Favorite Tracks list. 2. Observe the mini-map in a list item. | 1. If a Route is linked, its path is visible. 2. If no Route, a subtle heatmap of recent tracks is visible. 3. All 3 signature markers are visible. | Verified |
| **TST-SET-027** | | **Persistent DB Connection** | `REQ-SET-036` | 1. Start the app. 2. Open Android Studio Database Inspector. | 1. RouteClusters.db should be visible and accessible as an active connection. | Verified |
| **TST-SET-028** | | **Hit Count Synchronization** | `REQ-SET-037`, `038` | 1. Identify a cluster with hit count > 0. 2. Use Database Inspector to manually clear `clusterId` for its workouts. 3. Open Favorite Tracks list. | 1. The hit count should automatically update to 0. 2. Assigning a workout via editor should correctly increment both DBs. | Verified |
| **TST-SET-029** | | **Unclustered Tab Verification** | `REQ-SET-040` | 1. Navigate to Favorite Tracks. 2. Open 'Offen' tab. 3. Tap a workout. | 1. Workouts with clusterId=-1 are visible. 2. Selection dialog opens and reassignment works correctly. | Verified |
| **TST-SET-030** | | **Locale-Aware Distance Input** | `REQ-SET-039` | 1. Set device locale to German. 2. Open 'Add Workout Cluster' flow. 3. Enter '5,5' in the distance field. | 1. The 'Save' button becomes enabled. 2. Saving the cluster correctly parses '5,5' as 5.5 units (km or miles). | Verified |
| **TST-SET-032** | `ATT-390` | **Markers & Framing in Previews** | `REQ-SET-042` | 1. Open Workout History, Routes, and My Locations lists. 2. Observe the mini-maps. | 1. Workout previews show Start (Green), End (Red), and Apex (Blue) markers. 2. Route previews show ONLY Start and End markers. 3. All previews correctly frame the entire route, ensuring no parts are cropped. | Verified |
| **TST-SET-033** | | **Fingerprint Parity** | `REQ-SET-043` | 1. Import a Route. 2. Record a workout on that exact route. 3. Navigate to Favorite Tracks. | 1. Both the Route and Workout SHALL have the exact same Apex coordinate (Furthest from Start). 2. Grouping accuracy is 100%. | Verified |
| **TST-SET-034** | `ATT-399` | **Sport-Aware Clustering** | `REQ-SET-010`, `REQ-SET-043` | 1. Navigate to My Locations -> Tuning. 2. Toggle 'Take Sport Type into account' to ON. 3. Recalculate clusters. 4. Verify that Runs and Rides on the same path remain in separate clusters. 5. Toggle to OFF and recalculate. 6. Verify they merge into one cluster. | Clustering logic correctly applies/removes the sport-type similarity penalty. | Proposed |
| **TST-SET-035** | `ATT-427` | **Sport Mismatch with UNKNOWN** | `REQ-SET-043` | 1. Ensure Tuning toggle is ON. 2. Record a 'Running' workout on a path that currently has an 'UNKNOWN' cluster. | The 'Running' workout SHALL NOT match the 'UNKNOWN' cluster (+2.0 penalty applied). | Proposed |
| **TST-SET-040** | `ATT-278` | **Unclustered Workout Navigation** | `REQ-SET-056` | 1. Open 'Unclustered' tab in My Locations. 2. Verify layout parity with clusters (mini-map exists). 3. Tap a workout. 4. Verify navigation to map. 5. Tap 'Move to Cluster' icon. | 1. Layout is consistent. 2. Map opens correctly. 3. Cluster selection dialog appears. | Verified |
| **TST-SET-041** | `ATT-280` | **Cluster Member Listing** | `REQ-SET-057` | 1. Open 'Clusters' tab. 2. Click on the \"X recordings\" text for a cluster. 3. Verify navigation to a list containing only those recordings. | 1. List is correctly filtered by clusterId. | Verified |
| **TST-SET-042** | `ATT-317` | **Workout Session Naming Isolation** | `REQ-SET-034`, `REQ-SET-044` | 1. Track a workout matching cluster 'Park Loop'. 2. Open 'Edit Workout' screen. 3. Change name to 'Morning Run'. 4. Save. | 1. Workout is named 'Morning Run'. 2. Cluster name remains 'Park Loop' in the database and cluster list. | Proposed |
| **TST-SET-043** | `ATT-395` | **Cluster Visibility & Re-clustering** | `REQ-SET-058`, `REQ-SET-059` | 1. Open the Workout History list. 2. Verify each item displays its Cluster Name with the leading route icon at the very left below the workout name. 3. Edit a workout and manually re-assign it to a different cluster. 4. Save and verify the update in the list. | 1. Cluster name and icon are visible and aligned to the left. 2. Re-assignment in editor successfully updates the workout's association. | Verified |
| **TST-SET-044** | `ATT-429` | **Selection Dialog Hit Count Accuracy** | `REQ-SET-038` | 1. Identify a cluster with 5 associated workouts. 2. Open a workout for manual re-clustering or trigger a TCX import interaction. 3. Open the "Select Existing Route" dialog. | The cluster SHALL correctly display "5 Aufzeichnungen" (or equivalent) in the suggestion list. | Verified |
| **TST-SET-045** | `ATT-463` | **Selectable Cluster Markers** | `REQ-SET-060` | 1. Open a Workout Cluster heatmap. 2. Tap the Marker Options button. 3. Uncheck "Start Point". 4. Verify that all green pins (signature and member distribution) disappear. 5. Re-check and Verify reappearance. 6. Re-check persistence after app restart. | Visibility reflects user selection and persists across sessions. | Verified |
| **TST-SET-046** | `ATT-447` | **Reactive Editor Suggestions** | `REQ-SET-018` | 1. Edit a 'Running' workout on a 'Cycling' path. 2. Observe high similarity score (> 5.0) for 'Cycling' cluster in suggestions. 3. Change workout sport to 'Cycling'. 4. Verify score drops below 1.0 (penalty removed). | Scores update reactively to sport changes. | Proposed |
| **TST-SET-047** | `ATT-543` | **Idempotent Route Cluster Creation** | `REQ-SET-061` | 1. Import multiple TCX workout files that follow the same route name (e.g. "Route A"). 2. Verify cluster assignment. | The system refines the existing "Route A" cluster and does NOT create a duplicate cluster named "Route A var 2". | Verified |
| **TST-SET-048** | `ATT-544` | **Automatic Zero-Workout Cluster Purging** | `REQ-SET-062` | 1. Trigger a TCX import or cluster refresh. 2. Inspect the cluster list and database for orphan clusters with 0 assigned workouts and no linked Route. 3. Import an explicit Route with 0 workouts. | Unlinked orphan route clusters with 0 workouts are automatically deleted from the database. Clusters linked to an explicit Route are preserved even with 0 workouts. | Verified |
| **TST-SET-049** | `ATT-1138` | **Route Cluster Apex Arithmetic Mean & Live Tracking Synchronization** | `REQ-SET-063` | 1. Assign or learn workouts into a Workout Cluster. 2. Verify that the cluster's `maxDispLat` and `maxDispLng` are the exact arithmetic mean of the member workouts' maximum displacement coordinates. 3. Delete or move a member workout and verify the moving average reverses accurately. 4. Select/inspect the cluster in `WorkoutClustersViewModel` and verify that the cluster's apex in SQLite is NOT mutated or overwritten. 5. Verify `WorkoutClusterEngine.onWorkoutFinished` propagates `maxDisplacementLatLng` instead of `startLatLng`. 6. Verify `SpeedAndLocationDevice` clears `mStartLocation` upon reset. | The cluster apex marker strictly represents the arithmetic mean of all member workouts, without UI-triggered database mutations or track-snapping deviations. Live workout tracking and accumulator resets preserve spatial integrity. | Verified |
| **TST-SET-050** | `ATT-666` | **Explicit Cluster Selection (No Cluster, New Cluster) in Edit Workout** | `REQ-SET-059` | 1. Open the Workout History list and tap a workout to open `EditWorkoutScreen`. 2. Verify an explicit Cluster Assignment field is rendered directly below the Workout Name input, displaying the route icon (`my_locations`), label ("Route Assignment" / "Selected Route"), and current cluster name (or "Unclustered" / "Leave Unclustered"). 3. Tap the Cluster Assignment field to launch the assignment dialog (harmonized with `RouteClusterNamingDialog`). 4. Select "Leave Unclustered" / "Kein Cluster" and save the workout: verify that the workout is unassigned (`clusterId = -1L`), its previous cluster hit count is decremented, and the workout header displays "Unclustered". 5. Re-open editor, tap the Cluster Assignment field, select "Create New..." / enter a new route name, and save: verify a new cluster is generated from the workout's spatial fingerprint and assigned to the workout. 6. Re-open editor, tap the Cluster Assignment field, select an existing cluster candidate: verify the workout is reassigned, hit counts are updated atomically, and the new cluster name is displayed. | 1. Cluster selection is prominent and accessible below the workout name with the look and feel of the TCX import dialog. 2. "No Cluster" explicitly detaches the workout from any cluster. 3. "New Cluster" creates a new cluster from the workout. 4. Candidate cluster selection updates the cluster identity and hit counts reliably. | Verified |
| **TST-SET-051** | `ATT-773` | **Speed-Based Multi-Sport Cluster Discovery & Identity Resolution** | `REQ-SET-064` | 1. Create/seed a Workout Cluster with `BSportType.BIKE` for a known route geometry. 2. Simulate/record a workout following the identical route geometry with average speed 3.0 m/s without paired cadence/power sensors and without user pre-selection (speed suggests both `RUN` and `BIKE`). 3. Verify that `suggestCluster` identifies the existing bike cluster with 0.0 sport penalty and links the workout to it, adopting `BSportType.BIKE`. 4. Repeat with explicit user pre-selection of `BSportType.RUN`: verify the bike cluster receives +5.0 penalty and is rejected. 5. Repeat with high-confidence cadence sensor active: verify candidate sports are strictly `{BIKE}`. | 1. Ambiguous intermediate-speed workout matches existing route cluster without creating false duplicate cluster. 2. Workout adopts cluster sport type. 3. Explicit user pre-selection and hardware sensors take absolute precedence. | Verified |
| **TST-SET-052** | `ATT-502` | **Altitude Extrema-Aware Cluster Matching, Migration & Fingerprint Relocation** | `REQ-SET-065` | 1. Seed existing database with clusters and verify schema migration from v9 to v10 with automated backfill of member workout altitude centroids.<br>2. Evaluate similarity of workout against existing cluster with matching Start, End, Apex, and Distance, but differing Min/Max altitude positions (> 400m apart): verify score >= 1.0 (rejected match).<br>3. Evaluate workout with matching Min/Max altitude positions: verify score < 1.0 (matching cluster).<br>4. Verify fallback to 4-term matching with zero penalty when altitude is null or preference disabled.<br>5. Open Cluster Heatmap: verify Min and Max Altitude markers are rendered and toggleable in options menu.<br>6. Enter fingerprint editing: drag Min and Max Altitude markers, save, and verify persisted coordinates. | 1. Database upgrades to v10 seamlessly with historical altitude backfill.<br>2. 3D altitude-aware scoring distinguishes routes with distinct vertical profiles.<br>3. Flat/sensorless routes degrade gracefully.<br>4. Interactive map markers display and relocate altitude extrema anchors reliably. | Verified |
| **TST-SET-053** | `ATT-785` | **Workout Cluster Auto-Naming Counter Ergonomics Verification** | `REQ-SET-066` | 1. Unit test `WorkoutClusterEngine.formatClusterWorkoutName`:<br>• Test with `hitCount = 0` (or `1`): verify returned string is exactly `clusterName` without `"#1"` or any suffix.<br>• Test with `hitCount = 2`: verify returned string is `clusterName + " #2"`.<br>• Test with `hitCount = 5`: verify returned string is `clusterName + " #5"`.<br>2. Initial Cluster Assignment:<br>• Assign a workout to an empty cluster (`hitCount = 0`): verify workout name in database is set to `cluster.name` (NOT `cluster.name + " #1"`).<br>3. Subsequent Cluster Assignment:<br>• Assign a second workout to the same cluster: verify second workout name is set to `cluster.name + " #2"`.<br>4. Live Tracking Auto-Naming (`TrackerService`):<br>• Finish live tracking with high-confidence sensors matching a cluster with 0 hits: verify autoName is `suggestion.getName()`.<br>• Finish live tracking matching a cluster with 1 hit: verify autoName is `suggestion.getName() + " #2"`.<br>5. Workout Editor Selection (`EditWorkoutViewModel`):<br>• Apply a cluster with `hitCount = 0`: verify proposed workout name in UI is `cluster.name`.<br>• Apply a cluster with `hitCount = 1`: verify proposed workout name in UI is `cluster.name + " #2"`.<br>6. Normalization & Deduplication Invariants:<br>• Verify `stripHitCount` and `normalizeName` return identical base outputs for names with and without `"#1"`. | The first workout of a cluster receives the clean cluster name without `"#1"`. Subsequent workouts receive incrementing numerical suffixes `"#<count>"`. Clustering matching, suggestions, and normalization remain 100% stable. | Verified |
| **TST-SET-054** | `ATT-714` | **Optional Route Cluster Counter Verification** | `REQ-SET-067` | 1. Database Schema & Migration Verification:<br>• Seed database with v10 schema and clusters lacking `has_counter`.<br>• Trigger migration to v11: verify column `has_counter` is added with default value 1.<br>• Query all clusters: verify `hasCounter == true` for existing entries.<br>2. Unit test `WorkoutClusterEngine.formatClusterWorkoutName`:<br>• Test with `hasCounter = true, hitCount = 1`: verify returned name is `clusterName`.<br>• Test with `hasCounter = true, hitCount = 3`: verify returned name is `clusterName + " #3"`.<br>• Test with `hasCounter = false, hitCount = 1`: verify returned name is `clusterName` (no suffix).<br>• Test with `hasCounter = false, hitCount = 3`: verify returned name is `clusterName` (no suffix).<br>• Test with `hasCounter = false, hitCount = 10`: verify returned name is `clusterName` (no suffix).<br>3. Cluster Identity Modification:<br>• Open `EditWorkoutClusterIdentityDialog` for a cluster: toggle "Activity Counter" to off (`false`) and save.<br>• Verify database persists `has_counter = 0`.<br>• Assign subsequent workouts to this cluster: verify workout names do not receive `#<count>`.<br>4. Live Tracking & Editor Verification:<br>• Live tracking completion with cluster suggestion having `hasCounter = false`: verify suggested workout name has no suffix.<br>• Edit workout applying cluster with `hasCounter = false`: verify proposed workout name has no suffix.<br>5. Invariant Verification:<br>• Verify cluster hitCount in database continues to increment regardless of `hasCounter` setting. | When `hasCounter` is true (default), sequential numbering `#2`, `#3` applies. When `hasCounter` is false, workouts retain the static cluster name without numbers. Existing databases upgrade safely to v11. | Verified |
| **TST-SET-055** | `ATT-221` | **Workout Cluster Performance & Volume Statistics Verification** | `REQ-SET-068` | 1. Database Aggregation Unit Tests (`WorkoutClusterStatsTest.kt`):<br>• Zero Workouts: Query cluster with 0 sessions; verify `workoutCount = 0`, `bestDurationSec = null`, and safe default values without crashes.<br>• Single Workout: Verify Best Time equals Average Time equals workout duration; Total Distance equals workout distance; Total Ascent equals workout ascent.<br>• Multiple Workouts: Seed cluster with sessions of durations [2400s, 2100s, 2700s], distances [10000m, 10200m, 9800m], ascents [150m, 200m, 180m]; verify Best Time is 2100s, Average Time is 2400s, Total Distance is 30000m, Total Ascent is 530m.<br>• Unfinished Workout Filter: Include unfinished workout (`finished = 0`) with shorter duration; verify it is excluded from Best Time calculation.<br>2. Recency & Relative Time Formatting:<br>• Test `lastHitRelativeFormatted` for sessions logged today ("Today"), yesterday ("Yesterday"), 4 days ago ("4 days ago"), and 3 weeks ago ("3 weeks ago").<br>3. Presentation & Sport-Adaptive Verification:<br>• Running Sport: Verify velocity formats as Pace (e.g., `min/km`).<br>• Cycling Sport: Verify velocity formats as Speed (e.g., `km/h`).<br>• Detail Dashboard (`WorkoutClusterSummaryHeader`): Verify 3-tier card layout renders all metrics with trophy icon for Best Time.<br>• List Card (`ClusterItem`): Verify Best Time, Total Distance, and Last Hit render cleanly without clipping the 100x100 preview map.<br>4. Localization & Parity Check:<br>• Verify 100% translation coverage for all new stat labels across all 9 supported languages (EN, DE, ES, FR, IT, JA, NL, PL, PT).<br>5. Invariant & Regression Check:<br>• Full regression suite (`./gradlew testDebugUnitTest`) passes with 0 failures; spatial clustering and sorting remain 100% intact. | Aggregate statistics accurately calculate Best Time, Average Time, Average Speed/Pace, Total Distance, Total Climbing, and Recency. Metrics adapt to sport type, handle zero-workout edge cases, and render in a structured 3-tier layout across all 9 locales. | Verified |
| **TST-SET-056** | `ATT-820` | **Multi-Sport Candidate Clustering & Cost Minimization in Workout Editor & Tracker** | `REQ-SET-064` | 1. *Unit Test `EditWorkoutViewModel.fetchClusterSuggestions` with multi-sport candidate evaluation*:<br>• Create a workout with `bSportType = BSportType.UNKNOWN`, avg speed ~10 km/h (2.78 m/s). Speed matches both `RUN` (Running) and `BIKE` (custom sport `Einkaufen`, 7–15 km/h).<br>• Provide two candidate clusters: Cluster A (`BIKE`, geometric score 0.25) and Cluster B (`RUN`, geometric score 0.40).<br>• Verify that `fetchClusterSuggestions` applies 0.0 sport penalty to both clusters and ranks Cluster A first (cost 0.25 < 0.40).<br>• Verify that when `bSportType` was initially guessed as `RUN`, candidate sports are derived from average speed so that Cluster A (`BIKE`) is NOT artificially penalized with +5.0 mismatch penalty.<br>2. *Unit Test `EditWorkoutViewModel.updateSuggestedSportTypeNames` with overlapping sports*:<br>• For a workout with avg speed matching both Running and `Einkaufen` where `bSportType == UNKNOWN`, verify suggested sport types dropdown includes all candidate sports across all matching base sports (`Running` and `Einkaufen`).<br>3. *Unit Test `SportTypeDatabaseManager` speed query with `UNKNOWN`*:<br>• Query speed at ~7 km/h (1.94 m/s) with `BSportType.UNKNOWN`. Verify both `Running` and `Einkaufen` are returned without low-speed walking fallback blocking candidate sports.<br>4. *Unit Test Live Tracking Multi-Sport Resolution (`TrackerService.java` & `WorkoutClusterEngine.suggestCluster`)*:<br>• End a live tracking session with speed ~10 km/h and no sensors/pre-selection.<br>• Verify that cluster similarity cost is evaluated across all candidate sports before locking sport identity, and the lowest cost cluster is selected.<br>5. *Invariant & Precedence Verification*:<br>• Explicit user pre-selection (`RUN`): verify `BIKE` cluster receives +5.0 penalty and is rejected.<br>• High-confidence cadence sensor: candidate sports are strictly `{BIKE}`. | 1. Multi-sport speed profile correctly identifies all candidate sports across base sport boundaries without premature single-sport lock-in.<br>2. `EditWorkoutViewModel` evaluates clusters against candidate sports with 0.0 penalty and selects/ranks the lowest geometric cost cluster first.<br>3. Suggested sport type dropdown in `EditWorkoutScreen` lists all speed-matching sports.<br>4. `TrackerService` assigns the lowest-cost cluster on live tracking completion.<br>5. Dedicated hardware sensors and user pre-selection retain strict precedence. | Verified |
| **TST-SET-057** | `ATT-503` | **Direct Navigation from Workout to Linked Workout Cluster Verification** | `REQ-SET-058` | 1. *Compose Unit Test for `WorkoutHeader`*:<br>• Render `WorkoutHeader` with `clusterId = 42`, `clusterName = "Morning Run"`, and `onClusterClick` callback.<br>• Verify an interactive `Button` is displayed with subtle primary tint colors, containing the `my_locations` icon and `"Morning Run"`.<br>• Click the cluster button: verify `onClusterClick` receives `42L`.<br>• Click outside the button on the header surface: verify `onClicked` (edit workout) is invoked without triggering `onClusterClick`.<br>• Render `WorkoutHeader` with `clusterId = -1L, clusterName = null`: verify the neutral, non-clickable "Unclustered" text is displayed and clicks do not trigger `onClusterClick`.<br>2. *Unit Test for Navigation Flow (`WorkoutNavigationEvents` & `WorkoutClustersViewModel`)*:<br>• Trigger `WorkoutNavigationEvents.triggerCluster(42L)`: verify event is emitted on `navigateToClusterLiveData`.<br>• Test `WorkoutClustersViewModel.selectClusterById(42L)`: verify target cluster is selected and its map state/workouts are loaded.<br>3. *Integration & Manual Test Procedure*:<br>• Open Workout History (`WorkoutSummariesTabbedFragment`).<br>• Locate a workout associated with a cluster: tap the cluster button: verify direct navigation to `WorkoutClustersFragment` displaying `WorkoutClusterHeatmapScreen` for that cluster.<br>• Tap top app bar back arrow or system Back button: verify navigation returns directly to Workout History.<br>• Open a workout in `TrackOnMapScreen`: tap the cluster button: verify direct navigation to the cluster heatmap. | 1. Clustered workouts render a prominent, compact subtle primary tinted Button mirroring the cluster detail screen's recordings button.<br>2. Tapping the cluster button immediately routes to the linked cluster heatmap without triggering edit mode.<br>3. Unclustered workouts display neutral non-interactive text.<br>4. Back navigation returns reliably to the originating workout view. | Verified |
| **TST-SET-058** | `ATT-832` | **Workout, Route & Segment Bottom Scaffold Status Bar Boundary & Peek Verification** | `REQ-SET-069` | 1. *Unit / Architectural Verification*:<br>• Verify that `PeriodMapScreen.kt`, `MapFragmentWithTrack.kt`, and `WorkoutClusterHeatmapScreen.kt` wrap `BottomSheetScaffold` in `BoxWithConstraints` and constrain `sheetContent` height to `maxHeight - statusBarHeight`.<br>• Verify that the background map continues to draw edge-to-edge behind/underneath the system status bar.<br>• Verify that `TrackOnMapScreen.kt` passes `Modifier.fillMaxWidth()` to `WorkoutHeader` so the workout popup map container is not squashed.<br>• Verify that `MinimumDragHandle` retains its standard height (~24 dp) and minimal padding.<br>2. *Initial Popup (Peek) Verification*:<br>• Open a period map (`PeriodMapScreen`) and tap a workout: verify the initial workout popup peeks cleanly at the bottom with all key header details and map visible.<br>• Open `MapFragmentWithTrack` and tap a segment or route: verify the initial peek is compact and unobtrusive.<br>• Open a cluster heatmap (`WorkoutClusterHeatmapScreen`) and tap a workout: verify the initial workout peek displays properly with map visible.<br>3. *Full Expansion Verification*:<br>• Drag the bottom sheet in `PeriodMapScreen` all the way to the top (`SheetValue.Expanded`): verify the sheet top boundary stops cleanly below the system status bar (clock, battery, Wi-Fi).<br>• Drag the bottom sheet in `MapFragmentWithTrack` all the way to the top: verify the top boundary stops below the status bar.<br>• Drag the bottom sheet in `WorkoutClusterHeatmapScreen` all the way to the top: verify the top boundary stops below the status bar.<br>4. *Regression Verification*:<br>• Run full clean-room unit test suite (`./gradlew testDebugUnitTest`) to ensure zero regressions across all modules. | 1. When fully expanded, bottom sheets stop cleanly at the bottom of the system status bar across Workouts, Routes, and Segments with zero overlap.<br>2. Background maps render edge-to-edge under the status bar.<br>3. Workout popup displays both header and track map.<br>4. Initial popups display compact drag handles and unclipped header information.<br>5. All unit tests pass. | Verified |
| **TST-SET-059** | `ATT-506` | **Workout Popup Direct Edit & Header Button Layout Verification** | `REQ-SET-070` | 1. *Unit & Layout Verification*:<br>• Verify `WorkoutHeader` renders `ic_table_edit` on the very right when `onEditWorkout` is provided.<br>• Verify `ClusterSummaryHeader` renders `EditLocationAlt` on the left and `ic_table_edit` on the very right.<br>• Verify clicking either the edit icon button or the header surface invokes `onEditWorkout(workoutId)`.<br>2. *Period Map Popup Navigation Verification*:<br>• Open a period map in `PeriodsFragment`.<br>• Tap a workout to display the peek popup.<br>• Tap the edit button on the very right: verify `EditWorkoutScreen` opens with the workout's details.<br>• Change a detail (e.g. name or sport) and press back: verify view returns to `PeriodMapScreen` with the updated detail reflected.<br>3. *Cluster Heatmap Popup Navigation Verification*:<br>• Open a favorite track in `WorkoutClustersFragment`.<br>• Tap a member workout in the heatmap to peek its details.<br>• Verify the header displays the cluster transfer icon (`SwapHoriz`) on the left and the edit icon (`ic_table_edit`) on the very right.<br>• Tap the edit button: verify `EditWorkoutScreen` opens.<br>• Press back: verify view returns to `WorkoutClusterHeatmapScreen`.<br>4. *Regression Suite*:<br>• Execute `./gradlew testDebugUnitTest` to ensure zero regressions across all modules. | 1. Edit button appears on the very right across both workout popup and cluster summary headers.<br>2. Tapping the edit button or header opens `EditWorkoutScreen` without crashing or losing map state.<br>3. Returning from editor updates workout details cleanly.<br>4. All unit tests pass. | Verified |
| **TST-SET-060** | `ATT-850` | **Workout Card Click-to-Map Navigation & Dedicated Header Edit Action Verification** | `REQ-SET-071` | 1. *Unit & Interaction Verification (`WorkoutSummaryTest.kt` / `WorkoutHeaderTest.kt`)*:<br>• Verify that clicking `WorkoutHeader` surface invokes `onClicked` (routed to `onMapClick`).<br>• Verify that clicking `WorkoutDescription`, `WorkoutDetails`, and `WorkoutExtrema` invokes `onMapClick`.<br>• Verify that `WorkoutHeader` renders `ic_table_edit` before the `more_vert` export button (after `actions()` and before `more_vert`).<br>• Verify that clicking `ic_table_edit` invokes `onEditWorkout`.<br>• Verify that clicking the cluster button invokes `onClusterClick` and does not invoke `onMapClick` or `onEditWorkout`.<br>• Verify that tapping the 3-dots export button opens the dropdown and does not invoke `onMapClick`.<br>2. *Integration / Manual Workflow in Workout History & Map View*:<br>• Open Workout History (`WorkoutSummariesTabbedFragment` / `WorkoutSummariesListFragment`).<br>• Tap the header, description, metrics, or extrema of a workout card: verify immediate navigation to `TrackOnMapScreen`.<br>• In `TrackOnMapScreen`, tap the edit button (`ic_table_edit`): verify immediate opening of `EditWorkoutScreen` without requiring back navigation.<br>• Save or dismiss `EditWorkoutScreen`: verify return directly to `TrackOnMapScreen`.<br>• Press back from `TrackOnMapScreen`: verify return to the workout list at the same scroll position.<br>• Tap the edit button (`ic_table_edit`) in the workout list header (situated before the 3-dots button): verify immediate opening of `EditWorkoutScreen`.<br>• Save or dismiss the editor: verify return to the workout list with updated details reflected.<br>• Tap the cluster button on a clustered workout: verify direct navigation to the cluster heatmap.<br>• Tap the export status badge: verify the export details dialog opens.<br>3. *Regression Suite*:<br>• Execute `./gradlew testDebugUnitTest` to ensure zero regressions across all modules. | 1. Tapping workout body sections (header, description, details, extrema, map) reliably navigates to `TrackOnMapScreen`.<br>2. Tapping the dedicated edit button in the header (before the 3-dots) reliably opens `EditWorkoutScreen`.<br>3. Tapping the edit button within `TrackOnMapScreen` immediately opens `EditWorkoutScreen`, returning to `TrackOnMapScreen` upon dismissal.<br>4. Cluster button, export status badge, and export menu function independently without navigation conflicts.<br>5. All unit tests pass. | Verified |
| **TST-SET-061** | `ATT-956` | **Edit Workout New Cluster Creation & Name Synchronization Verification** | `REQ-SET-072` | 1. *Unit Test `EditWorkoutClusterDialog` Pre-fill*:<br>• Open dialog with `initialWorkoutName = "Hausrunde #3"`: verify text field initializes with `"Hausrunde"` (`stripHitCount`).<br>• Open dialog with unnumbered name `initialWorkoutName = "Hausrunde"`: verify text field initializes with `"Hausrunde"`.<br>2. *Unit Test `WorkoutClusterEngine.createNewClusterFromWorkout` Database Persistence*:<br>• Create new cluster from workout with existing `WORKOUT_NAME = "Hausrunde #3"` and custom cluster name `"Neue Runde"`.<br>• Verify `assignClusterToWorkout` updates `WorkoutSummaries.WORKOUT_NAME` in SQLite to `"Neue Runde"` (with `forceIdentity = true`).<br>• Verify `CLUSTER_ID` is set to the new cluster ID.<br>• Verify old cluster hit count is decremented and new cluster hit count is 1.<br>3. *Unit Test `EditWorkoutViewModel.createNewCluster` UI State & Smart Name Preservation*:<br>• Unmodified name case: User did not edit workout name field. Call `createNewCluster("Neue Runde")`. Verify `_workoutData` immediately reflects `clusterName = "Neue Runde"` and `workoutName = "Neue Runde"`.<br>• User-typed custom name case: User typed `"Mein Spezieller Lauf"` (`updateWorkoutName("Mein Spezieller Lauf")`). Call `createNewCluster("Neue Runde")`. Verify `_workoutData` reflects `clusterName = "Neue Runde"` while preserving `workoutName = "Mein Spezieller Lauf"`.<br>• Repository reload resistance: Simulate background emission from `repository.workout` after reload. Verify user-typed name is not overwritten by the database value.<br>4. *Integration / Manual Verification*:<br>• Open `EditWorkoutScreen` for workout matched to `"Hausrunde #3"`.<br>• Tap Route field to open `EditWorkoutClusterDialog`. Verify new route text field pre-fills `"Hausrunde"`.<br>• Enter `"Neue Abendrunde"`, tap OK.<br>• Verify Edit Workout screen immediately displays `"Neue Abendrunde"` in both Workout Name and Selected Route fields. | 1. Create Cluster dialog pre-fills clean base name without counter.<br>2. Creating a new cluster updates database and UI with the new cluster identity (`forceIdentity = true`).<br>3. Non-customized workout names automatically adopt the new cluster name; manually typed custom workout names are safely preserved. | Verified |

| **TST-UI-070** | `ATT-238` | **Deletion UI** | `REQ-UI-061` | 1. Long-press a Workout item. 2. Long-press a Route item. 3. Long-press an Equipment item. | The Deletion DropdownMenu appears in the exact same relative position (Top-Left) for all items. | Verified |
| **TST-STR-012** | | **Sub-Task Automation Verification** | `REQ-PRO-007` | 1. Define a requirement and a test case in the docs. 2. The agent should be able to create a sub-task in Jira linked to the main ticket with the test ID and summary. 3. Verify the sub-task is created with the correct parent. | Sub-task is successfully created in Jira with parent linkage. | Verified |
| **TST-STR-013** | `ATT-251` | **String Resource Integrity Check** | `REQ-UI-113` | 1. Audit all `values-*/strings.xml` files. 2. Compare the set of keys in each file with the keys in `values/strings.xml`. 3. Identify keys present in translations but missing in default. 4. Verify that after implementation, no such discrepancies exist. | Every key defined in a translation file exists in the default `strings.xml`. | Proposed |
| **TST-STR-015** | `ATT-486` | **Documentation Audit** | `REQ-PRO-011` | 1. Perform a static audit of the modified source files (e.g., `WorkoutRepository.kt`). 2. Verify that every class has a header describing its purpose. 3. Verify that every method has a header describing what it does and how. | 100% coverage of documentation headers in implemented files. | Verified |
| **TST-STR-014** | `ATT-484` | **Missing Translation Audit** | `REQ-UI-106` | 1. Run a script to identify all translatable keys in default `values/` that are missing in any supported locale (DE, ES, FR, IT, JA, NL, PL, PT). 2. Verify that after implementation, zero translatable keys from the default locale are missing in any supported sub-locale. 3. Verify that specialized resource files (e.g., `strings_devices.xml`, `strings_filters.xml`) are also consistent across locales. | All supported locales have 100% translation coverage of the default translatable keys. | Verified |
| **TST-STR-016** | `ATT-523` | **Global Format String Audit** | `REQ-UI-122` | 1. Execute a comprehensive search across all `strings.xml` files in all locales (DE, ES, FR, IT, JA, NL, PL, PT) for positional format specifiers that are incomplete (e.g., `%1` without `$s`, `$d`, or `$f`). 2. Correct any identified issues. 3. Verify that the final resource set contains only syntactically valid `java.util.Formatter` specifiers. | Zero incomplete or invalid format specifiers remain in the localized resource files. | Proposed |
| **TST-UI-078** | `ATT-565` | **Cluster Naming Dialog Localization Audit** | `REQ-UI-123` | 1. Set app locale to non-English (e.g., German/DE). 2. Trigger a TCX import to launch `ClusterNamingDialog`. 3. Inspect all text elements in the dialog. | All titles, labels, prompts, dropdown options, and buttons in `ClusterNamingDialog` are rendered in the active locale. Zero hardcoded English strings appear. | Verified |

## 5. Performance & Reliability (SWE.5)

| Test ID | Jira ID | Focus | Requirement ID(s) | Procedure | Expected Result | Status |
|:---|:---|:---|:---|:---|:---|:---|
| **TST-PERF-001** | `ATT-319` | **Heatmap Memory Scalability** | `REQ-MAP-017` | 1. Import a large volume of workouts (e.g. 500+). 2. Navigate to the 'Periods' view and scroll through Yearly/Monthly summaries. | The application SHALL NOT crash with an `OutOfMemoryError`. Map rendering MUST remain responsive. | Proposed |
| **TST-PERF-002** | `ATT-362` | **Cluster Detail Loading Speed** | `REQ-PER-001` | 1. Identify a cluster with > 30 recordings. 2. Tap on the cluster to open its details. 3. Measure time from tap to full heatmap/list display. | UI appears and becomes interactive in < 1 second. Heatmap preparation should not block the main thread. | Verified |
| **TST-BUG-001** | `ATT-364` | **Cluster Selection Race Condition** | `REQ-PER-002` | 1. Open Workout Cluster list. 2. Tap Cluster A. 3. Immediately go back and tap Cluster B. 4. Wait for loading to complete. | Only Cluster B's data and map are displayed. Cluster A's data does NOT overwrite Cluster B. | Verified |
| **TST-PERF-003** | `ATT-366` | **Initial Cluster Loading Progress** | `REQ-PER-003` | 1. Clear application cache or significantly modify history. 2. Navigate to 'My Locations'. | 1. A progress card following the 'Migration Status' style appears immediately. 2. It shows granular messages like \"Self-healing hit counts...\" or \"Loading family X of Y...\". | Verified |
| **TST-BUG-002** | `ATT-372` | **Aggregation Gap Stability** | `REQ-PER-001` | 1. Import training history with temporal gaps (e.g. skip a week/month). 2. Trigger a full hierarchical sync. | The aggregation SHALL complete without any 'NoSuchElementException' or 'IllegalArgumentException'. | Verified |
| **TST-PERF-004** | `ATT-374` | **Incremental Period Visibility** | `REQ-PER-001` | 1. Trigger a full hierarchical Periods sync. 2. Observe the list as the "Syncing [Month]..." progress updates. | Each month's period summaries SHALL appear in the UI immediately after its background processing phase completes, before the total migration is finished. | Verified |
| **TST-PERF-005** | `ATT-380` | **Dual-Phase Sync Efficiency** | `REQ-PER-004` | 1. Trigger a full hierarchical Periods sync. 2. Observe the status transitions and UI responsiveness. | 1. The status SHALL transition from 'Reading...' to 'Syncing...' without jumping back. 2. The period list SHALL grow visibly after each 'Syncing [Month]' update. 3. Scrolling SHALL remain smooth throughout the sync. | Verified |
| **TST-PERF-006** | `ATT-383` | **Multi-Phase Progress Feedback** | `REQ-PER-005` | 1. Trigger a Periods sync. 2. Inspect the progress card UI. | 1. A bold title ('Introducing Periods Database...') SHALL be visible. 2. Status messages during the reading phase SHALL be prefixed with 'Phase 1:'. 3. Status messages during the aggregation phase SHALL be prefixed with 'Phase 2:'. | Verified |
| **TST-PERF-007** | `ATT-393` | **Workout Cluster Migration Progress** | `REQ-PER-006` | 1. Trigger a Workout Cluster recalculation. 2. Observe the progress card. | 1. The progress card SHALL show 'Phase 1: Processing routes...' and 'Phase 2: Processing workout X of Y...'. 2. Progress bars SHALL update accurately. | Verified |
| **TST-PERF-008** | `ATT-442` | **Reactive Period Detail Verification** | `REQ-PER-007` | 1. Open the 'Periods' screen. 2. Tap the map icon for a period *immediately* before the background loading completes. 3. Observe the heatmap as it grows. | All workouts belonging to the period SHALL eventually appear on the map as they are loaded from the database. | Verified |
| **TST-PERF-009** | `ATT-441` | **Silent Cluster Refresh** | `REQ-PER-006` | 1. Navigate to My Locations. 2. Navigate away and back. 3. Verify no progress notification on 2nd visit. 4. Trigger recalculation and verify progress DOES appear. | Notification only shown when necessary. | Verified |
| **TST-PERF-010** | `ATT-441` | **Persistent Previews Audit** | `REQ-PER-006` | 1. Navigate to My Locations. 2. Wait for enrichment. 3. Kill and restart app. 4. Navigate back to My Locations. | List appears instantly with previews; NO progress UI shown on restart. | Proposed |
| **TST-PER-009** | `ATT-459` | **Precise Sport Filtering** | `REQ-PER-008` | 1. Open a Period Summary. 2. Click a Sub-Sport row (e.g. MTB). 3. Verify the resulting list only contains MTB workouts for that period. | List is correctly filtered by specific Sport ID. | Proposed |
| **TST-PER-010** | `ATT-460` | **Heatmap Toggle Removal** | `REQ-UI-120` | 1. Open Periods screen and Period Map. 2. Verify `Whatshot` toggle is gone. 3. Verify heatmap still reacts to global settings. | Toggle is removed, global consistency maintained. | Proposed |
| **TST-PER-012** | `ATT-466` | **Period Map Sport Filtering** | `REQ-PER-009` | 1. Open Period Detail Map. 2. Tap a Sport Row (e.g., Bike). 3. Verify map only shows Bike tracks and markers. 4. Verify heatmap only reflects Bike activities. | Map content is correctly filtered by sport selection. | Proposed |
| **TST-PER-013** | `ATT-531` | **Uniform Heatmap & Visual Transparency** | `REQ-PER-010` | 1. Open a Period summary (Month or Year) with > 20 workouts. 2. Verify that ALL workouts across the entire period contribute to the heatmap without geographic truncation. 3. Inspect the heatmap overlay over satellite/terrain maps and verify roads and landmarks remain legible through the translucent gradient. | Heatmap is complete across all workouts and is visually translucent rather than opaque. | Verified |
| **TST-PER-014** | `ATT-532` | **Longest Workout Metrics & Period Header** | `REQ-PER-011` | 1. Open the Period summary list. 2. Verify Period Card headers display duration and workout count. 3. Expand a sport summary row to inspect the Longest Workout highlight. | The Longest Workout highlight displays the workout's name, active duration, total distance, and total ascent. Period headers display total duration and workout count. | Verified |
| **TST-PER-015** | `ATT-536` | **Period Longest Workout Metric Integrity & Cross-Season Week Keys** | `REQ-PER-011` | 1. Execute `PeriodsTemporalKeyTest`. 2. Verify week start calculations across winter/summer DST transitions. 3. Verify real metrics hydrated for weekly summaries in UI without zero fallthrough. | Weekly summaries display real metrics for the longest workout; winter weeks align with workout ISO week sort keys; no empty/zero metric rows appear. | Verified |
| **TST-PER-016** | `ATT-579` | **Multi-Workout Period Longest Workout Selection Integrity** | `REQ-PER-011` | 1. Execute `PeriodsAggregationTest`. 2. Verify that in multi-workout periods (such as January 2015), the workout with the true maximum duration and distance (day 31) is selected as longest workout regardless of stale database IDs. | The period highlight reliably displays the true longest workout among all workouts of the period. | Verified |
| **TST-PER-017** | `ATT-488` | **Period Empty State Placeholder Verification** | `REQ-UI-125` | 1. Open the application with no recorded workouts or filter a sport type with 0 workouts. 2. Navigate to the 'Periods' screen (Woche, Monat, Jahr). 3. Observe the viewport below the period tabs and header when no period summaries exist. 4. Verify that an informative `EmptyStatePlaceholder` is displayed with an appropriate calendar/history icon, clear title ("No period summaries available" / "Keine Zeiträume verfügbar"), and supportive description. 5. Verify proper layout insets (collapsible header offset and bottom navigation bar insets). 6. Verify 9-language localization parity. | The screen renders an EmptyStatePlaceholder component informing the user that no period summaries are available, rather than displaying an empty blank viewport. | Proposed |
| **TST-PER-018** | `ATT-1107` | **Period Map Scalability, Memory Budget & Adaptive Layering Invariant** | `REQ-PER-012` | 1. Verify `AndroidManifest.xml` declares `android:largeHeap="true"`. 2. Execute unit tests verifying that when a period contains > 30 workouts, individual member vector tracks (`memberTracks`) and member markers (`memberMarkers`) are suppressed, while aggregate `heatmapPaths` and `anchorTracks` remain active. 3. Verify that when workouts <= 30 (or filtered by sport to <= 30), member tracks and markers are fully populated. 4. Verify that tapping/peeking any individual session within a high-volume period populates `peekedTracks` with a single focused vector line without background vector explosion. | The period detail map renders smoothly without OOM crashes, ANR freezes, or unbounded polyline/marker allocations across periods of any size. | Verified |
| **TST-PER-019** | `ATT-1151` / `ATT-1221` | **Multi-Region Period Map Viewport Partitioning & Navigation Verification** | `REQ-PER-013` | 1. *Unit Test Single-Region Fast-Path & Invariance*:<br>• Provide a list of workouts within a local geographic territory (e.g. within 30 km to 50 km diagonal span).<br>• Verify `SpatialRegionEngine.detectRegions()` returns exactly 1 region whose bounds match the global coordinate extrema.<br>• Verify camera bounds in `InteractivePeriodMap` and `PeriodSummaryCard` are 100% identical to legacy calculations.<br>2. *Unit Test Multi-Continent Partitioning & Dominant Ranking*:<br>• Provide a period with 100 workouts in Europe (~9°E) and 10 workouts in the USA (~-122°W).<br>• Verify `SpatialRegionEngine.detectRegions()` partitions workouts into exactly 2 distinct regions.<br>• Verify European region is ranked as primary (`isPrimary = true`, index 0) based on workout frequency.<br>• Verify secondary tie-breaking logic (distance, recency) when workout counts are equal.<br>• Verify European bounds tightly enclose European workouts and USA bounds tightly enclose USA workouts, with the North Atlantic Ocean completely excluded.<br>3. *Unit Test Antimeridian Crossing Normalization*:<br>• Provide workouts spanning across the $180^\circ$ meridian (e.g., $+179^\circ$ and $-179^\circ$, with $2^\circ$ geodesic separation).<br>• Verify `SpatialRegionEngine` merges workouts into a single connected region and constructs wrapped `LatLngBounds(southwest, northeast)` where `southwest.longitude > northeast.longitude` without constructing an erroneous $358^\circ$ world-spanning box.<br>4. *Unit Test Fast-Path Boundary Sensitivity*:<br>• Test boundary workouts spanning $45\text{ km}$ diagonal envelope (assert fast-path returns 1 region).<br>• Test workouts separated by $110\text{ km}$ ($> \text{REGION\_CLUSTER\_THRESHOLD\_METERS} = 75\text{ km}$) without intermediate bridging (assert 2 distinct regions are returned).<br>5. *Unit Test Non-GPS & Corrupted Coordinate Resilience*:<br>• Provide workouts with null coordinates, empty polylines, or `(0.0, 0.0)` sentinel coordinates.<br>• Verify engine bypasses non-GPS workouts during clustering without NPE or crash while preserving workout metrics.<br>6. *Unit Test `PeriodsViewModel` State Persistence & Threading*:<br>• Verify `SpatialRegionEngine.detectRegions()` executes off the main thread (`Dispatchers.Default`).<br>• Verify `selectedRegionId` is saved and restored via `SavedStateHandle` across configuration changes.<br>7. *Compose UI Interaction & Overflow Verification*:<br>• Verify `PeriodMapScreen` renders horizontally scrollable chip bar (`Modifier.horizontalScroll(rememberScrollState())`) when `regions.size > 1` and hides the chip bar completely when `regions.size <= 1`.<br>• Verify clicking a region chip updates `selectedRegionId` and animates the camera to that region's bounds.<br>• Verify clicking \"All Regions\" animates camera to global aggregate bounds.<br>8. *Localization Parity Audit*:<br>• Verify all new region chip text strings are defined across all 9 supported locales (EN, DE, ES, FR, IT, JA, NL, PL, PT).<br>9. *Clean-Room Regression*:<br>• Execute `./gradlew testDebugUnitTest` with 0 failures and 0 regressions. | Viewport centers tightly on the athlete's primary training grounds; ocean tiles and Greenland anomaly are eliminated; region switching is seamless and responsive; single-region periods retain 100% parity; all unit and regression tests pass. | Verified |


| **TST-MAP-020** | `ATT-538` | **Detailed Map View Heatmap Visibility** | `REQ-MAP-018` | 1. Open a Workout Cluster heatmap view or a Period Detail map view. 2. Zoom in to a detailed level (zoom 13 to 18) over a route area. 3. Inspect the map overlay. | The translucent density heatmap remains clearly visible over the route area at high zoom levels (13 to 18) and does not vanish or turn blank. | Verified |
| **TST-MAP-021** | `ATT-594` | **Minimum Altitude Marker Spatial Positioning Verification** | `REQ-MAP-019` | 1. Execute `LiveWorkoutSessionTest` to verify that starting without GPS fix anchors `minPos` to the first valid GPS fix at baseline altitude. 2. Simulate multi-sensor live tracking with barometric and GPS altitude; verify that non-best GPS altitude dips do not contaminate `min` or `minPos`. 3. Open a workout map view and verify that `ic_altitude_min` is positioned at the lowest geographic elevation point (e.g. trailhead), matching the minimum point on the elevation chart. | The minimum altitude marker renders at the true geographic minimum point of the activity without being diverted by GPS glitches or unanchored start conditions. | Verified |
| **TST-MAP-022** | `ATT-611` | **Authoritative Max Line Distance & Apex Coordinate Integrity** | `REQ-MAP-020` | 1. Verify `SpeedAndLocationDevice` instantiates `mLineDistanceSensor` as `MySensor<Double>` and does not alter initial value upon `RESET_ACCUMULATORS_INTENT`. 2. Simulate live workout tracking with branching trajectory (short spur vs long return); verify `LiveWorkoutSession` anchors line distance directly to `startLatLng`. 3. Simulate `finalizeLiveSession()` and verify apex coordinate and max line distance are derived deterministically from the recorded polyline geometry. 4. Verify existing workouts with corrupted or missing apex coordinates self-heal to the true maximum displacement point on the track. | The Max Line Distance marker (`ic_distance`) is positioned exactly at the true point of maximum straight-line distance from the start point across all recorded and legacy workouts. | Verified |
| **TST-UI-076** | `ATT-444` | **Period UI Layout Integrity** | `REQ-UI-104` | 1. Open the 'Periods' screen. 2. Inspect the visibility of the Bar Graph. 3. Scroll the period list downwards. | 1. The Bar Graph SHALL be immediately visible below the tabs. 2. The Bar Graph SHALL move up and down in synchronization with the list as the header collapses/expands. | Verified |

## 4. Release Verification Workflow

Whenever a file is modified, the following workflow is triggered:

1.  **Change Detection**: The agent identifies which files were touched.
2.  **Requirement Mapping**: The agent cross-references the files with `docs/requirements.md` (Implementation Column) to identify affected features.
3.  **Test Selection**: The agent selects the relevant **Verification ID** from the requirements table.
4.  **Co-Execution**: The user and agent perform the verification. The agent updates the `Status` column in `docs/requirements.md` upon success.
| **TST-STR-017** | `ATT-523` | **Protocol Integrity Audit** | `REQ-PRO-012` | Verify that the 'Format String Hardening' section is correctly incorporated into 'project_protocol.md'. | New safety standards are formally established and unambiguous. | Proposed |
| **TST-PRO-005** | `ATT-523` | **System Invariant & Impact Protocol Verification** | `REQ-PRO-013` | Verify that 'project_protocol.md' mandates System Invariants, pre-implementation `find_usages` requirement cross-checks, and adversarial self-reviews. | The project protocol incorporates explicit guardrails against side-effect regressions. | Verified |
| **TST-PRO-006** | `ATT-523` | **Dual-Gate Review Protocol Verification** | `REQ-PRO-014` | Verify that 'project_protocol.md' mandates the 3-stage sub-task lifecycle (`[RCA]`, `[Plan]`, `[Impl]`) using the 5-state workflow (`Zu erledigen` -> `In Bearbeitung` -> `In Überprüfung` -> `Freigabe (Human)` -> `Erledigt`), with mandatory human approval gates between stages and Jira comment-based communication. | The 3-stage sub-task review workflow with human release gates is formally established in the engineering documentation. | Verified |
| **TST-PRO-007** | `ATT-422` | **Agent Git Branch, Commit & Merge Protocol Verification** | `REQ-PRO-015` | 1. Verify `docs/project_protocol.md` and `.cursorrules` explicitly mandate agent-driven creation of feature/bugfix branches off `develop` at ticket start.<br>2. Verify protocol defines rules for agent-driven Conventional Commits with asterisk bullet points and ticket references.<br>3. Verify protocol mandates merging the ticket branch into `develop` using non-fast-forward merge (`git merge --no-ff <branch> -m "Merge branch '<branch>' into develop"`) upon completion and human approval.<br>4. Validate repository state demonstrates successful branch creation (`feature/ATT-422`), commit adherence, and merge flow. | Documentation and rules unambiguously define and mandate the complete agent-driven git lifecycle (branching, commits, non-fast-forward merge to `develop`), and repository practice conforms to the specification. | Verified |
| **TST-PRO-008** | `ATT-670` / `ATT-685` | **Automated 5-Stage ASPICE Workflow & Independent Agent 2 Audit Verification** | `REQ-PRO-014` | 1. Verify `docs/project_protocol.md` defines the unified 5-stage ASPICE lifecycle (`Analysis`, `Test Spec`, `Implementation Plan`, `Implementation`, `Test`, `Erledigt`) across all issue types (Bug, Improvement, Feature).<br>2. Verify protocol mandates automated creation of lifecycle subtasks (`[Analysis]`, `[Test-Spec]`, `[Impl-Plan]`, `[Implementation]`, `[Test]`) by Jira Automation upon entering each stage.<br>3. Verify protocol mandates that Agent 2 *always* conducts an automated independent gate audit whenever a sub-task is moved to `In Überprüfung`, posting the audit report as a Jira comment and transitioning to `Freigabe (Human)`.<br>4. Verify protocol enforces deliverable placement in the sub-task's Description (Agent 1) and audit report placement in Comments (Agent 2).<br>5. Verify protocol enforces the strict Human Decision Gate for `Erledigt` and automated parent transition upon sub-task completion.<br>6. Verify `tools/jira_util.py` `show` command displays subtasks.<br>7. Verify `.cursorrules` reflects the unified 5-stage workflow and automated Agent 2 audits.<br>8. Verify `tools/jira_util.py` displays `Lösungsversion` on `show` and `list`, supports `versions` and `set-fixversion`, and warns if missing.<br>9. Verify protocol and `.cursorrules` mandate `Lösungsversion` (*Fix Version/s*) and its audit in Gate 5 prior to closure. | Engineering protocol, AI rules, and tooling define and enforce the unified 5-stage ASPICE lifecycle with automated Jira sub-task generation, automated Agent 2 audits, mandatory Lösungsversion enforcement, and strict human release gates. | Verified |
| **TST-PRO-009** | `ATT-728` / `ATT-941` | **Inviolable ASPICE Human Decision Gate & Anti-Premature Trigger Protocol Verification** | `REQ-PRO-016` | 1. Verify `docs/project_protocol.md` and `.cursorrules` mandate `RequestFeedback: false` on all artifact tool calls.<br>2. Verify protocol explicitly states that synthetic IDE auto-approval messages hold zero authority and grant NO permission to modify code or Jira tickets.<br>3. Verify protocol and rules mandate `python3 tools/jira_util.py check-gate <Subtask>` before modifying any production code in Stage 4.<br>4. Verify `tools/jira_util.py` includes the `check-gate` command that returns exit code 0 when status is `Erledigt` and exit code 1 otherwise.<br>5. Verify `.agents/rules/aspice_governance.md` exists and enforces these rules in Antigravity. | Protocol documentation, AI rules, and Jira utility CLI deterministically prevent premature implementation and enforce the human gate prerequisite before code edits. | Verified |
| **TST-PRO-010** | `ATT-945` / `ATT-1000` | **Sub-Task Self-Sufficiency, Transition Sequencing, Discovery-First, and Git Finalization Verification** | `REQ-PRO-017` | 1. Verify `docs/project_protocol.md` explicitly mandates sub-task self-sufficiency (non-empty Description containing full deliverable, concrete audit comments).<br>2. Verify protocol mandates strict tool execution sequencing (`update-desc` and `comment` BEFORE state transitions).<br>3. Verify protocol mandates discovery-first sub-task handling via `tools/jira_util.py show <Parent>` to eliminate duplicate sub-task creation.<br>4. Verify protocol mandates post-completion git finalization (`git checkout develop && git merge --no-ff`) and integration commenting on the parent ticket.<br>5. Validate that ATT-945 deliverables adhere to the hardened protocol with full documentation in `docs/engineering/Sprint_Review_and_Retro_ATT-945.md`. | Protocol documentation and living engineering standards unambiguously define and enforce sub-task self-sufficiency, documentation-before-transition sequencing, automated sub-task discovery, and mandatory git merge finalization. | Verified |
| **TST-PRO-011** | `ATT-1122` / `ATT-1194` | **Autonomous Multi-Model Independent Review Agent & Automated Gate Audits Verification** | `REQ-PRO-018` | 1. Verify `tools/review_agent.py` exists and implements out-of-process gate auditing using Python 3 standard library (`urllib.request`, `json`, `os`, `sys`, `subprocess`) with zero external pip dependencies.<br>2. Verify provider credential loading from `.env.gemini` (`GEMINI_API_KEY`) and `.env.claude` (`ANTHROPIC_API_KEY`).<br>3. Verify automated gate stage detection based on sub-task summary prefixes (`[Analysis]`, `[Test-Spec]`, `[Impl-Plan]`, `[Implementation]`, `[Test]`).<br>4. Verify automated context assembly: sub-task description, parent ticket details, living documentation (`requirements.md`, `tests.md`), and active git diffs.<br>5. Verify audit report generation adheres to Gate 1-5 checklist criteria from `docs/project_protocol.md` with explicit recommendation (`RECOMMEND PASS`, `CHALLENGED`, `RECOMMEND REVISION`) and Jira Wiki formatting.<br>6. Verify automated comment posting to Jira prefixed with `[Automated comment by AI Agent (External Auditor)]` and transition to `Freigabe (Human)`.<br>7. Verify strict human decision gate guard: attempting to transition to `Erledigt` is blocked.<br>8. Execute automated unit/functional test of `tools/review_agent.py` verifying mock and live execution across providers. | Independent review runner accurately audits deliverables across Gates 1 to 5 using decoupled LLM providers; audit comments and transitions to `Freigabe (Human)` execute seamlessly without human friction; 0 pip dependencies; Human Decision Gate for `Erledigt` strictly preserved. | Verified |
| **TST-PRO-012** | `ATT-1123` / `ATT-1200` | **Multi-Account Jira Authentication, Atomic Resolution & Terminal Error Handling Verification** | `REQ-PRO-019` | 1. Unit Test Credential Configuration Parsing (`.env.jira`): Verify `get_config()` and `resolve_account(role)` parse role-specific keys (`JIRA_AGENT1_USER`, `JIRA_AGENT1_TOKEN`, `JIRA_AGENT2_USER`, `JIRA_AGENT2_TOKEN`, `JIRA_COORDINATOR_USER`, `JIRA_COORDINATOR_TOKEN`) alongside base `JIRA_USER`, `JIRA_TOKEN`.<br>2. Unit Test Atomic Pair Resolution & Fallback: Verify that when both role user and token are set, the role credentials are used; verify that when only one is set (e.g. user set, token empty), resolution falls back atomically to `(JIRA_USER, JIRA_TOKEN)` and emits notice to `stderr`; verify that when no role keys exist, resolution cleanly defaults to base credentials.<br>3. Unit Test Terminal Error on Missing Credentials: Mock configuration with no credentials defined; verify `get_headers()` or API request halts immediately with exit code 1 and writes diagnostic error to `stderr` without making unauthenticated requests.<br>4. Unit Test Basic Auth Header Generation: Verify `get_headers(role)` produces base64 Basic Auth headers matching the resolved credentials.<br>5. Backward Compatibility Invariant: Verify that single-account setups run without warnings or errors.<br>6. Standard Library Invariant: Verify all unit tests and tooling run with zero pip dependencies on Python 3 standard library. | Jira utility and review agent correctly authenticate under distinct agent accounts; atomic pair fallback prevents mismatched credential errors; unconfigured environments fail deterministically. | Verified |
| **TST-PRO-013** | `ATT-1123` / `ATT-1200` | **Role Locking, Precedence, Anti-Spoofing & Actor Comment Attribution Verification** | `REQ-PRO-020` | 1. Unit Test Precedence Resolution: Verify role selection follows `CLI Flag (--as <role>)` > `Environment Variable (JIRA_ACTOR)` > `Script Default`.<br>2. Unit Test Role Hardwiring in `review_agent.py`: Verify that `review_agent.py` hardwires role to `agent2` across all API calls and comments, rejecting or ignoring any attempt to run review audits as `agent1` or `coordinator`.<br>3. Unit Test Invalid Role Rejection: Verify `jira_util.py --as invalid_role` exits with error code 1 and descriptive usage message.<br>4. Unit Test Comment Prefix Attribution: Verify `add_comment` formats opening line with role-specific attribution prefix: `[Automated comment by AI Agent 1 (Implementer)]` for `agent1`, `[Automated comment by AI Agent 2 (Auditor)]` for `agent2`, and `[Automated comment by AI Coordinator]` for `coordinator`.<br>5. Unit Test Human Gate Invariance: Verify that attempting `transition_issue(key, "erledigt")` or `transition_issue(key, "freigabe erteilt")` is blocked with exit code 1 specifically for transitions targeting Erledigt / Done, while non-Erledigt transitions (`in_progress`, `in_review`, `freigabe`) succeed.<br>6. Automated Protocol Inspection: Programmatic test verifies `docs/project_protocol.md` and `.cursorrules` explicitly define the Coordinator role, specify Coordinator boundaries (cannot override Auditor verdicts, cannot skip gates, cannot close tickets to `Erledigt`). | Role precedence is deterministic; Agent 2 review runner cannot be spoofed by Agent 1; Coordinator role boundaries and human-only `Erledigt` transitions are technically and procedurally enforced; comment attribution accurately identifies the acting role. | Verified |
| **TST-PRO-014** | `ATT-1123` / `ATT-1200` | **Jira Credential & Token Secret Masking Verification** | `REQ-PRO-021` | 1. Unit Test Output Sanitization: Execute mock CLI invocations with valid, fallback, and invalid configurations; capture `sys.stdout` and `sys.stderr`; assert that raw token strings and Basic Auth base64 payload strings never appear in the output.<br>2. Unit Test Advisory Notice Scrubbing: Verify fallback notices printed to `stderr` reference only role names or usernames and contain zero token substrings.<br>3. Unit Test Exception Masking: Simulate HTTP errors (e.g. 401 Unauthorized); verify error outputs printed to console do not echo request authorization headers or secret tokens. | Console and error streams never expose Jira tokens or credentials under any execution or error condition. | Verified |
| **TST-PRO-015** | `ATT-1141` / `ATT-1205` | **Requirement Archaeology & Chesterton's Fence Governance Verification** | `REQ-PRO-022` | 1. *Unit Test Template & Diff Parser (`tools/verify_requirement_governance.py`)*:<br>• *Scenario 1 (Valid Full Archaeology)*: Given a deliverable proposing edits to an existing requirement with all 4 mandatory fields populated (`Original Requirement ID & Target`, `Historical Origin & Commit Trace`, `Root Reason for Existing Formulation`, `Preservation of Core Invariants`), verify the tool exits with code 0.<br>• *Scenario 2 (Missing or Empty Mandatory Field)*: Given a deliverable modifying an existing requirement where one or more fields are omitted or blank, verify the tool exits with code 1 and outputs structured schema error diagnostics to stderr.<br>• *Scenario 3 (Net-New Requirement Invariance)*: Given git diff containing only added requirement lines with a novel ID (absent from base), verify the tool identifies it as net-new and exits with code 0 without requiring archaeology.<br>• *Scenario 4 (Diff Detection against Base)*: Given `git diff develop..HEAD docs/requirements.md`, verify the script accurately parses modified requirement lines versus added/deleted lines using git hunk analysis.<br>2. *Gate 2 Auditor Model Verification (`tools/review_agent.py`)*:<br>• Verify `GATE_DEFINITIONS["Gate 2"]["checklist"]` includes explicit item: "Requirement Archaeology (Chesterton's Fence): If existing requirements are modified in `docs/requirements.md`, verify presence and validity of the 4-field archaeology section."<br>• Verify review prompt instructs the model to issue `CHALLENGED` or `RECOMMEND REVISION` when archaeology is omitted on modified requirements.<br>3. *Living Protocol & Rules Inspection*:<br>• Verify `docs/project_protocol.md` and `.cursorrules` explicitly document the Chesterton's Fence principle, the 4-field section schema, and the requirement archaeology hurdle.<br>4. *Standard Library Footprint Invariant*:<br>• Verify `tools/verify_requirement_governance.py` imports only Python 3 standard library modules (`os`, `sys`, `re`, `subprocess`, `unittest`) with zero external pip dependencies. | Machine-checkable validation and Gate 2 audit rules prevent unverified modification of existing requirements; net-new requirements are unaffected; zero external dependencies. | Verified |
| **TST-PRO-016** | `ATT-1125` / `ATT-1210` | **Android Test Fixtures & Jira Metadata Auto-Inheritance Verification** | `REQ-PRO-023` | 1. *Unit Test `MockCursorFactory` Fidelity (`MockCursorFactoryTest.kt`)*:<br>• *Empty Cursor*: Verify cursor initialized with empty row list has `count == 0`, `moveToFirst() == false`, and `isAfterLast == true`.<br>• *Column Resolution*: Verify `getColumnIndex` and `getColumnIndexOrThrow` return correct 0-based indices for defined columns and -1 / `IllegalArgumentException` for unknown columns.<br>• *Row Traversal*: Given a 3-row dataset, verify sequential `moveToNext()` traverses row 0, 1, 2 accurately, and returns false when exhausting rows.<br>• *Data Type Extraction*: Verify `getInt`, `getLong`, `getDouble`, `getFloat`, `getString`, and `getBlob` return expected casted values for heterogeneous cell values.<br>• *Nullability Assertions*: Verify `isNull` returns true for null cell values and false for non-null values.<br>2. *Integration / Regression Verification in Existing Tests*:<br>• Refactor a sample database test (e.g. in `EquipmentDbHelperRetiredTest.kt`) to utilize `MockCursorFactory.create(...)`; verify test passes cleanly with identical assertions and dramatically reduced boilerplate.<br>3. *CLI Unit Tests for Jira Metadata & Sprint Auto-Inheritance (`tools/test_jira_accounts.py`)*:<br>• Verify `create_subtask` and `create_issue` accept and process `--add-to-sprint` and `--fixversion` flags.<br>• Mock Jira Agile board sprint query; verify sub-task creation with `--add-to-sprint` attaches newly created issue key to active sprint.<br>4. *Living Protocol & Sandbox Documentation Inspection*:<br>• Verify `docs/project_protocol.md` and `.cursorrules` explicitly document filesystem boundaries for Gradle Daemon caches (`~/.gradle`, `~/.android`) and when to execute with elevated permissions (`BypassSandbox: true`). | `MockCursorFactory` cleanly substitutes complex MockK chains for Android SQLite Cursors; Jira CLI handles sprint auto-linking and version propagation without manual steps; zero APK runtime modifications. | Verified |
| **TST-STB-001** | `ATT-621` | **Resilient WorkManager Initialization & Startup Protection Verification** | `REQ-STB-001` | 1. Verify `AndroidManifest.xml` configures `androidx.startup.InitializationProvider` with `<meta-data android:name="androidx.work.WorkManagerInitializer" tools:node="remove" />`.<br>2. Verify `TrainingApplication.onCreate()` encapsulates `WorkManager.initialize(...)` within a `try ... catch (Throwable)` block that logs warnings on failure.<br>3. Verify `ExportManager.java` and `BackupWorker.kt` guard `WorkManager.getInstance()` invocations against `IllegalStateException` / `Throwable`.<br>4. Verify unit test confirms safe error handling when WorkManager is uninitialized.<br>5. Run full test suite (`./gradlew testDebugUnitTest`) to ensure zero regression. | WorkManager auto-initialization is decoupled from ContentProvider startup; manual initialization safely catches framework errors; background worker callers fail gracefully if WorkManager is unavailable. | Verified |
| **TST-STB-002** | `ATT-622` | **TrackerService Foreground Service Resilience Verification** | `REQ-STB-002` | 1. Verify `TrackerService.onStartCommand` returns `Service.START_STICKY`.<br>2. Verify that when `intent == null` and `hasBackgroundLocationPermission()` is false, `startForeground` is not called with location type, `stopSelf()` is invoked, and workout state is preserved.<br>3. Verify that all `startForeground` calls are wrapped in `try ... catch (SecurityException | IllegalStateException)`.<br>4. Create and run unit tests verifying safe handling of `null` intent restarts without background location permission and graceful recovery when `startForeground` throws `SecurityException`.<br>5. Run full test suite (`./gradlew testDebugUnitTest`) to ensure zero regression. | `START_STICKY` is retained; `TrackerService` safely handles OS background restarts without crashing; `SecurityException` is caught gracefully. | Verified |
| **TST-STB-003** | `ATT-635` | **Interrupted Workout Resumption & Recovery Verification** | `REQ-STB-003` | 1. Verify `TrackerService.showTrackingInterruptedNotification()` attaches `EXTRA_RESUME_INTERRUPTED_WORKOUT = true` and `SELECTED_FRAGMENT = START_OR_TRACKING` to its `PendingIntent`.<br>2. Verify `MainActivityWithNavigation.handleIntent()` detects the resumption extra, sets `cResumeFromCrash = true`, cancels the notification, and resumes tracking via `REQUEST_START_TRACKING`.<br>3. Verify `WorkoutSummariesDatabaseManager.hasUnfinishedWorkout()` accurately identifies unfinished workouts (`FINISHED == 0`).<br>4. Verify app launch prompts `StartOrResumeDialog` when an unfinished workout exists.<br>5. Execute unit tests verifying Intent extras, database detection, and resume flag handling.<br>6. Perform on-device validation on Pixel 10 (Android 16): start tracking, kill process via `run-as`, tap interrupted notification, confirm workout resumes in foreground without data loss.<br>7. Run full test suite (`./gradlew testDebugUnitTest`) to ensure zero regression. | Interrupted notification cleanly resumes tracking in foreground; unfinalized workouts prompt user for resumption; zero crashes or data loss. | Verified |
| **TST-STB-004** | `ATT-623` | **Location Provider Availability & Exception Shielding Verification** | `REQ-STB-004` | 1. Verify `SpeedAndLocationDevice_GPS` checks `mLocationManager.getProvider(LocationManager.GPS_PROVIDER) != null` before requesting location updates.<br>2. Verify all `LocationManager.requestLocationUpdates()` calls in `SpeedAndLocationDevice_GPS` and `SpeedAndLocationDevice_Network` are wrapped in `try ... catch (IllegalArgumentException | SecurityException)`.<br>3. Verify that when GPS or Network provider is missing or throws `IllegalArgumentException: provider does not exist`, the device calls `LocationUnavailable()` and does not crash.<br>4. Verify `DeviceManager` checks provider availability and isolates device initialization failures.<br>5. Execute unit tests in `SpeedAndLocationDeviceResilienceTest.kt` verifying missing provider and exception handling.<br>6. Run full regression test suite (`./gradlew testDebugUnitTest`) to ensure zero regression. | `BANALService` initializes cleanly without crashing on devices lacking GPS or Network location providers; standard location tracking is unaffected. | Verified |
| **TST-STB-005** | `ATT-644` | **Equipment Database Boundary Validation & Error Log Shielding Verification** | `REQ-STB-005` | 1. Verify `EquipmentDbHelper.getEquipmentNameFromId()` returns `null` immediately for non-positive IDs (`0`, `-1`, `-100`) without querying SQLite or calling `Log.e`.<br>2. Verify `EquipmentDbHelper.getStravaIdFromId()` returns `null` immediately for non-positive IDs without querying SQLite or calling `Log.e`.<br>3. Verify `EquipmentDbHelper.getDeviceIdsForEquipment()` returns an empty list for non-positive IDs without database queries.<br>4. Verify valid positive IDs (`> 0`) resolve equipment names and Strava IDs accurately.<br>5. Verify non-existent positive IDs (`> 0`) continue to log diagnostic `ERROR` messages.<br>6. Verify `WorkoutDataMapper`, `EquipmentDataProvider`, `StravaUploader`, `EquipmentAndSportTypeDiscoveryManager`, and `SportTypeViewModel` short-circuit lookups when `equipmentId <= 0`.<br>7. Execute unit tests in `EquipmentDbHelperResilienceTest.kt`.<br>8. Run full regression test suite (`./gradlew testDebugUnitTest`) to ensure zero regression. | Equipment lookups for unassigned equipment (IDs <= 0 or SQL NULL) execute with zero database queries and zero Logcat error logs; valid equipment resolution is preserved. | Verified |
| **TST-STB-006** | `ATT-1036`, `ATT-1086` | **Navigation Drawer Header Background Resource Availability & Crash Immunity Verification** | `REQ-STB-006` | 1. *Resource File Existence Verification*:<br>• Verify that `app/src/main/res/drawable-nodpi/menu_header_background.jpg` exists in the codebase.<br>• Verify that the resource can be resolved across all screen densities without falling into a missing-resource state.<br>2. *Defensive Fallback & Unit Test Verification (`AppNavigationDrawerTest.kt`)*:<br>• Verify that `DrawerHeader()` encapsulates background painter resolution in safe defensive error handling.<br>• Verify via automated unit test that when resource resolution throws `Resources$NotFoundException`, `DrawerHeader()` does NOT propagate the fatal exception and renders fallback UI cleanly.<br>3. *Clean-Room Regression Verification*:<br>• Run full test suite (`./gradlew testDebugUnitTest`) to ensure zero regressions across all modules.<br>4. *On-Device / Density Verification*:<br>• Deploy debug APK to connected device (`./gradlew installDebug`).<br>• Verify navigation drawer opens cleanly with landscape header background.<br>• Adjust system display size / text zoom setting and verify drawer continues to display without crashing. | Universal `menu_header_background.jpg` exists in `drawable-nodpi/`; `DrawerHeader()` is protected against `Resources$NotFoundException`; 100% of tests pass with zero regressions. | Verified |
| **TST-STB-007** | `ATT-1037`, `ATT-1092` | **AndroidX Core 1.15.0 Alignment & Insets Crash Immunity Verification** | `REQ-STB-007` | 1. *Dependency Graph Verification*:<br>• Run `./gradlew :app:dependencyInsight --configuration debugRuntimeClasspath --dependency androidx.core:core` and verify that the resolved version across all configurations is strictly `1.15.0`.<br>• Run `./gradlew :app:dependencyInsight --configuration debugRuntimeClasspath --dependency androidx.core:core-ktx` and verify that the resolved version is strictly `1.15.0`.<br>2. *Automated Unit & Compatibility Verification*:<br>• Create automated unit test `CoreDependencyAlignmentTest.kt` verifying that `WindowInsetsCompat` does not invoke missing platform methods on API 34 configurations.<br>3. *Clean-Room Regression Verification*:<br>• Run full test suite (`./gradlew testDebugUnitTest`) to ensure 100% of existing unit tests pass without regressions under `androidx.core:1.15.0`.<br>4. *On-Device / Compose Verification*:<br>• Deploy debug APK to connected device (`./gradlew installDebug`).<br>• Verify application startup, Compose navigation drawer, and bottom popups render properly without insets crashes. | `androidx.core` dependencies resolve strictly to `1.15.0`; 100% of automated unit tests pass; on-device Compose navigation renders flawlessly. | Verified |
| **TST-STR-018** | `ATT-525` | **Global Translation Parity, Array Integrity & Formatter Audit** | `REQ-LOC-001` | 1. Verify all 9 supported locales possess 100% of translatable string keys (0 missing keys, 0 orphaned keys).<br>2. Verify all localized format strings adhere strictly to `REQ-UI-122` positional specifier syntax (`%1$s`), with 100% parity in specifier count, indices, and conversion types against default English resources.<br>3. Verify all localized string-arrays have identical item counts to base `values/arrays.xml`.<br>4. Verify `filter_max` is defined in `strings_filters.xml` across all locales and removed from `arrays.xml` and `strings.xml`.<br>5. Verify `devices_sensors_title` is defined in `strings_devices.xml` in Spanish.<br>6. Verify `backup_interval_values` has `translatable="false"` and obsolete `Basic_Sport_Types_values` is removed.<br>7. Execute automated tests in `TranslationParityTest.kt`.<br>8. Run full regression test suite (`./gradlew testDebugUnitTest`) to ensure zero regression. | 100% translation key parity, string-array integrity, and format string safety verified across all supported global locales. | Verified |
| **TST-UI-079** | `ATT-508` | **Elevation Profile Chart Bounds Sanitization & Strict Containment Verification** | `REQ-UI-126` | 1. Test `ElevationProfile` with flat coastal elevation points (~0 m to 3 m) and corrupted override bounds (`minAltitudeOverride = -63.0`, `maxAltitudeOverride = 60.0`); verify bounds clamp to stream envelope with 20m aesthetic span.<br>2. Test `ElevationProfile` with workout points dipping to 360 m and override `minAltitudeOverride = 368.0` (or 364.0); verify `bounds.min` is clamped to <= 360 m, guaranteeing that NO point on the profile curve renders below the chart bottom bound.<br>3. Test with valid mountainous route; verify that full dynamic elevation range is preserved.<br>4. Run unit tests in `ElevationProfileBoundsTest.kt`. | Unplausible override bounds are sanitized to match stream envelope; chart bounds strictly envelope all rendered points without clipping; mountainous profiles retain full dynamic range. | Verified |
| **TST-DAT-003** | `ATT-508` | **Authoritative Altitude Extrema Reconciliation & Self-Healing Verification** | `REQ-DAT-009` | 1. Construct a workout summary record with decoded altitude stream (e.g., [0.0, 1.0, 2.0, 1.5, 0.5]) and corrupted SQLite extrema (Min = -63.0, Max = 60.0).<br>2. Construct a workout summary where stored min is higher than stream min (e.g. stored min = 368.0, stream min = 360.0); verify `reconcileAltitudeExtrema` reconciles min to 360.0 and updates SQLite.<br>3. Construct a workout summary where stored max is lower than stream max (e.g. stored max = 375.0, stream max = 385.0); verify `reconcileAltitudeExtrema` reconciles max to 385.0 and updates SQLite.<br>4. Verify legitimate workouts matching their stream are not altered.<br>5. Execute unit tests in `WorkoutDataMapperAltitudeTest.kt`. | Corrupted legacy extrema (both rogue spikes and extrema falling inside stream bounds) are automatically healed from the authoritative stream, satisfying `REQ-DAT-009` and restoring consistency to both summary table and chart. | Verified |
| **TST-CON-002** | `ATT-508` | **Barometric Cold-Start Baseline Protection Audit** | `REQ-CON-011` | 1. Verify `AltitudeFromPressureDevice` does not publish uncalibrated standard atmosphere altitude (using 1013.25 hPa assumption) to session listeners or running statistics before baseline calibration.<br>2. Simulate cold start with sea level pressure = 1020.8 hPa (raw altitude = -63.0 m); verify that -63 m is suppressed from session extrema.<br>3. Execute unit tests in `AltitudeFromPressureDeviceTest.kt`. | Cold-start uncalibrated pressure readings cannot contaminate session extrema before reference anchor or calibration is established. | Verified |
| **TST-DAT-004** | `ATT-296` | **Bulk Workout Deletion & Relational Storage Purge Verification** | `REQ-DAT-010` | 1. Insert mock workouts into SQLite: Workout A (400 days old), Workout B (370 days old), and Workout C (30 days old) with associated laps, extrema, and sample tables (`workout_samples_<fileBaseName>`).<br>2. Execute `WorkoutDeletionHelper.deleteOldWorkouts(daysToKeep = 365, callback)`.<br>3. Verify `getBaseFileName` is queried prior to summary deletion so `workout_samples_<fileBaseName>` tables for Workouts A and B are dropped.<br>4. Verify all rows in `WorkoutSummaries`, `accumulated_sensors`, `extrema_values`, and `laps` for Workouts A and B are completely deleted.<br>5. Verify Workout C (30 days old) is completely preserved with its sample table intact.<br>6. Verify progress callback is triggered for each deleted workout ID.<br>7. Verify `WorkoutRepository.deleteOldWorkouts` invokes `PeriodsRepository.resyncAllPeriods()` and `WorkoutClusterRepository.refreshClusters(forceCheckIntegrity = true)` post-deletion.<br>8. Verify surviving workout clusters keep their lifetime `hitCount` untouched, while unlinked zero-workout orphan clusters are purged.<br>9. Execute automated unit tests in `WorkoutDeletionHelperTest.kt` and `WorkoutClusterRepositoryTest.kt`. | Sessions older than threshold are fully purged across all databases without leaving orphaned sample tables; surviving sessions and cluster hit counts are preserved. | Verified |
| **TST-UI-080** | `ATT-296` | **Delete Old Workouts Compose Dialog & Action Trigger Verification** | `REQ-UI-127` | 1. Verify `defaultDaysToKeep` string resource evaluates to `"365"`.<br>2. Verify `WorkoutListActions` renders the delete sweep action button (`ic_baseline_delete_sweep_24`) with localized content description.<br>3. Verify clicking the action button triggers `DeleteOldWorkoutsDialog`.<br>4. Verify dialog pre-fills input field with `"365"`.<br>5. Verify entering empty string, non-numeric characters, or negative numbers disables the confirm button.<br>6. Verify entering valid integer (e.g. 180) enables the confirm button.<br>7. Verify confirming dispatches `executeDeleteOldWorkouts(180)` and closes dialog.<br>8. Verify dismissing closes dialog without triggering deletion.<br>9. Execute automated tests. | Action trigger and Compose dialog operate seamlessly with input validation and default 365 days retention. | Verified |
| **TST-DAT-005** | `ATT-296` | **Period Cache Synchronization on Bulk Deletion Verification** | `REQ-DAT-011` | 1. Populate `WorkoutSummaries` and pre-populate `PeriodSummaries.db` with periods spanning beyond 365 days.<br>2. Execute `WorkoutRepository.deleteOldWorkouts(daysToKeep = 365)`.<br>3. Verify `PeriodsRepository.resyncAllPeriods()` is executed.<br>4. Verify `PeriodSummaries.db` contains 0 records for periods whose underlying workouts were completely purged.<br>5. Verify boundary periods have updated metrics matching surviving workouts.<br>6. Verify all `longest_workout_id` values point to surviving workouts or are `-1`.<br>7. Execute automated unit tests. | Period cache completely purges orphaned historical periods, recomputes boundary periods, and eliminates dangling foreign keys upon bulk deletion. | Verified |
| **TST-UI-081** | `ATT-705` | **Bulk Workout Deletion Progress Feedback & Notification Verification** | `REQ-UI-128` | 1. Trigger bulk deletion with $N$ old workouts.<br>2. Verify `WorkoutTabsScreen` renders modal `WorkoutDeletionProgressDialog`.<br>3. Verify Phase 1 (Deletion): Dialog displays localized title (`@string/deleteOldWorkouts`), formatted session name (`@string/deleting_workout`), numeric counter ($X / Y$), and `LinearProgressIndicator` tracking fractional completion.<br>4. Verify dialog is modal and non-dismissible (`dismissOnBackPress = false`, `dismissOnClickOutside = false`).<br>5. Verify Phase 2 (Resynchronization): Dialog transitions to indeterminate `CircularProgressIndicator` and status message (`@string/please_wait`) during period cache and cluster resync.<br>6. Verify ongoing system notification (`NotificationCompat.Builder`) is posted on channel `NOTIFICATION_CHANNEL__EXPORT` with `CATEGORY_PROGRESS` and `setOngoing(true)` tracking progress $X / Y$, and is cancelled when operation concludes.<br>7. Verify upon operation completion or error, `deletionProgress` resets to `Idle`, the dialog dismisses automatically, and the notification is cleared.<br>8. Execute automated unit tests. | In-app modal dialog and system notification provide continuous, accurate progress updates during deletion and analytical resynchronization, automatically dismissing upon completion. | Verified |
| **TST-UI-082** | `ATT-704` | **Bulk Workout Deletion Two-Step Confirmation Verification** | `REQ-UI-129` | 1. Open `DeleteOldWorkoutsDialog` from the workouts header action.<br>2. Input a valid retention threshold (e.g. 180 days).<br>3. Verify clicking the proceed action in `DeleteOldWorkoutsDialog` dismisses the input dialog and displays the confirmation dialog (`DeleteConfirmationDialog`) with title `@string/deleteOldWorkouts` and formatted warning message (`@string/really_delete_old_workouts_format`) specifying 180 days.<br>4. Verify that tapping "Cancel", tapping outside, or executing the system back gesture dismisses the confirmation dialog without invoking deletion or altering the database.<br>5. Verify that while the confirmation dialog is displayed, no database write or deletion progress notification is triggered.<br>6. Verify that tapping "Delete" (`@string/delete`) in the confirmation dialog confirms the operation, dismisses the confirmation dialog, dispatches `executeDeleteOldWorkouts(180)`, and initiates deletion with progress feedback (`REQ-UI-128`).<br>7. Verify string formatting and localization parity across all 9 supported application locales.<br>8. Execute automated unit tests. | Bulk workout deletion requires explicit, two-step user confirmation; cancellation cleanly aborts without mutations, while confirmation accurately triggers deletion with the specified retention parameter. | Verified |
| **TST-UI-083** | `ATT-715` | **Sport-Specific Equipment Selection & In-Place Expansion Verification** | `REQ-UI-130` | 1. Configure mock `EquipmentRepository` with bicycles (`BSportType.BIKE`) and shoes (`BSportType.RUN`).<br>2. Case 1 ($N > 1$): Configure cycling sport with 2 linked bikes. Verify dropdown presents `[- none -, <linked bike 1>, <linked bike 2>, "+ all bikes +"]` and zero shoes.<br>3. In-Place Expansion: Select `"+ all bikes +"`; verify list expands in-place to `[- none -, <all bikes...>]` with `stayOpenOn` preventing premature dropdown closure.<br>4. Case 2 ($N == 1$): Configure running sport with 1 linked shoe. Verify dropdown presents `[- none -, <all shoes...>]` and the uniquely linked shoe is automatically preselected.<br>5. Case 3 ($N == 0$): Configure cycling sport with 0 linked bikes. Verify dropdown presents `[- none -, <all bikes...>]` and `- none -` is preselected.<br>6. Cross-Sport Switching (Different Base Sport): Switch from Cycling (with bike selected) to Running. Verify selected equipment clears / switches to linked shoe ($N == 1$) or `- none -` ($N == 0$) and options refresh strictly to shoes.<br>7. Cross-Sport Switching (Same Base Sport): Switch between cycling sports; verify valid selected bike is retained.<br>8. Other / Unknown Sport (`BSportType.UNKNOWN`): Verify dropdown presents only `[- none -]` (plus explicit links).<br>9. Execute automated unit tests in `EditWorkoutEquipmentTest.kt`. | Equipment dropdown strictly enforces base sport partitioning; 3-case hierarchy ($N > 1$, $N == 1$, $N == 0$) and in-place expansion operate as specified; cross-sport gear pollution is completely eliminated. | Verified |
| **TST-UI-084** | `ATT-716` | **Compact Strava Segment Efforts Presentation & Expandable Accordion Verification** | `REQ-UI-131` | 1. Unit Test Strava Parsing: Verify `StravaActivityParser` accurately parses `isStarred`, `starred_date`, `prRank`, and `komRank` from Strava activity JSON payloads (including nested `segment` object, flat fields, and `achievements` array).<br>2. Case A (Small Segment Count $\le 4$): Provide activity with $\le 4$ segments (e.g. 3 segments). Verify all 3 segments are rendered directly and NO expand/collapse button is displayed.<br>3. Case B (All Highlights $N == N_{\text{highlight}}$): Provide activity with $> 4$ segments (e.g. 6 segments) where all 6 have PR or Starred status. Verify all 6 are rendered directly and NO expand/collapse button is displayed.<br>4. Case C (Collapse Eligible $N > 4$ and $N > N_{\text{highlight}}$): Provide activity with 10 segments where 3 are PRs and 1 is starred (4 highlights, 6 standard). Verify by default only the 4 highlighted segments are displayed, followed by `"▼ Show all 10 segments (+6 more)"` and in-place expansion toggles to all 10.<br>5. Case D (Zero Highlights $N > 4, N_{\text{highlight}} == 0$): Provide activity with 7 segments where 0 are PRs or starred. Verify in collapsed view zero segment rows and `@string/strava_no_highlights` are displayed, followed by `"▼ Show all 7 segments (+7 more)"`, and tapping expansion reveals all 7.<br>6. In-Place Collapse: Trigger click on collapse button. Verify list collapses back to the highlighted state.<br>7. Starred Segment Matching: Verify starred segments are matched by Strava ID or segment name against `SegmentsDatabaseManager`.<br>8. Localization Parity: Verify action button labels and status notes are correctly formatted across all 9 supported application locales (EN, DE, ES, FR, IT, JA, NL, PL, PT).<br>9. Invariants: Verify Best Efforts, external Strava activity click launch, and Strava branding are fully preserved.<br>10. Automated Unit & UI Tests: Execute automated test suite. | Segment efforts in workout summary automatically collapse to highlights when $> 4$ segments with non-highlights exist; in-place accordion expands and collapses smoothly; card bloat is prevented. | Verified |
| **TST-UI-085** | `ATT-128`, `ATT-742` | **Multi-Dimensional Workout Filtering, Modal Sheet & Persistence Verification** | `REQ-UI-132` | 1. Unit Test Predicate Evaluation: Verify `WorkoutFilterCriteria.matches()` across all dimensions: text query, date range, year, specific `sportTypeId`, specific `equipmentId`, commute flag, trainer flag, GPS track flag, and distance/duration thresholds.<br>2. Action Trigger & Funnel Icon: Verify `WorkoutListActions` renders the Material funnel icon (`Icons.Default.FilterAlt`) with localized content description and badge counter when active.<br>3. Modal Filter Bottom Sheet Layout: Verify tapping the filter icon opens `WorkoutFilterBottomSheet` displaying a dedicated top header with title and dismiss button (`Icons.Default.Close`), proper bottom anchoring with zero gap artifacts, and a dedicated bottom action row for "Reset all" and "Apply".<br>4. Active Filter Chips: Verify applying filters displays horizontal scrolling chip strip above the workout list with localized labels and trailing `✕` icons. Verify tapping `✕` removes that specific filter dimension reactively.<br>5. Clear All / Reset: Verify tapping "Reset all" in the sheet or removing all chips resets criteria to default, hides chips and badge, and restores full list.<br>6. Main View Navigation Reset: Verify navigating away from the workouts section back to the main view clears active filter criteria, so subsequent visits start with the full unfiltered list.<br>7. Invariants: Verify sorting orders (`DATE`, `DURATION`, `DISTANCE`, `ELEVATION_GAIN`), view mode toggle (Compact/Detailed), sport tabs (All, Bike, Run, Other), and deletion workflows remain fully functional without regression.<br>8. Localization Parity: Verify all filter category labels, action buttons, chip descriptors, and empty-state filter prompts are localized across all 9 supported locales (EN, DE, ES, FR, IT, JA, NL, PL, PT).<br>9. Automated Unit Tests: Execute automated test suite. | Multi-dimensional workout filtering filters sessions accurately across all criteria; funnel icon and active badge render cleanly; bottom sheet layout is spacious and anchored flush; criteria reset when returning to main view; all existing list behaviors and invariants remain intact. | Verified |
| **TST-UI-086** | `ATT-736` | **Multi-Dimensional Route Filtering, Shared Modal Sheet & Persistence Verification** | `REQ-UI-133` | 1. Unit Test Predicate Evaluation: Verify `RouteFilterCriteria.matches()` across all dimensions: text query (name/description substring), route origin (`RouteSource`), map selection (`isSelected`), distance thresholds, and elevation gain thresholds.<br>2. Action Trigger & Funnel Icon: Verify `RouteTabbedScreen` renders the Material funnel icon (`Icons.Default.FilterAlt`) at the end of the action row (`Select All` $\rightarrow$ `Import` $\rightarrow$ `Sync` $\rightarrow$ `Sort` $\rightarrow$ `Filter`) with badge counter when filters are active.<br>3. Shared Modal Bottom Sheet Layout: Verify tapping the filter icon opens `RouteFilterBottomSheet` built on shared `FilterBottomSheetScaffold`, displaying a dedicated top header with title and dismiss button (`Icons.Default.Close`), zero bottom gap artifacts, filter sections (Query, Source, Selected, Distance, Elevation), and a dedicated bottom action row for "Reset all" and "OK".<br>4. Active Filter Chips: Verify applying route filters displays horizontal scrolling chip strip above the route list with localized labels and trailing `✕` icons using shared `RemovableFilterChip`. Verify tapping `✕` removes that specific filter dimension reactively.<br>5. Clear All / Reset: Verify tapping "Reset all" in the sheet or removing all chips resets criteria to default, hides chips and badge, and restores the full route list.<br>6. Main View Navigation Reset: Verify navigating away from the routes section back to the main view (`drawer_start_tracking`) or switching drawers clears active route filter criteria, so subsequent visits start with the full unfiltered list.<br>7. Code Sharing Invariant: Verify workout filtering (`REQ-UI-132` / `TST-UI-085`) continues to function without visual or functional regressions after adopting the shared `FilterBottomSheetScaffold` and `RemovableFilterChip`.<br>8. Route Invariants: Verify sorting orders (`DISTANCE_TO_USER`, `TOTAL_ELEVATION_GAIN`, `ROUTE_DISTANCE`, `NAME`), sport tabs (All, Bike, Run, Unknown), GPX import, Strava sync, and route editing remain fully operational.<br>9. Localization Parity: Verify all route filter category labels, action buttons, chip descriptors, and empty-state filter prompts are localized across all 9 supported locales (EN, DE, ES, FR, IT, JA, NL, PL, PT).<br>10. Automated Unit Tests: Execute automated test suite (`RouteFilterCriteriaTest`, `RoutesViewModelFilterTest`, `TranslationParityTest`). | Multi-dimensional route filtering accurately filters routes across all criteria; shared bottom sheet layout is spacious, gap-free, and code is cleanly shared with workout filtering; criteria reset when returning to main view; all existing route behaviors and invariants remain intact. | Verified |
| **TST-UI-087** | `ATT-735` | **Multi-Dimensional Segment Filtering, Shared Modal Sheet & Persistence Verification** | `REQ-UI-134` | 1. Unit Test Predicate Evaluation: Verify `SegmentFilterCriteria.matches()` across all dimensions: text query (name/city substring), minimum climb category (including category-0 exclusion), distance thresholds, elevation gain thresholds, and `hasPR` (`prTime_raw > 0`).<br>2. Action Trigger & Funnel Icon: Verify `SegmentsTabsScreen` renders the Material funnel icon (`Icons.Default.FilterAlt`) at the end of the action row (after sort button) with badge counter when filters are active.<br>3. Shared Modal Bottom Sheet Layout: Verify tapping the filter icon opens `SegmentFilterBottomSheet` built on shared `FilterBottomSheetScaffold`, displaying a dedicated top header with title and dismiss button (`Icons.Default.Close`), zero bottom gap artifacts, filter sections (Query, Climb Category, Distance, Elevation, Has PR), and a dedicated bottom action row for "Reset all" and "OK".<br>4. Active Filter Chips: Verify applying segment filters displays horizontal scrolling chip strip below the sport tabs with localized labels and trailing `✕` icons using shared `RemovableFilterChip`. Verify tapping `✕` removes that specific filter dimension reactively.<br>5. Clear All / Reset: Verify tapping "Reset all" in the sheet or removing all chips resets criteria to default, hides chips and badge, and restores the full segment list.<br>6. Climb Category Filter Semantics: Verify that when `minClimbCategory >= 1`, segments with `climbCategory_raw == 0` (uncategorized) are excluded from results.<br>7. Tab Persistence: Verify that active filter criteria persist when switching between Bike and Run tabs within the segment screen.<br>8. Code Sharing Invariant: Verify workout filtering (`REQ-UI-132` / `TST-UI-085`) and route filtering (`REQ-UI-133` / `TST-UI-086`) continue to function without visual or functional regressions after segment filter integration.<br>9. Segment Invariants: Verify sorting orders (`DISTANCE_TO_USER`, `CLIMB_CATEGORY`, `TOTAL_ELEVATION_GAIN`, `AVERAGE_GRADE`, `SEGMENT_DISTANCE`, `NAME`), sport tabs (Bike, Run), and Strava segment sync remain fully operational.<br>10. Localization Parity: Verify all segment filter category labels, action buttons, chip descriptors, and empty-state filter prompts are localized across all 9 supported locales (EN, DE, ES, FR, IT, JA, NL, PL, PT).<br>11. Automated Unit Tests: Execute automated test suite (`SegmentFilterCriteriaTest`, `SegmentListViewModelFilterTest`, `TranslationParityTest`). | Multi-dimensional segment filtering accurately filters starred segments across all criteria; shared bottom sheet layout is spacious, gap-free, and code is cleanly shared with workout and route filtering; criteria persist across tab switches; all existing segment behaviors and invariants remain intact. | Verified |
| **TST-UI-088** | `ATT-737` | **Multi-Dimensional Favorite Tracks Filtering, Shared Modal Sheet & Persistence Verification** | `REQ-UI-135` | 1. Unit Test Predicate Evaluation: Verify `ClusterFilterCriteria.matches()` across all dimensions: text query (cluster name substring), equipment name matching (cluster linked equipment contains selected equipment), minimum reference distance thresholds ($10\,\text{km}, 25\,\text{km}, 50\,\text{km}, 100\,\text{km}$), and minimum recording count thresholds ($\ge 3, 5, 10, 25$).<br>2. Action Trigger & Funnel Icon: Verify `WorkoutClustersTabsScreen` renders the Material funnel icon (`Icons.Default.FilterAlt`) in the header action row beside the settings tuning icon with badge counter reflecting active filter dimensions count.<br>3. Shared Modal Bottom Sheet Layout: Verify tapping the filter icon opens `ClusterFilterBottomSheet` built on shared `FilterBottomSheetScaffold`, displaying a dedicated top header with title and dismiss button (`Icons.Default.Close`), zero bottom gap artifacts, filter sections (Query, Equipment, Distance, Recordings), and a dedicated bottom action row for "Reset all" and "OK".<br>4. Active Filter Chips: Verify applying cluster filters displays horizontal scrolling chip strip below the sport tabs with localized labels and trailing `✕` icons using shared `RemovableFilterChip`. Verify tapping `✕` removes that specific filter dimension reactively.<br>5. Clear All / Reset: Verify tapping "Reset all" in the sheet or removing all chips resets criteria to default, hides chips and badge, and restores the full cluster list.<br>6. Main View Navigation Reset: Verify navigating away from Favorite Tracks (`drawer_my_locations`) back to Start Tracking or another drawer item in `MainActivityWithNavigation` clears active cluster filter criteria, so subsequent visits start with the full unfiltered list.<br>7. Tab Interoperability: Verify active filters remain applied across all sport tabs (All, Bike, Run, Other, Unclustered).<br>8. Code Sharing Invariant: Verify workout (`REQ-UI-132`), route (`REQ-UI-133`), and segment (`REQ-UI-134`) filtering continue to function without regressions after cluster filter integration.<br>9. Cluster Invariants: Verify cluster recalculation engine, tuning sliders, manual cluster addition, cluster deletion, and heatmap views remain fully operational.<br>10. Localization Parity: Verify all cluster filter category labels, action buttons, chip descriptors, and empty-state filter prompts are localized across all 9 supported locales (EN, DE, ES, FR, IT, JA, NL, PL, PT).<br>11. Automated Unit Tests: Execute automated test suite (`ClusterFilterCriteriaTest`). | Multi-dimensional favorite track filtering accurately filters clusters across all criteria; shared bottom sheet layout is spacious and gap-free; criteria reset when returning to main view; all existing cluster behaviors and invariants remain intact. | Proposed |
| **TST-UI-089** | `ATT-761` | **Favorite Tracks (Lieblingsstrecken) Sorting Verification** | `REQ-UI-136` | 1. Unit Test Sort Order Logic: Verify `ClusterSortOrder` enum items and sorting logic (`RECORDINGS`, `DISTANCE_TO_USER`, `DISTANCE`, `NAME`):<br>• `RECORDINGS`: Sorts clusters by `hitCount` descending, with secondary tie-breaking by `name.lowercase()` ascending.<br>• `DISTANCE_TO_USER`: Geodetic distance from user coordinates to cluster `startLat`/`startLng` ascending. When location is null, falls back to `name.lowercase()`.<br>• `DISTANCE`: Sorts clusters by `refDistance` descending, with secondary tie-breaking by `name.lowercase()` ascending.<br>• `NAME`: Sorts clusters alphabetically by `name.lowercase()` ascending.<br>2. Action Trigger & Sort Button: Verify `WorkoutClustersTabsScreen` renders the sort action button (`Icons.Default.Sort`) with localized content description (`@string/sort`), positioned between tuning icon and filter action button.<br>3. Dropdown Menu Layout & Checkmark: Verify tapping the sort button opens `DropdownMenu` showing all `ClusterSortOrder` entries with localized labels. Verify leading checkmark (`Icons.Default.Check`) is displayed strictly next to the active sort order.<br>4. GPS Location Availability Handling: Verify that when user location is null/unavailable, `DISTANCE_TO_USER` menu item is disabled (`enabled = false`) and styled with disabled alpha (`0.38f`).<br>5. Reactive List Ordering: Verify selecting a sort order updates `viewModel.sortOrder`, immediately re-sorts the displayed cluster list, and dismisses the dropdown menu.<br>6. Tab Scope: Verify sorting applies across all sport tabs (*All*, *Bike*, *Run*, *Other*), while the *Unclustered* tab retains native workout date ordering.<br>7. Invariants: Verify existing filtering (`REQ-UI-135`), cluster tuning, manual cluster creation, and route/segment sorting remain intact without regression.<br>8. Localization Parity: Verify all sort order labels are localized across all 9 supported locales (EN, DE, ES, FR, IT, JA, NL, PL, PT).<br>9. Automated Unit Tests: Execute automated unit test suite (`ClusterSortOrderTest`). | Favorite tracks sort cleanly and reactively across all dimensions; location availability is handled gracefully with muted state; sort dropdown UI adheres to Routes and Segments design language. | Proposed |
| **TST-UI-090** | `ATT-501`, `ATT-792`, `ATT-795`, `ATT-796` | **Cluster Algorithm Info Presentation & Action Trigger Verification** | `REQ-UI-137` | 1. Component Unit & Compose Render Test (`ClusterInfoDialogTest.kt`):<br>• Verify `ClusterInfoDialog` renders title `@string/cluster_info_title`.<br>• Verify all 4 educational sections (What are clusters, 3D Fingerprint, Sport Type Awareness, Tuning Guidance) render with non-empty localized strings.<br>• Verify dismiss button `@string/close` invokes the `onDismiss` callback.<br>2. Action Trigger in `WorkoutClustersTabsScreen`: Verify header action row displays info action button with localized content description `@string/cluster_info_title`, and clicking the button opens `ClusterInfoDialog`.<br>3. Action Trigger in `ClusterTuningScreen`: Verify `TopAppBar` displays info action button with localized content description `@string/cluster_info_title`, and clicking the button opens `ClusterInfoDialog`.<br>4. Localization Parity: Verify all newly introduced string keys exist across all 9 supported application locales (EN, DE, ES, FR, IT, JA, NL, PL, PT) via automated resource validation.<br>5. Invariants & State Isolation: Verify opening and closing `ClusterInfoDialog` does not mutate cluster tolerance preferences (`ClusterTolerances`), database entries, or sort/filter state. | Educational info modal displays clearly with complete 3D topological algorithm explanations; accessible from both Favorite Tracks header and Cluster Parameter screen; dismisses smoothly; zero effect on cluster calculations or settings state. | Verified |
| **TST-UI-091** | `ATT-818`, `ATT-821` | **Detailed Workout Inspection from Favorite Tracks Verification** | `REQ-UI-138` | 1. Unit Tests for `MapModels.toMapTrack()` (`MapModelsTest.kt`):<br>• Verify that `WorkoutData.toMapTrack()` correctly decodes `encodedAltitudes` and `encodedDistances` into `PathPoint(altitude, latLng, distance)`.<br>• Verify that empty or missing altitude/distance strings handle gracefully with default fallback without throwing exceptions.<br>2. Unit Tests for `WorkoutClustersViewModel` (`WorkoutClustersViewModelTest.kt`):<br>• Verify `selectCluster(cluster)` updates `_clusterWorkouts` state with the retrieved workout list.<br>• Verify `selectWorkoutForPeek(workoutId)` successfully finds the workout in `_clusterWorkouts.value`, triggers `workoutClusterRepository.getWorkoutWithTrack(workoutId)`, and updates `_peekedWorkoutDataWithTrack`.<br>3. View Model & Integration Verification:<br>• Verify that inspecting a workout in `WorkoutClustersFragment` triggers `trackOnMapViewModel.loadAftermathData(inspectedWorkout)`.<br>• Verify that `TrackOnMapScreen` displays valid elevation bounds ($min < max$) matching the workout's altitude extrema, and renders extrema markers (`MIN_ALTITUDE`, `MAX_ALTITUDE`, etc.).<br>4. Regression Verification:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`) to verify zero regressions. | Inspecting any workout from Lieblingsstrecken displays true altitude profile, correct elevation bounds, sensor extrema pins, and available technical track layers; `toMapTrack()` decodes elevations accurately. | Verified |
| **TST-UI-092** | `ATT-861`, `ATT-873`, `ATT-878` | **FastScrollbar Edge Alignment, Continuous Drag Accumulation & Action Non-Interference Verification** | `REQ-UI-139` | 1. *Component Unit & Math Verification (`FastScrollbarTest.kt`)*:<br>• Verify continuous drag accumulator advances `dragProgress` and calculates valid target indices under fractional per-frame drag deltas ($\Delta_y \approx 2\dots 10\text{px}$), preventing integer truncation loss.<br>• Verify boundary index clamping at 0 and $\text{totalItems}-1$.<br>• Verify visual thumb offset matches `effectiveProgress` during dragging (`isDragging = true`).<br>2. *Integration Verification in Workout List*:<br>• In `WorkoutList`, verify `FastScrollbar` sits flush to the screen edge with zero end padding.<br>• Verify tapping the 3-dots export menu button on any `WorkoutSummary` card opens the export menu cleanly.<br>• Verify tapping the edit button on any `WorkoutSummary` card opens the editor cleanly.<br>• Verify dragging the 28dp thumb at normal human finger speed smoothly scrolls the list from top to bottom.<br>3. *Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`) to verify zero regressions. | FastScrollbar sits flush at the right edge; thumb dragging smoothly scrolls the list even with small finger movements; taps on card action buttons are never intercepted by the scrollbar overlay. | Verified |
| **TST-UI-093** | `ATT-866`, `ATT-885`, `ATT-888` | **Universal FastScrollbar Integration & Action Non-Interference Verification Across Collection Views** | `REQ-UI-140` | 1. *View Integration & Scroll Verification*:<br>• In Periods (`PeriodList.kt`), verify `FastScrollbar` renders flush at the right edge when list exceeds viewport and scrolls smoothly upon dragging.<br>• In Routes (`RouteList.kt`), verify `FastScrollbar` renders flush at the right edge and respects collapsing app bar insets.<br>• In Favorite Tracks (`WorkoutClustersList.kt`), verify `FastScrollbar` renders in both clustered and unclustered lists and respects the 80dp FAB bottom padding.<br>• In Segments (`SegmentList.kt`), verify `FastScrollbar` renders flush at the right edge and scrolls smoothly.<br>• In Equipment (`EquipmentTabsScreen.kt`), verify `FastScrollbar` renders flush at the right edge and scrolls equipment items.<br>• In Sport Types (`SportTypesTabsScreen.kt`), verify `FastScrollbar` renders flush at the right edge and scrolls sport types.<br>• In Connected Devices (`DeviceListScreen.kt`), verify `FastScrollbar` renders flush at the right edge and scrolls devices.<br>2. *Auto-Hiding Verification*:<br>• For views with empty data or fewer items than viewport capacity, verify `FastScrollbar` remains hidden and does not alter layout padding.<br>3. *Card Action Non-Interference Verification*:<br>• Verify tapping interactive controls on cards across all 7 views (Route item toggle/delete, cluster card click, segment selection, equipment menu/stats, sport type edit/stats, device pair switch) functions immediately with zero touch interception.<br>4. *Hardware & Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`): BUILD SUCCESSFUL (0 regressions).<br>• Execute physical hardware verification on Google Pixel 10 (`66020DLCR002FL`): FastScrollbar thumb drags and list scrolls smoothly across Workouts, Periods, Routes, Sport Types, Equipment, and Clusters; auto-hiding confirmed on short lists. | All 7 collection views render the edge-aligned FastScrollbar when content overflows; dragging thumb scrolls each list smoothly; short lists hide the scrollbar automatically; all card actions and buttons remain 100% accessible; 0 test regressions. | Verified |
| **TST-UI-094** | `ATT-510`, `ATT-891`, `ATT-894`, `ATT-895` | **Workout Summary Lap Overview, Rabbit/Hedgehog Highlights & Database Migration Verification** | `REQ-UI-141` | 1. *Database Migration & Data Preservation Unit Test (`LapsDatabaseManagerTest.kt`)*:<br>• Initialize `Laps.db` at `DB_VERSION = 1` with sample laps for multiple workouts.<br>• Trigger migration to `DB_VERSION = 2`: verify `onUpgrade` executes `ALTER TABLE` adding `name` and `description` without dropping the table.<br>• Verify pre-existing lap records remain intact with correct durations, distances, and speeds, with `name = null` and `description = null`.<br>• Insert a lap with custom `name = "Warmup"` and `description = "Easy tempo"`, and verify retrieval.<br>• Test `getLaps(workoutId)` returns sorted laps for that workout.<br>• Test `getLapsForWorkouts(workoutIds)` vectorized batch retrieval returns grouped map matching single queries without N+1 query overhead.<br>2. *Data Layer & Mapper Unit Test (`WorkoutDataMapperLapsTest.kt`)*:<br>• Verify `WorkoutDataMapper.fromCursor(cursor)` and `fromCursor(cursor, batchMetadata)` accurately populate `WorkoutData.laps`.<br>3. *Compose UI Component Unit Test (`WorkoutLapsTest.kt`)*:<br>• Render `WorkoutLaps` with 1 lap: verify lap renders with default title "Lap 1", metrics formatted, and no rabbit/hedgehog badges.<br>• Render `WorkoutLaps` with 3 laps of differing speeds: verify fastest lap displays rabbit (🐇) badge and slowest displays hedgehog (🦔) badge.<br>• Render `WorkoutLaps` with custom lap name: verify custom name is displayed.<br>• Render `WorkoutLaps` with lap description: verify description is rendered as secondary text.<br>• Render `WorkoutLaps` with 5 laps: verify only first 3 are rendered initially along with "Show all 5 laps" button; verify clicking expand renders all 5 laps and changes toggle to "Show fewer".<br>• Verify empty laps list renders nothing.<br>4. *Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`) to ensure zero regressions across all modules. | Laps table migrates safely from v1 to v2 preserving historical records; batch loading eliminates N+1 queries; WorkoutLaps renders clean split table with rabbit (🐇) on fastest lap and hedgehog (🦔) on slowest lap; collapsible toggle works cleanly for >3 laps; 0 regressions. | Verified |
| **TST-UI-095** | `ATT-511`, `ATT-897`, `ATT-925`, `ATT-947` | **Interactive Lap Editing, Quick-Tag Presets, Sequential Navigation, and Full Workout Map Framing Verification** | `REQ-UI-142` | 1. *Geometry & Slicing Unit Test (`LapSegmentUtilsTest.kt`)*:<br>• Given a polyline with 100 `LatLng` points and 100 corresponding cumulative distances ($0 \dots 1000\text{m}$).<br>• Slicing for Lap 1 ($0 \dots 400\text{m}$): verify extracted sub-track contains points within $[0, 400\text{m}]$ and boundary anchor points.<br>• Slicing for Lap 2 ($400 \dots 1000\text{m}$): verify extracted sub-track contains points within $[400, 1000\text{m}]$.<br>• Verify `LatLngBounds` calculation correctly encloses the lap segment coordinates.<br>• Verify `LatLngBounds` calculation correctly encloses the full workout coordinates (`allPoints`), calculating valid bounds spanning the entire route ($minLat \le maxLat$, $minLng \le maxLng$).<br>• Verify empty polyline or 0-distance workouts gracefully return empty track and null bounds without throwing exceptions.<br>2. *Compose UI & BottomSheet Unit Test (`LapEditBottomSheetTest.kt`)*:<br>• Render `LapEditBottomSheet` with a lap: verify header displays formatted duration, distance, and speed.<br>• Verify preset quick-tag chips (`Warm-up`, `Interval`, `Recovery`, etc.) are displayed.<br>• Tap "Interval" chip: verify custom name field immediately updates to "Interval".<br>• Enter custom description: verify description field captures text.<br>• Sequential navigation:<br>  - For first lap (Lap 1 of 3): verify `< Previous` button is disabled, `Next >` button is enabled.<br>  - Tap `Next >`: verify callback or state transition advances to Lap 2 with pending changes saved.<br>  - For last lap (Lap 3 of 3): verify `Next >` button is disabled, `< Previous` button is enabled.<br>• Map integration & Full Course Framing:<br>  - Verify map camera automatically calculates and animates to `workoutBounds` enclosing the entire workout session (`allPoints`), providing global spatial context.<br>  - Verify map polyline layering: full workout polyline rendered in muted neutral styling (`zIndex = 1f`), active lap segment rendered in prominent primary styling (`zIndex = 2f`), start and end markers pinned at lap boundaries (`zIndex = 3f`).<br>  - Verify sequential navigation updates the highlighted lap segment and markers while map camera framing remains stable over the entire course.<br>  - For indoor/zero-GPS session, verify map renders `NoGpsTrackCard` fallback placeholder without error.<br>3. *Data Layer & In-Memory Update Unit Test (`WorkoutRepositoryLapUpdateTest.kt`)*:<br>• Invoke `WorkoutRepository.updateLapDetails(workoutId, lapNr, name, description)`:<br>  - Verify `LapsDatabaseManager.updateLapDetails` is invoked with correct arguments.<br>  - Verify in-memory `WorkoutData.laps` is updated with new name and description.<br>  - Verify `WorkoutData` emitted by repository reflects updated lap details immediately without full list reload.<br>4. *Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`) to ensure zero regressions across all modules. | Lap edit bottom sheet allows one-tap tag assignment, custom text entry, sequential lap navigation, and highlighted segment map visualization with stable camera bounds framing the full workout course; repository updates database and cache reactively; 0 regressions. | Verified |
| **TST-UI-096** | `ATT-920`, `ATT-931` | **Workout Summary Laps Table Header, Pace Single-Line & Column Width Verification** | `REQ-UI-143` | 1. *Compose UI Component Unit Test (`WorkoutLapsTest.kt`)*:<br>• *Header Row Presence & Localization*: Render `WorkoutLaps` with 1+ laps. Verify header row renders labels for Lap (`@string/lap_table_header_lap`), Time (`@string/lap_table_header_time`), Distance (`@string/lap_table_header_distance`), and sport-appropriate Pace/Speed with unit (e.g., `"Tempo [min/km]"` or `"Geschw. [km/h]"`).<br>• *Numerical Formatting (No Cell Units)*: Verify pace/speed cell renders numerical string (`formatters.pace.format` / `formatters.speed.format`) without repeating unit text.<br>• *Single-Line Constraint*: Verify time, distance, and pace texts specify `maxLines = 1` and `softWrap = false`.<br>• *Proportional Widths*: Verify column weights (1.6f for name, 0.85f for time, 0.85f for distance, 1.1f for pace/speed, 26dp for badge) in both `LapTableHeader` and `LapRow`.<br>• *Badge Centering*: Verify Rabbit (🐇) and Hedgehog (🦔) badges are positioned in the dedicated badge column with vertical centering.<br>• *Empty Laps Guard*: Verify workouts with 0 laps render no header and no lap rows.<br>2. *Localization Parity Test*: Verify string keys `lap_table_header_lap`, `lap_table_header_time`, `lap_table_header_distance`, and `pace_short` / `speed_short` exist and are translated across all 9 supported locales (values, values-de, values-es, values-fr, values-it, values-ja, values-nl, values-pl, values-pt).<br>3. *Regression Verification*: Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`) to ensure zero regressions across all modules. | Laps table in `WorkoutSummary` displays distinct headers styled with `labelSmall`, pace values never wrap across multiple lines due to column unit placement, lap names have expanded 1.6f width, badges are vertically centered, and 100% translation parity is preserved across all 9 locales with 0 regressions. | Verified |
| **TST-UI-097** | `ATT-883`, `ATT-974` | **Strava Best Efforts Collapsible Display, Metric Unit Filtering & Longest Distance Verification** | `REQ-UI-144` | 1. *Unit System Filtering Unit Tests (`StravaActivitySectionTest.kt`)*:<br>• Under `MyUnits.METRIC`, verify that mile intervals (`"1/2 mile"`, `"1 mile"`, `"2 miles"`, `"10 miles"`) are excluded from the collapsed view.<br>• Under `MyUnits.IMPERIAL`, verify that mile intervals remain eligible for the collapsed view.<br>2. *Longest Distance & PR Aggregation Unit Tests (`StravaActivitySectionTest.kt`)*:<br>• For an activity with 0 PRs, verify that the longest distance (e.g. 10k in a 10k run) is displayed in the collapsed view.<br>• For an activity with an intermediate PR (e.g. 1k PR in a 10k run), verify that both 1k (PR) and 10k (longest) are displayed.<br>• For an activity where the longest distance is a PR (e.g. 5k PR in a 5k run), verify that 5k is displayed exactly once without duplicates.<br>• Verify that items in the collapsed view maintain natural ascending distance order.<br>3. *Accordion State & Expansion Unit Tests (`StravaActivitySectionTest.kt`)*:<br>• Verify that when `allEfforts.size > reducedEfforts.size`, accordion is collapsible with accurate hidden count ($N - M$).<br>• Verify that when expanded (`isExpanded = true`), all $N$ best efforts (including miles) are returned.<br>• Verify that when `allEfforts.size == reducedEfforts.size`, section is non-collapsible.<br>4. *Localization Parity Test (`TranslationParityTest.kt`)*:<br>• Verify `strava_show_all_best_efforts_format`, `strava_show_less_best_efforts_format`, and `strava_show_less_best_efforts` exist and pass format specifier validation across all 9 supported locales (values, values-de, values-es, values-fr, values-it, values-ja, values-nl, values-pl, values-pt).<br>5. *Clean-Room Regression Verification*:<br>• Execute full repository test suite (`./gradlew testDebugUnitTest`): BUILD SUCCESSFUL with 0 regressions. | Collapsed best efforts view excludes miles when metric units are active; guarantees visibility of the longest distance time; displays PRs without duplication; expands in-place to all efforts; maintains 100% localization parity across all 9 locales with zero regressions. | Verified (2026-09-13 via automated unit tests in `StravaActivitySectionTest` and `TranslationParityTest` with 100% full-suite regression pass) |
| **TST-UI-098** | `ATT-917`, `ATT-983` | **UI Deletion of Old Unfinished Workouts & Active Workout Protection Verification** | `REQ-UI-145` | 1. *Active Tracking State Unit Tests (`TrainingApplicationTrackedTest.kt`)*:<br>• When `cTrackingMode == TrackingMode.READY`, verify `TrainingApplication.isActivelyTracked(workoutId)` returns `false` for any workout ID.<br>• When `cTrackingMode == TrackingMode.TRACKING` and `activeWorkoutId == 100L`, verify `isActivelyTracked(100L)` returns `true` and `isActivelyTracked(101L)` returns `false`.<br>• When tracking stops (`stopTracking()`), verify `isActivelyTracked` resets to `false`.<br>2. *WorkoutHeader & Summary Deletion Ergonomics Unit Tests (`WorkoutDeletionErgonomicsTest.kt`)*:<br>• Render `WorkoutHeader` with `canDelete = true` (old unfinished or finished workout): verify `combinedClickable` attaches long-click action and triggers `onDeleteRequest()`.<br>• Render `WorkoutHeader` with `canDelete = false` (actively tracked workout): verify long-click action is disabled / does not trigger `onDeleteRequest()`.<br>• Verify 3-dots export menu is enabled only when `menuEnabled == true` (`finished == true`), preventing export of unfinished sessions.<br>3. *WorkoutSummaryCompact Deletion Guard Unit Tests*:<br>• For an old unfinished workout (`finished == false` and not actively tracked), verify "Delete" option is accessible in the context menu.<br>• For an actively tracked workout (`isActivelyTracked == true`), verify "Delete" is disabled or hidden.<br>4. *Clean-Room Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`): BUILD SUCCESSFUL (0 regressions). | Old unfinished workouts allow UI deletion via long-press in both full and compact views; live actively tracked workouts are strictly protected from deletion; unfinished workouts retain alpha = 0.5f; 100% test pass with 0 regressions. | Verified (2026-09-13 via automated unit tests in `TrainingApplicationTrackedTest` and `WorkoutDeletionErgonomicsTest` with 100% full-suite regression pass) |
| **TST-UI-099** | `ATT-987`, `ATT-989` | **Mark Old Unfinished Workout Finished & Active Workout Protection Verification** | `REQ-UI-146` | 1. *Unit & State Transition Tests (`WorkoutMarkFinishedTest.kt`)*:<br>• Given an unfinished workout (`finished == false`): verify `canMarkFinished == true` when idle (`isTracking() == false`).<br>• Verify visual alpha is `0.5f` and export `menuEnabled` is `false`.<br>• Trigger `markWorkoutFinished(workoutId)`: verify `finished` transitions to `true`.<br>• In finished state: verify `contentAlpha == TTAlpha.High` (1.0f), export `menuEnabled == true`, and "Mark as finished" option is omitted.<br>2. *Active Tracking Safety Guard Tests*:<br>• When `isTracking() == true` and `workoutId == activeWorkoutId`: verify `canMarkFinished == false`, strictly preventing active workout from being marked finished via aftermath UI.<br>• When `isTracking() == true` and `workoutId != activeWorkoutId`: verify `canMarkFinished == true` for an old unfinished session.<br>3. *Period Integration & Notification Tests*:<br>• Verify marking finished triggers `PeriodsRepository.onWorkoutFinished`, updating Day, Week, Month, and Year periods.<br>4. *Localization Parity Test*:<br>• Verify string key `mark_as_finished` exists and is translated across all 9 supported application locales (values, values-de, values-es, values-fr, values-it, values-ja, values-nl, values-pl, values-pt).<br>5. *Clean-Room Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`): BUILD SUCCESSFUL (0 regressions). | Unfinished workouts allow marking as finished via context menu; active sessions are strictly protected; marking finished updates alpha to 1.0f, enables export, triggers periods rollup, and translates across all 9 locales; 0 regressions. | Verified (2026-09-13 via automated unit tests in `WorkoutMarkFinishedTest` with 100% full-suite regression pass) |
| **TST-UI-100** | `ATT-993`, `ATT-995` | **Compact Workout Summary Context Menu Streamlining Verification** | `REQ-UI-147` | 1. *Unit & State Tests (`WorkoutSummaryCompactMenuTest.kt`)*:<br>• *Idle Completed Workout*: Verify `canDelete == true`, `canMarkFinished == false`, `hasContextMenu == true`. Verify context menu contains only "Delete" and omits "Edit workout".<br>• *Idle Unfinished Workout*: Verify `canDelete == true`, `canMarkFinished == true`, `hasContextMenu == true`. Verify context menu contains "Mark as finished" and "Delete", and omits "Edit workout".<br>• *Actively Tracked Workout*: When `isActivelyTracked == true`, verify `canDelete == false`, `canMarkFinished == false`, and `hasContextMenu == false` (disabling long-press).<br>2. *Full Summary Editing Invariant*: Verify `WorkoutSummary` / `WorkoutHeader` retains `onEditWorkout` parameter and continues to support edit interaction in expanded view.<br>3. *Regression Verification*: Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`): BUILD SUCCESSFUL with 0 regressions. | Compact view context menu excludes "Edit workout" across all workout states, restricts items to lifecycle actions, disables context menu when actively tracked, and preserves expanded view editing with zero regressions. | Verified (2026-09-13 via automated unit tests in `WorkoutSummaryCompactMenuTest` and `WorkoutDeletionErgonomicsTest` with 100% full-suite regression pass) |
| **TST-UI-101** | `ATT-939`, `ATT-1025` | **Edge-to-Edge Bottom Sheet Insets & Shared Core Components Verification** | `REQ-UI-148` | 1. *Unit & Layout Verification (`CoreComponentsIntegrityTest.kt`, `AppModalBottomSheetTest.kt`, `AppTableHeaderTest.kt`, `FastScrollbarTest.kt`)*:<br>• Verify `CoreComponentsIntegrityTest` confirms presence, public access, and contracts for all 6 core components (`AppModalBottomSheet`, `AppTableHeader`, `AppTableTypography`, `MinimumDragHandle`, `FastScrollbar`, `BadgeBox`).<br>• Verify `AppModalBottomSheet` composable renders title, close button, drag handle, and content slot with `navigationBarsPadding()` and `imePadding()`.<br>• Verify `AppTableHeader` and `AppTableTypography` render column headers with `includeFontPadding = false` and `LineHeightStyle(Alignment.Bottom, Trim.Both)`.<br>• Verify `LapTableHeader` in `WorkoutLaps.kt` uses `AppTableHeader`, `BadgeSpacer`, and `BadgeBox` with column weights (1.6f, 0.85f, 0.85f, 1.1f, 26dp) and renders localized titles cleanly.<br>• Verify `FastScrollbarTest` confirms 11 unit test scenarios for scroll calculations and progress accumulation.<br>2. *Edge-to-Edge Insets Verification*:<br>• In `RichStatsSheet.kt`, verify `navigationBarsPadding()` replaces hardcoded `padding(bottom = 32.dp)`.<br>• On devices with 3-button navigation bars and gesture navigation bars, verify sheets anchor cleanly to the navigation bar without clipping or excessive bottom gap.<br>3. *Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest --no-daemon`) to ensure zero regressions across all modules. | `AppModalBottomSheet`, `AppTableHeader`, `AppTableTypography`, `MinimumDragHandle`, `FastScrollbar`, and `BadgeBox` provide clean, reusable core primitives; bottom sheets handle navigation bars reactively; `LapTableHeader` and `WorkoutExtrema` render tabular metrics with bottom-baseline alignment and uniform 26dp badge width; zero unit test regressions. | Verified (2026-09-14 via automated unit tests in `CoreComponentsIntegrityTest`, `AppModalBottomSheetTest`, `AppTableHeaderTest`, and `FastScrollbarTest` with 100% full-suite regression pass) |
| **TST-UI-102** | `ATT-900`, `ATT-1029`, `ATT-1031`, `ATT-1032` | **Modernized Modal Bottom Sheet Dialogs & Selection Flows Verification** | `REQ-UI-149` | 1. *Unit & Contract Verification (`ModalBottomSheetDialogsIntegrityTest.kt`)*:<br>• Verify `AppModalBottomSheet` supports both vector icon (`icon: ImageVector?`) and painter icon (`iconPainter: Painter?`) parameters in its composable signature.<br>• Verify `ExportSettingsDialog`, `UnitsSettingsDialog`, `DisplaySettingsDialog`, and `ActivityTypeSelectionDialog` render as bottom popups utilizing `AppModalBottomSheet`.<br>• Verify `SensorSourceDialog` and `DeviceTypeSelectionDialog` render as bottom popups utilizing `AppModalBottomSheet`.<br>• Verify `EditSensorFieldDialog` and `ConfigureFilterDialog` render as bottom popups utilizing `AppModalBottomSheet`.<br>• Verify `EditSportTypeDialog`, `EditEquipmentDialog`, and `EditDeviceDialog` render as bottom popups utilizing `AppModalBottomSheet`.<br>• Verify `WorkoutClusterSelectionDialog`, `EditWorkoutClusterDialog`, and `ClusterInfoDialog` render as bottom popups utilizing `AppModalBottomSheet`.<br>• Verify `ExportDetailsDialog` renders as a bottom popup utilizing `AppModalBottomSheet`.<br>• Verify that `ExportSettingsDialog` preserves toggles for TCX, GPX, Golden Cheetah JSON, and CSV with `TrainingApplication` getters/setters.<br>• Verify that `UnitsSettingsDialog` persists Metric vs Imperial selection to `SharedPreferences` under `SP_UNITS`.<br>• Verify that `DisplaySettingsDialog` preserves `forcePortrait`, `keepScreenOn`, and `noUnlocking` toggles and synchronizes with `TrainingApplication.setDisplayOptions`.<br>• Verify that `ActivityTypeSelectionDialog` displays sport type icons with un-tinted authentic colors (`REQ-UI-114`) and triggers `onTypeSelected`.<br>• Verify that `SensorSourceDialog` displays active source, backups, and available devices, and triggers `onDeviceClick`.<br>• Verify that `DeviceTypeSelectionDialog` lists protocol devices and handles `onSelected`.<br>2. *Centered Dialog Invariant Verification*:<br>• Verify `LapSummaryDialog` retains its centered 3-second auto-dismissing HUD presentation during active tracking.<br>• Verify destructive confirmation dialogs (`DeleteConfirmationDialog`, `WorkoutDeleteDialog`, `DeleteOldWorkoutsDialog`), deletion progress dialogs (`WorkoutDeletionProgressDialog`), and system redirects (`GPSDisabledDialog`, `StartOrResumeDialog`) retain centered `AlertDialog` presentation.<br>3. *Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest --no-daemon`) to ensure zero regressions across all modules. | All interactive configuration and selection popups render as bottom sheets anchored above the navigation bar; transient tracking HUD and destructive alerts remain centered dialogs; SharedPreferences state and callbacks are 100% preserved; zero regressions. | Verified (2026-09-14 via `ModalBottomSheetDialogsIntegrityTest`, `DisplaySettingsTest`, and 100% full-suite clean-room regression pass) |
| **TST-UI-103** | `ATT-1034`, `ATT-1039` | **Standardized Bottom Sheet Action Layouts, Semantic Action Verbs & Route Dialog Verification** | `REQ-UI-150` | 1. *Unit & Layout Contract Verification (`DialogActionsIntegrityTest.kt` / `ModalBottomSheetDialogsIntegrityTest.kt`)*:<br>• Verify `AppDialogActions.SaveCancel`, `AppDialogActions.Confirm`, and `AppDialogActions.CancelOnly` provide uniform structure, paddings, and button semantics in `ui.components.core`.<br>• Verify data-storing sheets (`EditDeviceDialog`, `EditSensorFieldDialog`, `ConfigureFilterDialog`, `UnitsSettingsDialog`, `EditEquipmentDialog`, `EditSportTypeDialog`, `EditRouteDialog`, `ClusterTuningContent` / `ImportBackupTabsScreen`) bind primary action text to `R.string.save` ("Speichern").<br>• Verify immediate-setting sheets (`ExportSettingsDialog`, `DisplaySettingsDialog`) bind primary action text to `android.R.string.ok` ("OK") and eliminate `R.string.Done`.<br>• Verify informational sheets (`SensorSourceDialog`, `ExportDetailsDialog`, `ClusterInfoDialog`) bind action button to `android.R.string.ok` or `R.string.OK`.<br>• Verify `EditRouteDialog` composes `AppModalBottomSheet` with `AppDialogActions.SaveCancel`, replacing full-screen `Scaffold` while preserving `RouteSummary` editing.<br>• Verify `ImportBackupTabsScreen` Cluster-Parameter tuning sheet composes `AppModalBottomSheet` and `AppDialogActions.SaveCancel`, eliminating `OutlinedButton`.<br>2. *Navigation & Callback Invariant Verification*:<br>• Verify `EditRouteDialog` invokes `onSave` with updated route details and `onCancel` on dismiss.<br>• Verify `UnitsSettingsDialog` persists selected unit to `SP_UNITS` on save and discards uncommitted selection on cancel.<br>• Verify `EditDeviceDialog` calls `viewModel.saveChanges()` on save and discards on cancel.<br>3. *Clean-Room Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest --no-daemon`) to ensure zero regressions across all modules. | Bottom popups provide uniform action bar layouts and semantic verb distinctions (Save vs. OK); route editing is integrated as an `AppModalBottomSheet`; cluster tuning in import conforms to design system; 100% test pass with zero regressions. | Verified |
| **TST-UI-104** | `ATT-1043`, `ATT-1046` | **Workout Editing Modal Bottom Sheet & Viewport Layering Verification** | `REQ-UI-151` | 1. *Unit & Reflection Contract Verification (`ModalBottomSheetDialogsIntegrityTest.kt`)*:<br>• Verify `EditWorkoutDialog` (and `EditWorkoutScreen`) exists, is public, and composes `AppModalBottomSheet` with `AppDialogActions.SaveCancel`.<br>• Verify primary action is bound to `@string/save` ("Speichern") and secondary action is bound to `@string/Cancel` ("Abbrechen").<br>2. *ViewModel & Mutation Contract Verification*:<br>• Verify `viewModel.saveChanges()` commits `workoutData` to `WorkoutRepository`.<br>• Verify dismissing without tapping "Speichern" discards in-memory modifications without invoking `saveChanges()`.<br>• Verify `EditWorkoutClusterDialog` integration retains cluster assignment, unassignment, and creation.<br>3. *Caller Layering Integrity*:<br>• Verify callers in `WorkoutSummariesListFragment`, `WorkoutSummariesTabbedFragment`, `PeriodsFragment`, and `WorkoutClustersFragment` preserve underlying screen composition while `selectedWorkoutIdForEdit != null` / `editedWorkoutId != null`.<br>4. *Clean-Room Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest --no-daemon`) ensuring zero regressions. | `EditWorkoutScreen` conforms to `AppModalBottomSheet` and `AppDialogActions.SaveCancel`; callers maintain active background layers beneath scrim; 100% tests pass. | Verified |
| **TST-UI-105** | `ATT-1051`, `ATT-1053` | **Dropbox Settings Modal Bottom Sheet & Navigation Integration Verification** | `REQ-UI-152` | 1. *Unit & Reflection Contract Verification (`ModalBottomSheetDialogsIntegrityTest.kt`)*:<br>• Verify `DropboxSettingsDialogKt` exists, is public, and exposes `@Composable fun DropboxSettingsDialog(onDismiss: () -> Unit)`.<br>• Verify `DropboxSettingsDialogFragment` extends `androidx.fragment.app.DialogFragment` and provides `newInstance()`.<br>2. *Authentication & Connection Logic Verification*:<br>• Verify disconnected state renders `@string/dropbox_connect` and triggers `Auth.startOAuth2PKCE(...)`.<br>• Verify connected state renders `@string/dropbox_connected_status` in green and provides `@string/dropbox_disconnect` calling `TrainingApplication.deleteDropboxCredential()` and `TrainingApplication.setUploadToDropbox(false)`.<br>• Verify `DropboxSettingsDialogFragment.onResume()` captures credentials via `Auth.getDbxCredential()` upon return from OAuth flow.<br>3. *Backup Configuration & Action Bar Verification*:<br>• Verify automated backups toggle binds to `automated_backups` with summary `@string/automated_backups_summary`.<br>• Verify backup interval selector binds to `backup_interval_days` (values 1, 3, 7, 30) from `@array/backup_interval_entries`.<br>• Verify action bar uses `AppDialogActions.SaveCancel`: dismissing or tapping "Abbrechen" discards modifications; tapping "Speichern" commits preferences to `SharedPreferences` and triggers `BackupWorker.schedule(context)`.<br>4. *Navigation Drawer Verification*:<br>• In `MainActivityWithNavigation.kt`, verify `R.id.drawer_dropbox` and `onPreferenceStartScreen("cloudUpload")` launch `DropboxSettingsDialogFragment` without replacing the main screen fragment.<br>5. *Clean-Room Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest --no-daemon`) ensuring zero regressions. | Dropbox settings conforms to `AppModalBottomSheet` and `AppDialogActions.SaveCancel`; drawer opens modal bottom sheet; credential capture and backup scheduling preserved; 100% tests pass. | Verified |
| **TST-UI-106** | `ATT-1048`, `ATT-1058` | **Strava Settings Modal Bottom Sheet & Navigation Integration Verification** | `REQ-UI-153` | 1. *Unit & Reflection Contract Verification (`ModalBottomSheetDialogsIntegrityTest.kt`)*:<br>• Verify `StravaSettingsDialogKt` exists, is public, and exposes `@Composable fun StravaSettingsDialog(onDismiss: () -> Unit)`.<br>• Verify `StravaSettingsDialogFragment` extends `androidx.fragment.app.DialogFragment` and provides `newInstance()`.<br>2. *Authentication & Connection Logic Verification*:<br>• Verify disconnected state renders `@string/strava_disconnected_status` and provides button initiating `StravaHelper.requestAccessToken(...)`.<br>• Verify connected state renders `@string/strava_connected_status` in green and provides `@string/strava_disconnect` calling `TrainingApplication.deleteStravaToken()` and `StravaDeauthorizationThread`.<br>• Verify loading state renders circular progress indicator with `@string/please_wait`.<br>3. *Sync Actions & Selective Upload Verification*:<br>• Verify manual sync actions render for equipment and routes, displaying formatted summaries from `TrainingApplication.getLastUpdateTimeOfStravaEquipment()` and `TrainingApplication.getLastUpdateTimeOfStravaRoutes()`.<br>• Verify selective upload toggles bind to `uploadStravaGPS`, `uploadStravaAltitude`, `uploadStravaHR`, `uploadStravaPower`, `uploadStravaCadence`, defaulting to `true`.<br>• Verify action bar uses `AppDialogActions.SaveCancel`: dismissing or tapping "Abbrechen" discards toggle modifications; tapping "Speichern" commits preferences to `SharedPreferences`.<br>4. *Navigation Drawer & Preference Verification*:<br>• In `MainActivityWithNavigation.kt`, verify `R.id.drawer_strava` and `onPreferenceStartScreen(TrainingApplication.PREFERENCE_SCREEN_STRAVA)` launch `StravaSettingsDialogFragment` without replacing the active fragment.<br>5. *Clean-Room Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest --no-daemon`) ensuring zero regressions. | Strava settings conforms to `AppModalBottomSheet` and `AppDialogActions.SaveCancel`; drawer opens modal bottom sheet directly; token revocation, sync triggers, and selective upload options preserved; 100% tests pass. | Verified |
| **TST-UI-107** | `ATT-1044`, `ATT-1063` | **Search Settings Modal Bottom Sheet & Navigation Integration Verification** | `REQ-UI-154` | 1. *Unit & Reflection Contract Verification (`ModalBottomSheetDialogsIntegrityTest.kt`)*:<br>• Verify `SearchSettingsDialogKt` exists, is public, and exposes `@Composable fun SearchSettingsDialog(onDismiss: () -> Unit)` with single dismiss parameter.<br>• Verify `SearchSettingsDialogFragment` extends `androidx.fragment.app.DialogFragment` and provides `newInstance()`.<br>2. *Search Preferences & Staging Verification*:<br>• Verify search tries slider renders with min 1, max 5, steps 3, bound to `numberOfSearchTriesInt` with numeric text label.<br>• Verify trigger event switches bind to `startSearchWhenAppStarts` (default true), `startSearchWhenResumeFromPaused` (default true), `startSearchWhenUserChangesSport` (default true), and `startSearchWhenTrackingStarts` (default false).<br>• Verify behavior switches bind to `searchOnlyForSportSpecificDevices` (default true) and `changeSportWhenDeviceGetsLost` (default true) with title and summary.<br>• Verify action bar uses `AppDialogActions.SaveCancel`: dismissing or tapping "Abbrechen" discards modifications; tapping "Speichern" commits all 7 preferences to `SharedPreferences`.<br>3. *Navigation Drawer & Preference Verification*:<br>• In `MainActivityWithNavigation.kt`, verify `R.id.drawer_search_settings` and `onPreferenceStartScreen("search_settings")` launch `SearchSettingsDialogFragment` without replacing the active fragment.<br>4. *Clean-Room Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest --no-daemon`) ensuring zero regressions. | Search settings conforms to `AppModalBottomSheet` and `AppDialogActions.SaveCancel`; drawer opens modal bottom sheet directly; search triggers, behavior options, and retry slider preserved; 100% tests pass. | Verified |
| **TST-UI-108** | `ATT-1018`, `ATT-1068` | **Legacy Fragment Retirement, Unused Layout Purge & Resource Cleanup Verification** | `REQ-UI-155` | 1. *Static Analysis & Source Code Verification*:<br>• Verify `CloudUploadFragment.kt`, `SearchSettingsFragment.kt`, and `StravaUploadFragment.kt` are deleted from the codebase.<br>• Verify `MainActivityWithNavigation.kt` contains zero unused imports for the deleted fragments.<br>• Verify `StarredSegmentsFragment.kt` launches `StravaSettingsDialogFragment.newInstance()` and contains zero references to `StravaUploadFragment`.<br>2. *Layout Integrity & Resource Audit*:<br>• Verify all 59 obsolete View-based layout files are deleted from `app/src/main/res/layout/`.<br>• Verify the 9 actively required layout files (`check_ant_installation_dialog.xml`, `export_notification__expanded.xml`, `export_notification__group.xml`, `main_activity_with_navigation.xml`, `main_activity_without_navigation.xml`, `pebble_config_list_5.xml`, `preference_category.xml`, `preference_slim.xml`, `tabbed_config_views.xml`) remain present and uncorrupted.<br>• Verify unreferenced string resources in `res/values/strings.xml` and translation files associated with deleted layouts are cleanly purged without breaking active string resolution.<br>3. *Automated Unit & Reflection Tests*:<br>• Run `./gradlew testDebugUnitTest --no-daemon` to confirm 100% clean-room test pass with 0 regressions across all test suites.<br>• Run `ModalBottomSheetDialogsIntegrityTest` to ensure all modernized bottom sheet and dialog contracts remain intact.<br>4. *Device Deployment & Runtime Sanity*:<br>• Deploy debug APK to Google Pixel 10 (`./gradlew installDebug`) and verify clean launch without `InflateException` or missing resource crashes. | Obsolete fragments, 59 dead layout XML files, and unreferenced strings purged; active layouts preserved; 100% clean-room test pass with zero regressions. | Verified |
| **TST-UI-109** | `ATT-1072`, `ATT-1080` | **Bottom Popup Single-Window BottomSheetDialogFragment, Decor & Zero-Flicker Dismiss Verification** | `REQ-UI-156` | 1. *Theme Overlay & Resource Verification (`themes.xml`)*:<br>• Verify `ThemeOverlay.aTrainingTracker.BottomSheetDialogFragment` is declared in `res/values/themes.xml` inheriting from `ThemeOverlay.MaterialComponents.BottomSheetDialog`.<br>• Verify `android:windowDrawsSystemBarBackgrounds` is set to `true`.<br>• Verify `android:navigationBarColor` and `android:statusBarColor` are `@android:color/transparent`.<br>• Verify `android:enforceNavigationBarContrast` and `android:enforceStatusBarContrast` are `false`.<br>• Verify `android:windowLightNavigationBar` and `android:windowLightStatusBar` are `true`.<br>• Verify `android:windowBackground` is `@android:color/transparent`.<br>2. *Base Class & Inheritance Reflection Verification (`ModalBottomSheetDialogsIntegrityTest.kt`)*:<br>• Verify `com.atrainingtracker.trainingtracker.ui.components.core.AppBottomSheetDialogFragment` exists, is abstract, and inherits directly from `com.google.android.material.bottomsheet.BottomSheetDialogFragment`.<br>• Verify all 7 dialog fragments (`SearchSettingsDialogFragment`, `ExportSettingsDialogFragment`, `UnitsSettingsDialogFragment`, `StravaSettingsDialogFragment`, `DropboxSettingsDialogFragment`, `ActivityTypeSelectionDialogFragment`, `DisplaySettingsDialogFragment`) inherit directly from `AppBottomSheetDialogFragment`.<br>• Verify that `newInstance()` and `TAG` contracts remain present and public on all 7 companion objects.<br>3. *Single-Window Architecture Verification*:<br>• Verify that the Compose layout inside the 7 dialogs renders content directly within the single `BottomSheetDialog` window without spawning a nested Compose `ModalBottomSheet`.<br>4. *Automated Unit Test Suite*:<br>• Run `./gradlew testDebugUnitTest --tests "com.atrainingtracker.trainingtracker.ui.components.core.ModalBottomSheetDialogsIntegrityTest"`.<br>• Run `./gradlew testDebugUnitTest` to confirm 100% clean-room test pass with 0 regressions across all modules.<br>5. *Physical Device Verification (Pixel 10, Android 17 / API 37)*:<br>• Deploy debug APK to Google Pixel 10 (`./gradlew installDebug`).<br>• Open each bottom popup from navigation drawer (Display, Units, Export, Search, Strava, Dropbox) and tracking screen (Activity Type).<br>• Verify top status bar displays the underlying activity dimmed by a single scrim (never solid black).<br>• Dismiss each popup via system back button, navigation gesture, scrim tap, and close button: verify 100% transparent navigation bar with zero black flicker. | All 7 bottom popup dialog fragments inherit from `AppBottomSheetDialogFragment` (extending `BottomSheetDialogFragment`); theme overlay correctly configures single-window edge-to-edge transparent system bars; status bar remains transparent with single scrim; dismissal is completely free of black flickering; 100% of unit tests pass with zero regressions. | Verified |
| **TST-UI-110** | `ATT-882`, `ATT-1103`, `ATT-1108` | **Advanced Interval, Tab-Aware Sport & Sport-Aware Equipment Workout Filtering Verification** | `REQ-UI-157` | 1. *Filter Criteria Interval Evaluation Unit Tests (`WorkoutFilterCriteriaTest.kt`)*:<br>• Distance interval filtering: verify workout with distance below `minDistanceMeters` or above `maxDistanceMeters` is rejected; workouts within $[D_{\text{min}}, D_{\text{max}}]$ match.<br>• Duration interval filtering: verify workout with duration below `minDurationSec` or above `maxDurationSec` is rejected; workouts within $[T_{\text{min}}, T_{\text{max}}]$ match.<br>• Unbounded intervals: verify only `minDistanceMeters` set behaves as $\ge D_{\text{min}}$, only `maxDistanceMeters` set behaves as $\le D_{\text{max}}$, etc.<br>• Serialization test: verify JSON round-trip preserves all fields including `maxDistanceMeters` and `maxDurationSec`, and legacy JSON without max fields parses with null bounds.<br>• Active filter count calculation test: verify distance interval, duration interval, and date interval count towards active filters.<br>2. *Tab-Aware Sport Selection Logic Verification*:<br>• Verify `availableSports` filtering logic when `activeBSportType` is `BSportType.BIKE`, `BSportType.RUN`, `BSportType.UNKNOWN` (Other), or `null` (All).<br>3. *Sport-Aware Equipment Selection Logic Verification*:<br>• Verify `availableEquipment` filtering logic when filtering by `activeBSportType` (Bike, Run, Other, All) and `localSportId`.<br>• Verify automatic clearing (`localEquipId = null`) when active equipment does not belong to the selected sport/tab.<br>4. *Filter Bottom Sheet Component Verification*:<br>• Verify interval input controls for distance, duration, date pickers, and reactive chip state.<br>5. *Clean-Room Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`) to ensure zero regressions across all modules. | Workouts are accurately filtered by date interval, distance interval, and duration interval; sport type selector displays sports contextual to the active tab; equipment selector displays equipment contextual to the active sport/tab; JSON serialization is backward-compatible; 100% unit tests pass with zero regressions. | Verified |
| **TST-UI-111** | `ATT-1118` | **Retired Equipment Isolation in Pickers, Visual Identification & Equipment Lifecycle Management Test** | `REQ-UI-158` | 1. *Unit Test `EquipmentData` and `EquipmentDbHelper`*:<br>• Verify `getEquipmentItems()` maps `isRetired` from the `Retired` column.<br>• Verify `updateEquipment` and `setEquipmentRetired` persist changes to `Retired`.<br>2. *Unit Test `EditWorkoutViewModel`*:<br>• Verify active equipment list filters out retired items for workouts without equipment or with active equipment.<br>• Verify when workout has an existing retired equipment assigned, that specific retired equipment is retained in `_equipmentNames` alongside active items.<br>• Verify expansion functions (`showAllBikes`, `showAllShoes`) exclude retired equipment except when assigned.<br>3. *Unit Test `EquipmentAndSportTypeDiscoveryManager`*:<br>• Verify `getLinkedEquipmentIds` and `resolveIdentity` exclude equipment marked `isRetired == true`.<br>4. *Unit Test `SportTypeViewModel`*:<br>• Verify `availableEquipment(bSportType)` returns only active equipment.<br>5. *Unit Test `EquipmentViewModel`*:<br>• Verify `loadEquipment` populates `EquipmentItem.isRetired`.<br>• Verify updating equipment persists `isRetired` to database.<br>6. *UI Component Verification*:<br>• Verify `EditEquipmentDialog` contains retirement toggle switch and emits updated item.<br>• Verify `EquipmentTabsScreen` renders retired badge and handles sectioning/collapsible state.<br>7. *Clean-Room Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`) to ensure zero regressions across all modules. | Retired equipment is hidden from workout pickers unless assigned to the current workout; retired equipment is excluded from auto-inference; equipment manager displays visual retired indicators and grouping; manual retirement toggle functions properly; historical workouts preserve equipment name and icon with zero regressions. | Verified |
| **TST-DAT-006** | `ATT-892`, `ATT-915`, `ATT-921` | **TCX Lap Serialization, Parsing, Round-Trip & Escaping Verification** | `REQ-DAT-012` | 1. *TCX Export Formatting & Schema Conformance Unit Test (`TCXFileWriterLapTest.kt`)*:<br>• Create a mock workout with annotated laps (Lap 1: Name="Warm-up", Desc="10 min easy"; Lap 2: Name="Interval & Hill", Desc="Zone 4 <hard>"; workout description="Morning ride").<br>• Execute `TCXFileWriter.doExport(exportInfo)`.<br>• Verify generated XML conforms to `TrainingCenterDatabasev2.xsd` with `<Notes>` and `<Extensions>` strictly placed after `</Track>` and before `</Lap>`.<br>• Verify Lap 1 contains `<Notes>[Warm-up] 10 min easy</Notes>`.<br>• Verify Lap 2 contains XML-escaped `<Notes>[Interval &amp; Hill] Zone 4 &lt;hard&gt;</Notes>`.<br>• Verify structured `<Extensions>` contains `<att:LapExtension>` with `<att:Name>` and `<att:Description>` verbatim.<br>• Verify `<Activity>` contains `<Notes>Morning ride</Notes>` before `<Creator>`.<br>2. *TCX Import Parsing Unit Test (`LegacyImportEngineLapTest.kt`)*:<br>• Verify bracket format parsing: `<Notes>[Recovery] Easy spin</Notes>` extracts `name = "Recovery"`, `description = "Easy spin"`.<br>• Verify name-only format: `<Notes>[Warm-up]</Notes>` extracts `name = "Warm-up"`, `description = null`.<br>• Verify multiline format: extracts line 1 as name and remaining lines as description.<br>• Verify structured extension extraction: `<att:Name>` and `<att:Description>` directly populate `ParsedLap`.<br>• Verify XML escaping round-trip: characters like `&`, `<`, `>`, `"`, `'` are restored identically.<br>• Verify legacy file compatibility: TCX without lap notes imports safely with null name and description.<br>3. *Database Insertion & Round-Trip Verification*:<br>• Export a workout with custom laps and re-import it via `LegacyImportEngine.importFromTcx`.<br>• Query `LapsDatabaseManager.getLaps(importedWorkoutId)` and verify exact equality of lap names and descriptions.<br>4. *Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`) to ensure zero regressions across all modules. | TCX files serialize lap name and description in both human-readable `<Notes>` and structured `<Extensions>`; TCX import accurately parses both formats and populates `Laps.db`; full round-trip preservation is verified with zero regressions. | Verified |
| **TST-DAT-007** | `ATT-922`, `ATT-924` | **TCX Workout Name & Description Bidirectional Export, Import, Round-Trip & Cluster Protection Verification** | `REQ-DAT-013` | 1. *TCX Export Serialization Unit Test (`TCXFileWriterWorkoutNameTest.kt`)*:<br>• Create mock workout with custom name (`"Tempo Run & Hills"`), description (`"Zone 3-4 <hard>"`), and laps.<br>• Verify `BaseFileWriter.getHeaderData()` populates `workoutName` from `WorkoutSummaries.WORKOUT_NAME`.<br>• Execute `TCXFileWriter.doExport(exportInfo)`.<br>• Verify XML schema compliance: `<Activity Sport="...">` contains `<Notes>[Tempo Run &amp; Hills] Zone 3-4 &lt;hard&gt;</Notes>` before `<Creator>`.<br>• Verify structured `<Extensions>` contains `<att:ActivityExtension>` with `<att:Name>Tempo Run &amp; Hills</att:Name>` and `<att:Description>Zone 3-4 &lt;hard&gt;</att:Description>`.<br>• Verify export with name only (`[Name]`) and description only (`Description`).<br>2. *TCX Import Deserialization Unit Test (`LegacyImportEngineWorkoutNameTest.kt`)*:<br>• Parse TCX with `<att:ActivityExtension>`: verify `workoutName` and `description` are correctly extracted.<br>• Parse TCX with standard bracket `<Notes>[Morning Ride] Easy pace</Notes>`: verify name="Morning Ride", desc="Easy pace".<br>• Parse TCX with `<Training><Plan><Name>Speed Session</Name></Training>`: verify name="Speed Session".<br>• Verify XML character unescaping (`&`, `<`, `>`, `"`, `'`).<br>• Verify legacy TCX without workout name gracefully falls back to `fileBaseName`.<br>3. *Round-Trip & Cluster Overwrite Immunity Test*:<br>• Export workout with custom name `"Interval Session"`.<br>• Import exported TCX file via `LegacyImportEngine`.<br>• Verify `WorkoutSummaries.WORKOUT_NAME` equals `"Interval Session"`.<br>• Run `WorkoutClusterEngine.assignClusterToWorkout()` on the imported workout: verify `WORKOUT_NAME` remains `"Interval Session"` and is NOT overwritten by the cluster name.<br>4. *Clean-Room Regression Verification*:<br>• Execute full unit test suite (`./gradlew testDebugUnitTest`) to ensure zero regressions across all modules. | TCX files serialize workout name and description in `<Notes>` and `<att:ActivityExtension>`; TCX import restores `WORKOUT_NAME` and `DESCRIPTION`; custom names are immune to cluster overwrites; zero test regressions. | Verified |
| **TST-TRK-002** | `ATT-896` | **Workout Lap Lifecycle, Pause Neutrality & Zero-Duration Phantom Prevention** | `REQ-TRK-002` | 1. *Pause/Resume Neutrality Unit Test (`TrainingApplicationLapTest.kt`)*:<br>• Verify that `TrainingApplication.pauseTracking()` transitions `cTrackingMode` to `PAUSED` without broadcasting `REQUEST_NEW_LAP`.<br>• Verify that `TrainingApplication.resumeFromPaused()` transitions `cTrackingMode` to `TRACKING` without broadcasting `REQUEST_NEW_LAP`.<br>2. *Workout Finalization & Zero-Duration Guard Unit Test (`TrackerServiceLapTest.kt`)*:<br>• Simulate workout tracking with 1 active lap. Pause tracking, then trigger `TrackerService.endWorkout()`.<br>• Verify that `endWorkout()` / `createNewLap()` does NOT persist a second zero-duration lap to `LapsDatabaseManager`.<br>• Verify that for a workout with duration 1200s and distance 3000m without manual laps, exactly 1 lap record is written with 1200s and 3000m.<br>• Verify that for a workout with 1 manual lap at 500s and final stop at 1200s, exactly 2 laps are written (Lap 1: 500s, Lap 2: 700s) and zero phantom laps.<br>3. *Database Input Guard & Query Filtering Unit Test (`LapsDatabaseManagerGuardTest.kt`)*:<br>• Invoke `LapsDatabaseManager.saveLap(workoutId, 1, 0, 0.0, 0.0)`: verify 0 rows are inserted into `Laps.TABLE`.<br>• Directly insert a raw legacy row with `timeTotal_s = 0, distanceTotal_m = 0.0` into `Laps.TABLE`. Query `getLaps(workoutId)` and `getLapsForWorkouts(listOf(workoutId))`: verify the zero-duration record is excluded from results.<br>4. *Clean-Room Regression Verification*:<br>• Execute full clean-room unit test suite (`./gradlew testDebugUnitTest`): BUILD SUCCESSFUL (0 regressions). | 1. Pausing and resuming does not split or increment laps.<br>2. Single-lap tracked workouts contain exactly 1 lap with correct duration and distance, with 0 phantom laps.<br>3. Workouts with $N$ manual laps contain exactly $N+1$ laps.<br>4. `LapsDatabaseManager` rejects zero-duration writes and filters legacy zero-duration rows.<br>5. 100% of unit tests pass with zero regressions. | Verified |








